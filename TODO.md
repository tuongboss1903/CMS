# TODO — CMS Đa Website

> Theo dõi tiến độ theo Phase/Task. Task ID dạng `CMS-XXX`, đánh số tuần tự theo thứ tự triển khai thực tế (không nhảy số).

## Phase 1 — Core Framework Foundation

Mục tiêu: dựng khung core tự viết (không framework nền), đủ để boot 1 request qua Router → Middleware → View/JSON. Chi tiết xem `cms-architecture-proposal.md` mục 2 và phần "QUYẾT ĐỊNH CHÍNH THỨC".

- [x] **CMS-001** — Khởi tạo project skeleton
  - [x] Tạo cấu trúc thư mục đầy đủ (`app/`, `core/`, `modules/`, `plugins/`, `themes/`, `public/`, `storage/`, `resources/`, `database/`, `config/`, `docs/`)
  - [x] `.gitkeep` cho các thư mục rỗng
  - [x] `composer.json` với PSR-4 autoload (`Core\`, `App\`, `Modules\`)
  - [x] `.gitignore`
  - [x] `git init` + commit đầu tiên — đã chạy thủ công (xác nhận qua `.git/COMMIT_EDITMSG`, `.git/index`).
  - [x] `composer install` / `composer dump-autoload` — đã chạy thủ công (xác nhận qua `vendor/autoload.php`).
- [x] **CMS-002** — Config loader + file cấu hình
  - [x] `core/Config.php` — sửa từ static class (vi phạm "No Global Variable"/"Dependency Injection") sang instance-based, inject `configPath` qua constructor
  - [x] `config/app.php`, `config/database.php` — sửa để đọc qua `getenv()` với default an toàn thay vì hard-code `debug=true`/`env=development` trực tiếp trong file version-controlled
  - [x] `config/cache.php`, `config/auth.php`, `config/tenants.php` — tạo mới (còn thiếu so với danh sách đã chốt trong `cms-architecture-proposal.md`)
  - [x] Dọn thư mục thừa `config/core/` (rỗng, không thuộc kiến trúc)
- [x] **Architecture/Code Review (sau CMS-002)** — phát hiện `public/index.php` (tự tạo, không thuộc task nào) vẫn gọi API static cũ của `Config` (`Config::init()`/`Config::get()`) → lỗi runtime `Call to undefined method`. Đã sửa: chuyển sang instance-based (`new Config(...)`), bỏ echo trực tiếp `database.connections.mysql.host` ra output (rủi ro rò rỉ thông tin hạ tầng). Kết luận: Phase 1 (CMS-001, CMS-002) đạt tiêu chuẩn sau khi sửa.
- [ ] **Kỹ thuật nợ cần theo dõi**: chưa có cơ chế nạp `.env` (`config/*.php` đọc `getenv()` nhưng chưa quyết định dotenv library hay set qua server) — chưa gây lỗi vì luôn có default, cần xử lý trước khi deploy production, chưa thuộc phạm vi task nào hiện tại.
- [ ] **Quyết định kiến trúc cần chốt trước Phase 3** (phát hiện ở Architecture Review tổng thể sau CMS-005): `Database` và `View` đều `final class`, không có interface (`DatabaseInterface`/`ViewInterface`). Hiện chấp nhận được (YAGNI, chỉ 1 implementation), nhưng phải quyết định rõ trước khi module Auth/User đầu tiên viết Repository/Controller — thêm interface sau khi nhiều module đã phụ thuộc sẽ là breaking change tốn kém.
- [x] **CMS-003** — `core/Container.php` (DI Container, PSR-11)
  - [x] Design Review — 9 điểm bổ sung: PSR-11 thật (`psr/container`), chỉ Constructor Injection, Circular Dependency detection, 3 exception (`ContainerException`, `BindingNotFoundException`, `CircularDependencyException`), singleton theo vòng đời instance (không static/global), quy ước Service Provider (chưa tạo file — hoãn tới task Plugin/Module), Reflection cache, API giữ nguyên 6 method, chuẩn bị Unit Test
  - [x] `core/Container.php`, `core/ContainerException.php`, `core/BindingNotFoundException.php`, `core/CircularDependencyException.php`
  - [x] `composer.json` — thêm `psr/container ^2.0` (require), `phpunit/phpunit ^10.5` (require-dev), autoload-dev `Tests\` → `tests/`
  - [x] `tests/Core/ContainerTest.php` + `tests/Fixtures/*.php` — 12 test case bao phủ resolve class/interface, singleton, auto-wiring đệ quy, circular dependency (đúng kịch bản `UserService → RoleService → PermissionService → UserService`), binding not found, `instance()`, scalar có/không default, `make()`, `has()`
  - [x] `phpunit.xml`
  - [x] Code Review — trace tay toàn bộ luồng logic, không phát hiện lỗi
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHPUnit 10.5.64, PHP 8.3.30), 0 Errors/Failures/Warnings/Deprecations → **CMS-003 COMPLETED — tag `v0.0.3`**
- [x] **CMS-004** — `core/Database.php` + `core/QueryBuilder.php` (PDO wrapper, transaction API)
  - [x] Design Review — 14 mục: vai trò Database (connection+execute+transaction+exception+log) tách bạch QueryBuilder (chỉ dựng SQL, luôn giao Database thực thi), Transaction API nested-safe, không Connection Pool (ghi nhận lý do), 4 Exception chuẩn hoá, Prepared Statement 100%, Multi-tenant qua `forTenant()` sugar (không business logic), Query Logging chỉ hook/interface, không tối ưu sớm, sẵn sàng multiple connections + Database-per-Tenant
  - [x] `core/Database.php`, `core/QueryBuilder.php`, `core/Database/{DatabaseException,ConnectionException,QueryException,TransactionException,IdentifierValidator,SqlCompiler}.php`
  - [x] `tests/Fixtures/config/database.php` (SQLite in-memory), `tests/Core/DatabaseTest.php`, `tests/Core/QueryBuilderTest.php` — 31 test case (transaction commit/rollback/nested, exception, query log, CRUD, whereIn/forTenant, identifier injection, edge case mảng rỗng, count()/aggregate regression, SQL injection qua identifier)
  - [x] Self Code Review — phát hiện & sửa 4 bug thật: `whereIn([])`/`insert([])`/`update([])` sinh SQL sai cú pháp; `count()` đưa `"COUNT(*) as aggregate"` qua `IdentifierValidator` như 1 identifier (phát hiện qua PHPUnit thật) → tách `core/Database/SqlCompiler.php` (`@internal`, chỉ `QueryBuilder` dùng, không Business Logic) để `count()` tự dựng SQL riêng, đồng thời đưa `QueryBuilder.php` về dưới 300 dòng
  - [x] Self Architecture Review — đối chiếu đủ các nguyên tắc đã chốt, không phát hiện vấn đề
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHPUnit 10.5.64, PHP 8.3.30), 0 Errors/Failures/Warnings/Deprecations → **CMS-004 COMPLETED — tag `v0.0.4`**
- [x] **CMS-005** — `core/View.php` (Theme Engine PHP thuần)
  - [x] Design Review + 9 điều chỉnh: View Resolution 2 cấp cố định (active theme → default theme → `ViewNotFoundException`, bỏ `addPath()`/N-path đã đề xuất trước), Layout 1 cấp (không đa cấp), Data Binding chỉ qua `render()` (không global/static), Escape tường minh (không tự động), Partial/Component dùng chung `include()`, Exception rút gọn còn 2 lớp (`ViewException`, `ViewNotFoundException`)
  - [x] `core/View.php`, `core/View/ViewException.php`, `core/View/ViewNotFoundException.php`
  - [x] `tests/Fixtures/themes/{active,default}/views/*` (fixture 2 theme) + `tests/Core/ViewTest.php` (15 test: resolve/dot notation/theme fallback/view not found/path traversal/data binding/escape/layout+section+yield/partial/section không cân bằng/reset state) + `tests/Core/ViewContainerIntegrationTest.php` (regression: ráp qua Container CMS-003)
  - [x] Self Code Review — phát hiện & sửa rò rỉ output buffer khi `section()` gọi không cân bằng (cả trong test lẫn production `render()` — thêm cơ chế tự dọn buffer trong `finally`)
  - [x] Self Architecture Review — đối chiếu đủ 9 nguyên tắc, không phát hiện vấn đề
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHPUnit 10.5.64, PHP 8.3.30), 0 Errors/Failures/Warnings/Deprecations → **CMS-005 COMPLETED — tag `v0.0.5`**
- [x] **CMS-006** — `core/Router.php` (Router + HTTP Layer: Request/Response/Route/Middleware Pipeline/Controller Resolver)
  - [x] Design Review + 10 điểm bổ sung: Router chỉ Routing (không Database/View/Auth/Validation), Request/Response Immutable (`new self(...)` thay vì `clone` — PHP 8.1 không cho gán lại `readonly` trong `__clone()`), Middleware Pipeline Onion (Before/After + short-circuit), Route Parameter không ép kiểu/validate, Duplicate Route throw ngay lúc đăng ký (không đợi runtime), Group chỉ merge prefix/middleware/domain, phân biệt rõ 404/405, Controller Contract (chỉ nhận Request, phải trả Response), tách `ControllerResolver` riêng theo sơ đồ `Middleware Pipeline → Controller Resolver → Controller`, HTTP layer tự viết nhẹ (không PSR-7/15, giữ tinh thần "không framework nền")
  - [x] `core/Http/{Request,Response}.php`, `core/Route.php`, `core/Router.php`, `core/Router/{ControllerResolver,RouteNotFoundException,MethodNotAllowedException,DuplicateRouteException}.php`, `core/Middleware/{MiddlewareInterface,MiddlewarePipeline}.php`
  - [x] `tests/Fixtures/Http/*.php` (8 fixture) + `tests/Core/RouterTest.php` (14 test) + `tests/Core/Router/ControllerResolverTest.php` (3 test) + `tests/Core/Http/RequestTest.php` (3 test) + `tests/Core/RouterContainerDatabaseViewIntegrationTest.php` (1 test — Regression: Router ráp đúng Container+Database+View, không sửa 3 file đó)
  - [x] Self Code Review — trace tay toàn bộ 21 test case (đặc biệt thứ tự Onion và phân biệt 404/405), không phát hiện lỗi
  - [x] Self Architecture Review — đối chiếu đủ 10 nguyên tắc, không phát hiện vấn đề
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật, 0 Errors/Failures/Warnings/Deprecations → **CMS-006 COMPLETED — tag `v0.0.6`**
  - [x] **Architecture Review tổng thể HTTP Layer (sau khi Completed)** — phát hiện `Request::fromGlobals()` chỉ đọc `$_POST`, bỏ sót JSON body (PHP không tự điền `$_POST` khi `Content-Type: application/json`) → sẽ chặn cứng `POST /api/v1/auth/login` (module Auth, Phase 3) ngay khi bắt đầu. Đã vá: `resolveBody()` đọc `php://input` + `json_decode` khi Content-Type là JSON, fallback `$_POST` cho form thường; sửa `extractHeaders()` bắt thêm `CONTENT_TYPE`/`CONTENT_LENGTH` (2 header không có tiền tố `HTTP_` trong `$_SERVER`, khác mọi header khác). Thêm 3 test (`tests/Core/Http/RequestFromGlobalsTest.php`, dùng stream wrapper giả lập `php://input`). **Verified** — xác nhận không phá vỡ test cũ.
  - [ ] Ghi nhận (không chặn): `Response::json()` chưa tự bọc chuẩn `{success, data, message, errors}` — cân nhắc thêm `Response::apiSuccess()/apiError()` khi module API đầu tiên viết, không bắt buộc phải làm ở CMS-006/007. `dispatch()` ném exception (không trả `Response`) cho 404/405 — CMS-011/012 phải bắt và map thành `Response`, ghi rõ ở đây để không quên.
- [x] **CMS-007** — `core/Session.php` (wrapper duy nhất quanh PHP Session)
  - [x] Design Review + 8 điểm bổ sung: Session chỉ Storage (không login/logout/check quyền), Lazy Start (không tự start trong constructor), Flash đúng vòng đời 1 request (hết hạn theo tuổi request, không phải "xoá khi đọc"), Namespace dot-notation lồng nhau giống `Config::get()` (`auth.user_id/roles/permissions`, `csrf.token`, `locale.current`, `tenant.current`), Security (`regenerate()`, `destroy()` xoá cả cookie, cookie params đầy đủ từ Config), API tối giản (không `push()/increment()/decrement()`)
  - [x] `core/Session.php`, `core/SessionException.php` (guard gọi trước `start()`)
  - [x] `tests/Fixtures/config/auth.php` (fixture) + `tests/Core/SessionTest.php` (13 test — mô phỏng 3 "request" trong 1 test qua `session_write_close()`/`start()` lại để test đúng vòng đời Flash)
  - [x] Self Code Review — trace tay Flash lifecycle qua 3 "request" mô phỏng, không phát hiện lỗi
  - [x] Self Architecture Review — đối chiếu đủ 8 nguyên tắc, không phát hiện vấn đề
  - [x] **Sửa Risky Test** (phát hiện qua PHPUnit thật) — `RouterTest::testDoesNotConfuseNotFoundWithMethodNotAllowed` dựa vào type của `catch` để "xác minh ngầm", không có assertion thật → PHPUnit báo Risky. Viết lại: `catch (Throwable)` rộng + `assertInstanceOf()`/`assertNotInstanceOf()` tường minh cho cả 2 chiều (404 không được là 405, 405 không được là 404) trong cùng 1 test — không dùng `@doesNotPerformAssertions`. Rà soát toàn bộ `catch` khác trong test suite, xác nhận không còn risky nào khác.
  - [x] **Sửa Deprecation** (phát hiện qua PHPUnit thật, root cause do người dùng phân tích) — `Tests\Fixtures\Http\PhpInputStreamStub` không khai báo property `$context` tường minh; PHP Stream Wrapper API tự gán `$stream->context` khi đăng ký wrapper → PHP 8.2+ báo "Creation of dynamic property" Deprecated. Sửa: khai báo `public mixed $context = null;` tường minh (không dùng `#[AllowDynamicProperties]` vì chỉ che cảnh báo, không sửa gốc). Rà soát toàn bộ project — xác nhận đây là stream wrapper tự viết DUY NHẤT, không còn chỗ nào khác.
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (94 tests, 140 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations → **CMS-007 COMPLETED — tag `v0.0.7`**
- [x] **CMS-008** — `core/Cache.php` + `core/Cache/{CacheDriver,FileCacheDriver,RedisCacheDriver,CacheException}.php` (Cache Layer)
  - [x] Design Review: interface `CacheDriver` tối giản (không tag ở driver), Tag support đặt ở `Cache` facade (registry key portable qua mọi driver), Tenant key là quy ước đặt tên (không phải API riêng, nhất quán `Database`/`QueryBuilder`), `RedisCacheDriver` dùng `ext-redis` (không thêm composer dependency), Redis test có điều kiện (`markTestSkipped` nếu không có Redis thật)
  - [x] `core/Cache.php`, `core/Cache/CacheDriver.php`, `core/Cache/FileCacheDriver.php` (atomic write: file tạm + `rename()`, chống corrupt khi nhiều PHP-FPM worker ghi đồng thời), `core/Cache/RedisCacheDriver.php`, `core/Cache/CacheException.php`
  - [x] `tests/Core/Cache/FileCacheDriverTest.php` (8 test), `tests/Core/CacheTest.php` (8 test — facade, tag, prefix, `remember()`), `tests/Core/Cache/RedisCacheDriverTest.php` (4 test, tự skip nếu môi trường không có Redis), `tests/Core/CacheContainerIntegrationTest.php` (1 test — Regression: ráp qua Container)
  - [x] Self Code Review — trace tay toàn bộ 21 test case mới (đặc biệt cơ chế tag qua registry key và atomic write), không phát hiện lỗi. Không sửa file nào của 7 Core Component trước đó — không có rủi ro regression từ phía đó.
  - [x] Self Architecture Review — SOLID/KISS/DRY/YAGNI/Security/Performance/Testability đều đạt, không phát hiện vấn đề
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (115 tests, 170 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (`RedisCacheDriverTest` — môi trường không có `ext-redis`) → **CMS-008 COMPLETED — tag `v0.0.8`**
  - [x] **Architecture Review riêng Cache Layer (sau khi Completed)** — phát hiện & sửa: `RedisCacheDriver::connection()` chỉ bắt `RedisException`, nhưng `ext-redis` mặc định KHÔNG ném exception cho `auth()`/`select()` thất bại (chỉ trả `false`) → sai password/database index bị bỏ qua âm thầm. Sửa: kiểm tra tường minh giá trị trả về của `connect()/auth()/select()`, ném `CacheException` rõ ràng ngay tại điểm lỗi. Ghi nhận Technical Debt: File cache không có cơ chế dọn định kỳ entry hết hạn chưa đọc lại (lazy-only expiry), registry tag không có TTL riêng — cần cron dọn khi lên production thật.
- [x] **CMS-009** — `core/Hook.php` (Hook System kiểu WordPress — Action/Filter/priority/wildcard, qua Container)
  - [x] Design: unified registry cho Action+Filter (khác nhau ở cách gọi `do()`/`apply()`, không khác cách đăng ký — đúng cách WordPress làm bên trong), priority mặc định 10 (số nhỏ chạy trước), wildcard trộn đúng thứ tự priority với exact match (không tách riêng trước/sau), cách ly lỗi try/catch riêng từng callback + điểm mở `onError()` (đúng `13-module-plugin.md`), không static/global/ham global, instance duy nhất trong 1 request qua Container
  - [x] `core/Hook.php`
  - [x] `tests/Core/HookTest.php` (17 test: priority, insertion order, filter chain, remove, wildcard, cách ly lỗi, onError, truyền tham số) + `tests/Core/HookContainerIntegrationTest.php` (2 test — Regression: singleton qua Container, auto-wire không cần bind tường minh)
  - [x] Self Code Review — trace tay toàn bộ 19 test case (đặc biệt thứ tự trộn wildcard+exact theo priority, và filter giữ nguyên giá trị khi 1 callback lỗi giữa chuỗi), không phát hiện lỗi. Không sửa file nào của 8 Core Component trước đó.
  - [x] Self Architecture Review — SOLID/KISS/DRY/YAGNI/Security/Performance/Testability đều đạt
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (133 tests, 192 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-009 COMPLETED — tag `v0.0.9`**

## Roadmap Phase 1+ (phần còn lại) — CHÍNH THỨC, xác nhận ngày hôm nay

Thay thế hoàn toàn kế hoạch cũ (Middleware cụ thể / `public/index.php` riêng / Error Handler riêng / `/health` riêng). Đối chiếu đã chốt: **Error Handler + `/health`** gộp vào CMS-012 (Application/Bootstrap — nơi tự nhiên để wire global exception handling và route kiểm thử pipeline); **`TenantResolverMiddleware` + `AuthMiddleware`/`CsrfMiddleware`** gộp vào CMS-015 (Authentication — lúc đó mới thực sự cần Auth/Tenant context trong Middleware Pipeline).

- [x] **CMS-010** — Module Manager (`core/ModuleManager.php` + `core/Module/*`)
  - [x] Design: discover module qua `module.json` (glob), resolve thứ tự load bằng topological sort + phát hiện circular dependency (**cùng mô hình** `Container::resolve()` — stack `resolving`, chặn tại chỗ), `boot()` nạp `routes.php` của module đã bật vào `Router` qua closure cô lập scope. Không tự query DB để biết module nào "bật" — nhận `enabledKeys` từ bên ngoài (giữ core trung lập, nhất quán `Database`/`View`/`Cache`).
  - [x] `core/ModuleManager.php`, `core/Module/{ModuleDescriptor,ModuleException,ModuleNotFoundException,CircularModuleDependencyException}.php`
  - [x] `tests/Fixtures/Modules/{Alpha,Beta,Circular1,Circular2,NoRoutes}/*`, `tests/Fixtures/ModulesInvalid/BadModule/module.json` + `tests/Core/ModuleManagerTest.php` (9 test) + `tests/Core/ModuleManagerContainerIntegrationTest.php` (1 test Regression — qua Container+Router)
  - [x] Self Code Review — trace tay toàn bộ 10 test case (đặc biệt: thứ tự load đúng dependency dù `enabledKeys` liệt kê ngược thứ tự), không phát hiện lỗi. Không sửa file nào của 9 Core Component trước đó.
  - [x] Self Architecture Review — SOLID/KISS/DRY/YAGNI/Security/Performance/Testability đều đạt
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (144 tests, 212 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-010 COMPLETED — tag `v0.0.10`**
- [x] **CMS-011** — Application / Bootstrap (`core/Application.php` + `public/index.php`)
  - [x] Design Review + 4 quyết định xác nhận: `View` dùng `config('app.theme')` cho cả active/default theme (chưa có TenantManager), `ModuleManager::boot()` mặc định bật tất cả module đã `discover()`, log lỗi qua `file_put_contents` trực tiếp (chưa xây `Core\Logger` đầy đủ — ngoài phạm vi), response lỗi luôn JSON `{success,data,message,errors}` (chưa phân biệt SSR/API)
  - [x] `core/Application.php` — `handle(Request): Response` thuần (test được) tách khỏi `run(): void` (I/O boundary duy nhất). `boot()` idempotent (guard `$booted`). Debug gate cho message lỗi 500 (`config('app.debug')`). Bổ sung 1 method ngoài thiết kế đã duyệt: `container(): Container` (cần thiết để test xác nhận Core Service đăng ký đúng, đã ghi rõ trong docblock)
  - [x] Sửa `config/app.php` (thêm key `theme`), sửa `public/index.php` (thay smoke test bằng bootstrap thật, còn 3 dòng)
  - [x] `tests/Fixtures/App/*` (config đầy đủ + module + theme fixture), `tests/Fixtures/AppProduction/*` (fixture riêng cho test `debug=false`) + `tests/Core/ApplicationTest.php` (11 test: health route, module route, 404/405/500, debug gate, logging, Container wiring, singleton, boot idempotent)
  - [x] Self Code Review — trace tay toàn bộ 11 test case, không phát hiện lỗi. Không sửa file nào của 9 Core Component trước đó ngoài `config/app.php` (đã duyệt) và `public/index.php` (đúng phạm vi task)
  - [x] Self Architecture Review — SOLID/KISS/YAGNI/Security/Performance/Testability đều đạt
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (154 tests, 237 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-011 COMPLETED — tag `v0.0.11`**
- [x] **CMS-012** — Plugin Manager (`core/PluginManager.php` + `core/Plugin/*`)
  - [x] Design Review + quyết định chốt trước khi code: `PluginManager` độc lập hoàn toàn với `ModuleManager` (không refactor, không chia sẻ abstraction, chấp nhận trùng lặp logic topological sort để giữ ổn định), `discover()` memoize trong instance (khác chủ đích với `ModuleManager::discover()`), `boot()` reset `failures` mỗi lần gọi, cách ly lỗi tuyệt đối ở tầng `require Hooks.php` (không rethrow), giữ public API tối giản (không thêm interface/abstraction ngoài phạm vi)
  - [x] `core/PluginManager.php`, `core/Plugin/{PluginDescriptor,PluginException,PluginNotFoundException,CircularPluginDependencyException}.php`
  - [x] `tests/Fixtures/Plugins/{GoodPluginA,GoodPluginB,BrokenPlugin,NoHooksPlugin,CircularA,CircularB,ScopeCheckPlugin}/*`, `tests/Fixtures/PluginsInvalid/BadPlugin/plugin.json`, `tests/Fixtures/PluginsDuplicate/{PluginX,PluginY}/plugin.json` + `tests/Core/PluginManagerTest.php` (16 test) + `tests/Core/PluginManagerContainerIntegrationTest.php` (2 test Regression)
  - [x] Sửa `core/Application.php` — CHỈ 2 điểm: đăng ký `PluginManager` vào Container, gọi `pluginManager->boot()` trong `boot()` ngay sau `ModuleManager`, không sửa gì khác
  - [x] Self Code Review — trace tay toàn bộ 18 test case (đặc biệt: thứ tự nạp theo dependency qua filter chain `plugin.trace`, cô lập lỗi 1 plugin không ảnh hưởng plugin khác, reset `failures` mỗi lần `boot()`, scope `Hooks.php` chỉ thấy đúng `$hook`), không phát hiện lỗi. Không sửa file nào của `ModuleManager`/`Hook`/`Container`/9 Core Component trước đó ngoài `core/Application.php` (đúng phạm vi đã duyệt)
  - [x] Self Architecture Review — SRP/Dependency/KISS/YAGNI/không premature optimization/không trùng Public API/`ModuleManager` không bị ảnh hưởng/không phát sinh Technical Debt mới ngoài trùng lặp đã được chấp nhận trước — đều đạt
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (171 tests, 273 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-012 COMPLETED — tag `v0.0.12`**
- [x] **CMS-013** — Migration System (`core/MigrationManager.php` + `core/Migration/*` + `bin/migrate.php`)
  - [x] Adversarial Architecture Review trước khi code — phát hiện & xử lý: driver detection không được tự đọc PDO (`getAttribute()`), không thêm API mới cho `Database`; Container trong `bin/migrate.php` sẽ trùng lặp logic wiring `Database` với `Application.php` nên bị loại; định dạng migration file chọn Closure-array (không interface, không class, không DSL)
  - [x] Final Design 8 quyết định chốt: (1) Closure-array `['up'=>Closure,'down'=>Closure]`, (2) DDL raw SQL qua `Database::statement()` — không Schema Builder, (3) rollback theo batch, (4) CLI entry point riêng `bin/migrate.php` — không sửa `Application.php`/`public/index.php`, (5) không dùng Container trong CLI, (6) fail-fast tuyệt đối (khác `ModuleManager`/`PluginManager`), (7) giữ `status()` trong Public API, (8) `driver: string` truyền qua constructor từ `Config`, `MigrationManager` không biết PDO
  - [x] `core/MigrationManager.php`, `core/Migration/{MigrationException,MigrationNotFoundException}.php`, `bin/migrate.php`
  - [x] `tests/Fixtures/Migrations/{Valid,Failing,Malformed}/*` + `tests/Core/MigrationManagerTest.php` (16 test: discover, migrate thành công, batch tăng dần qua nhiều lần chạy, status applied/pending, rollback theo batch đảo ngược, rollback rỗng khi chưa có batch, malformed migration, fail-fast dừng ngay khi lỗi, `MigrationNotFoundException` khi file bị xoá, validate driver, regression không ảnh hưởng bảng khác)
  - [x] Không sửa file nào trong 12 Core Component đã hoàn thành (`Application`/`Database`/`Container`/`Config`/`Router`/`View`/`Session`/`Cache`/`Hook`/`ModuleManager`/`PluginManager`) — 0 file Core cũ bị đụng, đúng Decision #4/#5/#8
  - [x] Self Code Review — trace tay toàn bộ 16 test case, phát hiện 1 sai sót đặt tên (không phải bug logic): fixture ban đầu dùng `ALTER TABLE ... DROP COLUMN` (rủi ro không tương thích SQLite cũ) + tên file không khớp nội dung — đã sửa thành 2 migration `CREATE TABLE` độc lập
  - [x] Self Architecture Review — SRP (chấp nhận mức gộp trách nhiệm tương đương `ModuleManager`), Dependency (không PDO, không Container, không API mới cho `Database`), KISS/YAGNI đạt
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (185 tests, 299 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-013 COMPLETED — tag `v0.0.13`**
- [x] **CMS-014** — Validation Layer (`core/Validator.php` + `core/Validation/*`)
  - [x] Design Review + 6 quyết định chốt: registry rule dạng closure nội bộ (không class-per-rule), trả `ValidationResult` thay vì throw cho input sai (chỉ throw `ValidationException` khi rule name không tồn tại — lỗi cấu hình), không đăng ký Container/Application (giống `MigrationManager`, không có state cần singleton), chỉ 1 rule format string kiểu Laravel, giữ `extend()` custom rule, validate hết toàn bộ rule của 1 field (không bail)
  - [x] `core/Validator.php`, `core/Validation/{ValidationResult,ValidationException}.php`
  - [x] `tests/Core/ValidatorTest.php` (31 test: passes/fails/errors/firstError, 16 rule built-in, custom rule qua `extend()`, custom message, unknown rule throw, field optional bị bỏ qua, nhiều lỗi tích luỹ trên 1 field)
  - [x] Không sửa file nào trong 13 Core Component đã hoàn thành — 0 dependency vào Config/Container/Database/Router/Session/Hook/ModuleManager/PluginManager/MigrationManager
  - [x] Self Code Review — phát hiện & sửa 2 vấn đề: (1) test tự viết sai logic (`min`/`max` mâu thuẫn số học trên cùng 1 giá trị) — sửa lại rule độc lập không mâu thuẫn; (2) bug thật trong rule `in` — ép `(string) $value` trực tiếp gây PHP Warning nếu `$value` là mảng, sửa bằng guard `is_scalar()`
  - [x] Self Architecture Review — SRP/Coupling/Cohesion/KISS/YAGNI đạt, 0 dependency vào Core Component khác
  - [x] **Verified** — `vendor/bin/phpunit` PASS (bao gồm trong lần chạy toàn bộ suite cùng CMS-015, 229 tests/382 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-014 COMPLETED — tag `v0.0.14`**
- [x] **CMS-015** — HTTP Request Layer (mở rộng `core/Http/Request.php` đã có từ CMS-006)
  - [x] Architecture Analysis phát hiện xung đột quan trọng: yêu cầu ban đầu mô tả trùng trách nhiệm với `Core\Http\Request` đã hoàn thành (v0.0.6) — chốt Decision #0: CMS-015 là MỞ RỘNG file đã có (không tạo Request thứ 2), mọi API mới additive, không breaking method cũ
  - [x] Final Design 9 quyết định chốt: giữ nguyên 8 method cũ (chỉ thêm alias), tham số constructor mới (`files/cookies/server`) đặt cuối với default `[]`, không Container/singleton, copy dữ liệu 1 lần, giữ eager JSON parsing, không phụ thuộc Session/Validator/Database/Auth, không Method Spoofing (`_method`), không Trusted Proxy (`ip()` chỉ đọc `REMOTE_ADDR`)
  - [x] Sửa `core/Http/Request.php` — CHỈ additive: thêm `files/cookies/server` (cuối constructor), `fromGlobals()` đọc thêm `$_FILES/$_COOKIE/$_SERVER`, `withRouteParams()` giữ nguyên 3 property mới, thêm 13 method (`method/uri/path/all/has/filled/cookie/file/ip/userAgent/isMethod/ajax/json`)
  - [x] `tests/Core/Http/RequestTest.php` (+14 test), `tests/Core/Http/RequestFromGlobalsTest.php` (+1 test)
  - [x] Rà soát toàn bộ ~30 lệnh gọi `new Request(...)` hiện có trong codebase — xác nhận 100% backward compatible (đều dùng ≤7 tham số, không bị ảnh hưởng bởi 3 tham số mới)
  - [x] Self Code Review — phát hiện & sửa 2 failure PHPUnit thật: test `ajax()`/`json()` tự viết dùng header key sai convention (chưa uppercase, trong khi `header()` chỉ chuẩn hoá phía tra cứu, không chuẩn hoá key đã lưu — hành vi đã có từ CMS-006, không sửa production code) — sửa đúng phạm vi trong test data
  - [x] Self Architecture Review — 0 dependency mới, đúng cả 9 Decision, không breaking Public API
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (229 tests, 382 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-015 COMPLETED — tag `v0.0.15`**
- [x] **CMS-016** — HTTP Response Layer (mở rộng `core/Http/Response.php` đã có từ CMS-006)
  - [x] Architecture Analysis + Final Design 6 quyết định: mở rộng file đã có (Decision #0), KHÔNG `apiSuccess()/apiError()` (business convention, không phải HTTP contract — Decision #1 REJECTED), thêm `withHeader()/withHeaders()/withStatus()` (Decision #2), `withCookie()` không đọc Config, giữ 0 dependency (Decision #3), KHÔNG `download()/file()` (filesystem là chủ đề riêng — Decision #4 REJECTED), thêm `withCache()/noCache()` chỉ thao tác `Cache-Control` (Decision #5)
  - [x] Sửa `core/Http/Response.php` — thêm property `cookies` (cuối constructor, default `[]`, tách riêng khỏi `headers` vì HTTP cho phép nhiều `Set-Cookie`), 7 method mới: `withHeader/withHeaders/withStatus/withCookie/withCache/noCache/getCookies`, `send()` gửi thêm `Set-Cookie` qua `header(..., false)`
  - [x] `tests/Core/Http/ResponseTest.php` (17 test — file test mới, trước đây `Response` chỉ được kiểm chứng gián tiếp qua `RouterTest`/`ControllerResolverTest`)
  - [x] Self Code Review — không phát hiện bug, trace tay xác nhận cookie cùng tên ghi đè đúng, `httponly` mặc định `true` nhưng tôn trọng giá trị `false` tường minh, chuỗi `with*()` không mutate instance gốc
  - [x] Self Architecture Review — 0 dependency mới, immutable tuyệt đối, đúng cả 6 Decision
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (243 tests, 424 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-016 COMPLETED — tag `v0.0.16`**
- [x] **CMS-017** — Redirect (Architecture Decision, KHÔNG Implementation)
  - [x] Architecture Analysis phát hiện: `Response::redirect()` (CMS-006) + `Session::flash()`/`getFlash()` (CMS-007) + `Request::all()`/`header('Referer')` (CMS-015) đã đủ để Controller tự viết pattern "redirect kèm flash message/old input" mà không cần thêm Core code nào — Decision #0 được đặt ra: có thực sự cần 1 class trung gian (`Redirector`) hay chỉ là convention?
  - [x] Phân tích rủi ro nếu tạo class trung gian: phương án gắn thẳng vào `Response`/`Session` (mở rộng file đã có) sẽ **phá vỡ nguyên tắc "Response/Session độc lập tuyệt đối"** đã xuyên suốt từ CMS-001 và vừa tái khẳng định ở Decision #3 của CMS-016 (giữ `Response` 0 dependency) — xác định đây là xung đột kiến trúc thật, không phải chi tiết implementation
  - [x] **Owner quyết định (Decision #0): KHÔNG cần Core code mới** — đúng YAGNI, chưa có Module Auth/Form thật nào cần pattern này để chứng minh nhu cầu lặp lại đủ nhiều. Ghi nhận **convention chính thức**: Controller tự gọi `$session->flash($key, $value)` (có thể gọi nhiều lần cho nhiều field) rồi `return Response::redirect($location)` — không qua lớp trung gian nào
  - [x] Technical Debt ghi nhận: nếu Module Auth/Form sau này (Phase 3+) thực sự lặp lại boilerplate này ở ≥2 nơi, xem xét lại việc tạo `core/Redirector.php` (phương án C đã phân tích — service riêng, nhận `Session` qua constructor, KHÔNG đụng `Response.php`/`Session.php` đã hoàn thành) — không tạo trước khi có nhu cầu thật, đúng nguyên tắc "không tạo abstraction chỉ để DRY"
  - [x] **CMS-017 đóng bằng Architecture Decision — không có file tạo/sửa, không có PHPUnit run, không có tag mới** (không phải Implementation task)
- [x] **CMS-018** — Middleware Pipeline Extension (mở rộng `core/Router.php`, Middleware Pipeline gốc từ CMS-006 giữ nguyên)
  - [x] Architecture Analysis phát hiện: `MiddlewareInterface`/`MiddlewarePipeline` đã tồn tại và hoạt động từ CMS-006 (onion model, short-circuit, resolve qua Container) — phạm vi thật của CMS-018 không phải "xây Pipeline" mà là hoàn thiện cách đăng ký middleware: (1) Global Middleware (chưa có cơ chế nào áp dụng cho MỌI route), (2) Route-level middleware đơn lẻ (trước đây chỉ gán được qua `group()`)
  - [x] Final Decision: Router là owner duy nhất của middleware lifecycle (không sửa `Application.php`), giữ nguyên tuyệt đối `MiddlewareInterface`/`MiddlewarePipeline`/`process(Request,Closure):Response`/short-circuit, thứ tự onion `Global → Group → Route-specific → Controller`, loại trừ rõ: không alias middleware, không terminate hook, không plugin/event middleware
  - [x] Sửa `core/Router.php` — thêm `globalMiddleware` (property) + `middleware(array): static` (append, fluent), `get/post/put/patch/delete` thêm tham số cuối `array $middleware = []`, `addRoute()` gộp `[...groupMiddleware, ...routeMiddleware]` lúc đăng ký, `dispatch()` gộp `[...globalMiddleware, ...route->getMiddleware()]` **runtime** (không "nướng cứng" vào Route lúc đăng ký, tránh phụ thuộc thứ tự gọi `middleware()`). `core/Route.php` xác nhận **không cần sửa** (constructor vẫn nhận middleware phẳng như cũ)
  - [x] `tests/Fixtures/Http/MiddlewareC.php` (mới, tag thứ 3 để phân biệt Global/Group/Route trong 1 test) + `tests/Core/RouterTest.php` (+6 test: global chạy trên mọi route, thứ tự onion Global→Group→Route đúng, route-level chỉ áp dụng đúng route đó, route-level kết hợp đúng với group, global short-circuit chặn cả group/route/Controller, global không rò rỉ qua Router instance khác)
  - [x] Self Code Review — trace tay toàn bộ 6 test mới + xác nhận 17 test cũ không bị ảnh hưởng (tham số mới additive, default `[]`)
  - [x] Self Architecture Review — đúng Final Decision, không đụng `Application.php`/`Route.php`/`MiddlewareInterface.php`/`MiddlewarePipeline.php`
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `RouterTest.php` riêng: 19 tests/29 assertions OK; toàn bộ suite: 249 tests, 433 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-018 COMPLETED — tag `v0.0.18`**
- [x] **CMS-019** — Exception Handler (`core/ExceptionHandler.php`, tách khỏi `core/Application.php`)
  - [x] Architecture Analysis xác nhận: exception handling đã tập trung tuyệt đối tại `Application::handle()` từ CMS-011 (không duplicate ở đâu khác — `Router`/`MiddlewarePipeline`/`ControllerResolver`/`View` đều không có try/catch swallow, `View::renderTemplate()` chỉ rethrow sau khi dọn output buffer). Phát hiện xung đột cấu trúc loại bỏ Option "Middleware-based Exception Handler": `Router::match()` ném `RouteNotFoundException`/`MethodNotAllowedException` TRƯỚC KHI `MiddlewarePipeline` được gọi — 1 Middleware (kể cả Global, dù mới có ở CMS-018) không bao giờ bắt được 2 exception này
  - [x] Final Design 6 quyết định: tách `Core\ExceptionHandler` riêng (Option B, cải thiện SRP cho `Application`), mapping tĩnh không registry/extend() (YAGNI — chỉ 2/23 exception cần map riêng), debug block (exception/file/line/trace) chỉ khi `debug=true && status=500`, Logger vẫn ngoài phạm vi (giữ nguyên `logException()` tại `Application`, không chuyển vào `ExceptionHandler`), không auto-map `ValidationException`→422 (ranh giới: Controller tự catch nếu muốn), không map 403 (chưa có Auth/Authz)
  - [x] `core/ExceptionHandler.php` — `final class`, **0 dependency** (không Config/Container/Logging), 1 method `handle(Throwable, bool): Response`, mapping tĩnh 404/405/500, debug block dùng `explode("\n", getTraceAsString())` (không dùng `getTrace()` để tránh lỗi serialize JSON với object/resource không encode được)
  - [x] Sửa `core/Application.php` — gộp 3 nhánh `catch` (`RouteNotFoundException`/`MethodNotAllowedException`/`Throwable`) thành 1 `catch (Throwable)` duy nhất, delegate `ExceptionHandler`, quyết định log dựa trên `$response->getStatusCode() >= 500` (thay vì theo loại exception) — giữ **chính xác** hành vi log cũ (404/405 không log, 500 vẫn log) mà không cần `Application` biết tên class exception cụ thể nào nữa. Xoá `errorResponse()` (đã grep xác nhận 0 nơi khác dùng trước khi xoá). `isDebug()`/`logException()` không đổi 1 dòng
  - [x] `tests/Core/ExceptionHandlerTest.php` (8 test, unit thuần không Router/Application/filesystem) + `tests/Core/ApplicationTest.php` (+1 test `testRouteNotFoundDoesNotWriteToLogFile`, khoá tường minh hành vi "chỉ log status ≥ 500", 11 test cũ không sửa)
  - [x] Self Code Review — trace tay toàn bộ 8+1 test mới, đối chiếu số liệu PHPUnit thật khớp chính xác dự đoán (8 tests/20 assertions ExceptionHandlerTest, 11 tests/27 assertions ApplicationTest — đúng 10 test cũ + 1 test mới)
  - [x] Self Architecture Review — SRP cải thiện rõ rệt (`Application` chỉ boot+log-trigger+delegate, `ExceptionHandler` chỉ map), 0 dependency mới ngoài `Application→ExceptionHandler`, không đụng `Router.php`/`Route.php`/`Middleware/*`/`Response.php`/`Request.php`
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `ExceptionHandlerTest`: 8 tests/20 assertions; `ApplicationTest`: 11 tests/27 assertions; `RouterTest`: 19 tests/29 assertions; toàn bộ suite: 258 tests, 455 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-019 COMPLETED — tag `v0.0.19`**
- [x] **CMS-020** — CSRF Protection (`core/Csrf.php` + `core/Middleware/CsrfMiddleware.php`)
  - [x] Architecture Analysis phát hiện: `Session` (CMS-007) đã dự trù sẵn namespace `csrf.token` trong docblock; `cms-architecture-proposal.md` đã định vị CSRF là 1 Middleware trong pipeline gốc (`TenantResolver → Auth → Locale → CSRF (POST) → Controller`), chỉ áp dụng cho route dùng Session (Admin/SSR), không áp dụng `/api/*` (JWT) — loại trừ tự nhiên qua việc chỉ gắn middleware vào đúng group cần, không cần logic phát hiện API bên trong
  - [x] Final Design 7 quyết định: tách `Csrf` (token lifecycle, thuần logic) + `CsrfMiddleware` (tích hợp HTTP) — Option B; hardcode field/header name (`_token`/`X-CSRF-TOKEN`/`X-XSRF-TOKEN`, không Config); fail trả 419 (không 403) với envelope cố định; không `CsrfException` mới (Middleware tự trả Response, không đụng `ExceptionHandler`); token sống theo Session, không regenerate mỗi request; đặt trong Core (thuần cơ chế, không business logic); không sửa `View` (Controller tự truyền `$csrf->token()`)
  - [x] Xác nhận qua đọc trực tiếp `Container.php`: `Csrf`/`CsrfMiddleware` tự auto-wire được qua Container (fallback `class_exists()` khi không có binding tường minh) — **0 file Core cũ nào cần sửa**, kể cả `Application.php`
  - [x] Code Review Simulation (trước Implementation) phát hiện 1 gap cần xử lý: `Request::input('_token')` có thể trả về `array` (không chỉ `string`) — bắt buộc guard `is_string()` trước khi gọi `Csrf::verify()`, không được ép kiểu `(string)` mù quáng (tránh PHP Warning "Array to string conversion")
  - [x] `core/Csrf.php` (`token()`/`verify()`, `bin2hex(random_bytes(32))`, `hash_equals()`), `core/Middleware/CsrfMiddleware.php` (implement `MiddlewareInterface` có sẵn từ CMS-006, không đổi contract)
  - [x] `tests/Core/CsrfTest.php` (6 test) + `tests/Core/Middleware/CsrfMiddlewareTest.php` (11 test — 9 test chuẩn + 2 edge case bắt buộc từ Code Review: `_token` rỗng không fallback qua header, `_token` dạng mảng bị từ chối sạch không Warning)
  - [x] Self Code Review — trace tay toàn bộ 17 test mới, đối chiếu đúng số liệu PHPUnit thật (6 tests/7 assertions `CsrfTest`, 11 tests/17 assertions `CsrfMiddlewareTest`)
  - [x] Self Architecture Review — SRP đạt (2 class tách bạch), 0 file cũ bị sửa, CSRF hoàn toàn opt-in (không tự gắn route nào — việc đó thuộc Module/App sau này)
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `CsrfTest`: 6 tests/7 assertions; `CsrfMiddlewareTest`: 11 tests/17 assertions; toàn bộ suite: 275 tests, 479 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-020 COMPLETED — tag `v0.0.20`**
- [x] **CMS-021** — Authentication (`core/Auth.php` + `core/Middleware/AuthMiddleware.php`)
  - [x] Architecture Analysis phát hiện: `Session` (CMS-007) đã dự trù sẵn namespace `auth.user_id/auth.roles/auth.permissions`; không có bảng `users` nào tồn tại (chưa có migration thật); `02-module-auth.md` mô tả "Module Auth" đầy đủ (JWT/password reset/login_logs) là phạm vi Phase 3+ riêng, tách biệt khỏi CMS-021 Foundation
  - [x] Final Design (Option A): `Auth` CHỈ quản lý session-state ("đã đăng nhập hay chưa"), không verify credential, không Database, không password — việc xác thực mật khẩu against DB là trách nhiệm Module Auth tương lai, gọi `Auth::login()` SAU KHI đã tự verify xong
  - [x] `core/Auth.php` (`login()/logout()/check()/id()/user()`) — `login()` theo đúng thứ tự `regenerate()` → `remove('csrf.token')` → `set('auth.user_id')` → `set('auth.user')` (chống session fixation + rotate CSRF token khi nâng mức tin cậy); `logout()` dùng `Session::destroy()` (huỷ toàn bộ, không chỉ xoá `auth.*`)
  - [x] `core/Middleware/AuthMiddleware.php` — implement `MiddlewareInterface` có sẵn, chặn request chưa đăng nhập trả 401 JSON, không redirect, không Config
  - [x] `tests/Core/AuthTest.php` (13 test) + `tests/Core/Middleware/AuthMiddlewareTest.php` (3 test)
  - [x] Self Code Review phát hiện & sửa 1 bug thật trong TEST (không phải trong `Auth`): `Session::destroy()` khiến `isStarted()` về `false`, test kiểm tra lại trạng thái ngay sau `logout()` mà không `start()` lại session sẽ nhận `SessionException` — sửa bằng cách mô phỏng đúng "request kế tiếp tự start() lại", không đổi `Auth`/`Session`
  - [x] Xác nhận qua đọc lại `Container.php` (nhất quán CMS-020): `Auth`/`AuthMiddleware` tự auto-wire, **0 file Core cũ nào bị sửa**
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `AuthTest`: 13 tests/17 assertions; `AuthMiddlewareTest`: 3 tests/4 assertions; toàn bộ suite: 291 tests, 500 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-021 COMPLETED — tag `v0.0.21`**
- [x] **CMS-022** — Authorization (`core/Authorization.php` + `core/Middleware/AuthorizationMiddleware.php`)
  - [x] Architecture Analysis phát hiện xung đột kiến trúc thật (đã đọc trực tiếp `Container.php`/`MiddlewarePipeline.php`): cơ chế Middleware hiện tại (`list<class-string>`, `Container::get(string $id)`) không hỗ trợ tham số hoá per-route — không thể `new AuthorizationMiddleware(['admin'])` gắn trực tiếp vào 1 route cụ thể như đề xuất ban đầu
  - [x] Owner Decision: **Phương án C** — không sửa `MiddlewarePipeline`/`Router`/`Route`/`Container`, `AuthorizationMiddleware` không tham số hoá, chỉ là gate chung (chặn khi user không có role/permission nào); kiểm tra quyền cụ thể per-route là trách nhiệm Controller tự gọi `Authorization::hasRole()/can()` trực tiếp
  - [x] `core/Authorization.php` (`roles()/permissions()/hasRole()/hasAnyRole()/hasAllRoles()/hasPermission()/hasAnyPermission()/hasAllPermissions()/can()` — `can()` alias thuần của `hasPermission()`), đọc `auth.roles`/`auth.permissions` từ Session (namespace đã dự trù từ CMS-007), không ghi, không DB
  - [x] `core/Middleware/AuthorizationMiddleware.php` — chặn khi `roles()===[] && permissions()===[]`, trả 403 JSON
  - [x] `tests/Core/AuthorizationTest.php` (17 test) + `tests/Core/Middleware/AuthorizationMiddlewareTest.php` (4 test)
  - [x] **Verified** — `vendor/bin/phpunit` PASS (trong lần chạy toàn bộ suite cùng CMS-023/024) → **CMS-022 COMPLETED — tag `v0.0.22`**
- [x] **CMS-023** — Rate Limiter (`core/RateLimiter.php` + `core/Middleware/RateLimitMiddleware.php`)
  - [x] Architecture Analysis phát hiện đúng loại xung đột như CMS-022 (Middleware cần tham số `key`/`maxAttempts`/`decaySeconds` per-route, cơ chế hiện tại không hỗ trợ)
  - [x] Owner Decision: `RateLimiter` là Foundation Component thuần cơ chế (đọc/ghi/reset quota qua Session, namespace `rate_limit.{key}`, chỉ integer timestamp — không object/DateTime/serialize). `RateLimitMiddleware` là **placeholder framework component** — chỉ implement `MiddlewareInterface` và `return $next($request);`, KHÔNG tự xác định key/limit, KHÔNG gọi `hit()`. Logic rate-limit thật do Module tương lai tự gọi `RateLimiter` trực tiếp (biết đủ business context để xác định bucket, VD `hit('login:'.$ip, 5, 60)`)
  - [x] `core/RateLimiter.php` (`hit()/tooManyAttempts()/attempts()/remaining()/clear()/availableIn()`)
  - [x] `tests/Core/RateLimiterTest.php` (14 test) + `tests/Core/Middleware/RateLimitMiddlewareTest.php` (4 test — chỉ xác nhận hành vi passthrough, không test 429 vì hành vi đó không tồn tại theo Owner Decision)
  - [x] **Verified** — `vendor/bin/phpunit` PASS (trong lần chạy toàn bộ suite cùng CMS-022/024) → **CMS-023 COMPLETED — tag `v0.0.23`**
- [x] **CMS-024** — Logger (`core/Logger.php`)
  - [x] Architecture Analysis: phát hiện "CMS-024 — Cache" (đề xuất ban đầu) trùng lặp hoàn toàn với `Cache`/`CacheDriver` đã hoàn thành từ CMS-008 (tag `v0.0.8`) — dừng lại, Owner xác nhận đổi CMS-024 thành Logger. Grep toàn bộ `core/` xác nhận: chỉ 1 nơi ghi log thật (`Application::logException()`), 2 điểm mở đã dự trù từ trước chưa được nối dây (`Database::onQueryExecuted()` từ CMS-004, `Hook::onError()` từ CMS-009)
  - [x] Owner Decision: không PSR-3 (không thêm Composer package), constructor nhận `string $logPath` trực tiếp (không Config, theo tiền lệ `MigrationManager`), không level filtering/channel/formatter/handler/rotation/async/buffering — API tối giản đúng 1 method `log(level, message, context)`
  - [x] `core/Logger.php` — ghi 1 dòng `[timestamp] level: message {context json nếu có}` vào file cố định, tự tạo thư mục cha nếu thiếu (cùng cơ chế `Application::logException()`), `@file_put_contents(..., FILE_APPEND|LOCK_EX)` không throw khi ghi thất bại
  - [x] `tests/Core/LoggerTest.php` (8 test, filesystem thật/temp dir)
  - [x] Không nối dây vào `Application`/`Database`/`Hook`/`ExceptionHandler` — đúng phạm vi Foundation, việc thay thế `Application::logException()` và nối 2 điểm mở còn để dành CMS sau
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `LoggerTest`: 8 tests/10 assertions; toàn bộ suite: 338 tests, 561 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-024 COMPLETED — tag `v0.0.24`**
- [x] **CMS-025** — TenantManager (`core/TenantManager.php`)
  - [x] Architecture Analysis phát hiện: `core/View.php` (docblock từ CMS-005) đã dự trù `$activeTheme` "tương lai: TenantManager"; `config/app.php` (comment từ CMS-011) chờ "TenantManager thật (Phase 2+)"; `Session.php` đã dự trù namespace `tenant.current` từ CMS-007; `Cache`/`QueryBuilder::forTenant()` chờ nguồn cung cấp `{tenantId}` — 6 điểm mở khác nhau xác nhận gap thật, không suy đoán. Đồng thời phát hiện `TODO.md` lỗi thời so với `cms-architecture-proposal.md` gốc (thiếu hẳn `TenantManager`/`ThemeManager` trong danh sách Core Component dòng 405)
  - [x] Phát hiện giới hạn giống `Auth` (CMS-021): bảng `sites`/`site_domains` (nơi thật sự resolve domain→tenant, theo `database-design.md`) chưa tồn tại (chưa có migration) — TenantManager KHÔNG tự resolve, chỉ quản lý state "tenant hiện tại"
  - [x] Owner Decision quan trọng: **KHÔNG dùng Session** (khác `Auth`/`Authorization`/`Csrf`/`RateLimiter`) — vì tenant phải xác định cho MỌI request (kể cả API/JWT, không riêng luồng Session-based), ép `Session::start()` chỉ để biết site nào sẽ vi phạm triết lý lazy-start đã có từ CMS-007. Dùng property in-memory thuần — state tự cô lập theo từng request vì `Container` đã là 1-per-request
  - [x] `core/TenantManager.php` — `setCurrent(int|string $tenantId, array $data=[])/check()/id()/current()`, **0 dependency** (không constructor, mức cô lập cao nhất từ trước tới nay, thấp hơn cả `Logger`)
  - [x] `tests/Core/TenantManagerTest.php` (9 test, unit thuần — không Session/filesystem/DB, đơn giản nhất từ trước tới nay)
  - [x] Không sửa `View`/`Application`/`Session`/`Config`/`Database`/`Request` — đúng phạm vi Foundation, việc nối dây (`View` factory trong `Application.php` đọc `TenantManager::current()`, resolve domain→tenant qua `site_domains`) để dành CMS sau khi có migration `sites`/`site_domains` thật
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `TenantManagerTest`: 9 tests; toàn bộ suite: 347 tests, 574 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-025 COMPLETED — tag `v0.0.25`**
- [x] **CMS-026** — ThemeManager (`core/ThemeManager.php` + `core/Theme/*`)
  - [x] Architecture Analysis xác nhận ranh giới rõ ràng với `View` (đọc lại toàn văn `View.php`): `View` chỉ render (resolve file theo active/default theme đã biết trước), không hề biết theme nào tồn tại/metadata — `ThemeManager` chỉ discovery/metadata, không render. So sánh với `ModuleManager`/`PluginManager` xác nhận đây là Component mới thật, mô hình discovery-qua-manifest giống `ModuleManager` (không memoize) nhưng đơn giản hơn (không dependency resolution giữa theme). Đọc `database-design.md` xác nhận filesystem LUÔN là source of truth cho theme (không chỉ tạm thời do thiếu migration như `Auth`/`TenantManager` — đây là nguyên tắc thiết kế vĩnh viễn) → **ThemeManager không dùng Database, kể cả trong tương lai**
  - [x] Owner Decision: API chỉ `discover()/find()` (không `exists()/all()/active()/setActive()`); `ThemeDescriptor` chỉ `key/name/version/screenshot/path` (không `dependencies/boot file/enabled state/tenant_id`); **không memoize** (đối lập `PluginManager`, giống `ModuleManager` — filesystem là source of truth, theme có thể đổi runtime); không `boot()`; không đăng ký `Application`/`Container` (Foundation trước, integration để CMS sau)
  - [x] `core/ThemeManager.php` (`discover()/find()`, glob `theme.json`, không memoize), `core/Theme/ThemeDescriptor.php` (value object readonly), `core/Theme/ThemeException.php` (`cannotRead()/invalidManifest()`, mirror `ModuleException`)
  - [x] `tests/Core/ThemeManagerTest.php` (7 test) + fixture `tests/Fixtures/Themes/{Alpha,Beta}` + `tests/Fixtures/ThemesInvalid/BadTheme`
  - [x] Không sửa `View`/`Application`/`Container`/`Config`/`TenantManager` — đúng phạm vi Foundation, việc nối `ThemeManager` vào `View`/`Application` (thay `config('app.theme')` tĩnh) để dành CMS sau
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `ThemeManagerTest`: 7 tests/15 assertions; toàn bộ suite: 354 tests, 589 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-026 COMPLETED — tag `v0.0.26`**
- [x] **CMS-027** — Middleware Parameterization (`core/Middleware/MiddlewarePipeline.php`)
  - [x] Architecture Analysis xác nhận lại (đọc trực tiếp `MiddlewarePipeline.php`/`Router.php`/`Route.php`/`Container.php`/`AuthorizationMiddleware.php`/`RateLimitMiddleware.php`): giới hạn gốc đúng như phát hiện ở CMS-022/023 — `MiddlewarePipeline::handle()` chỉ chấp nhận `list<class-string>`, resolve cứng qua `Container::get(string $id)`, không có đường nào truyền instance đã cấu hình sẵn vào pipeline
  - [x] Owner Decision: **CMS-027 chỉ là infrastructure** — `MiddlewarePipeline` hỗ trợ thêm `MiddlewareInterface` instance đã cấu hình sẵn (song song class-string cũ), KHÔNG redesign `AuthorizationMiddleware`/`RateLimitMiddleware`, KHÔNG thêm permission parameter/rate limit configuration, KHÔNG đổi security policy. Invalid middleware (không phải string, không phải `MiddlewareInterface`) → `throw new \InvalidArgumentException` (không tạo Exception class mới, tránh abstraction thừa). Bắt buộc: `Container.php` không sửa, `Router`/`Route` runtime không đổi (chỉ PHPDoc)
  - [x] Sửa `core/Middleware/MiddlewarePipeline.php` — thêm `private resolve(mixed $middlewareEntry): MiddlewareInterface` (string → `Container::get()` như cũ; `MiddlewareInterface` → dùng trực tiếp; còn lại → `\InvalidArgumentException` kèm `get_debug_type()`), `handle()` gọi qua `resolve()` thay vì `Container::get()` trực tiếp, tham số closure đổi `string` → `mixed` (để lỗi type rơi vào `resolve()` thay vì `TypeError` mù mờ của PHP)
  - [x] Cập nhật PHPDoc (không đổi runtime): `core/Route.php` (constructor + `getMiddleware()`), `core/Router.php` (`middleware()/get()/post()/put()/patch()/delete()/group()/addRoute()` + 2 property `$groupMiddleware`/`$globalMiddleware`) — kiểu mới `list<class-string<MiddlewareInterface>|MiddlewareInterface>`
  - [x] `tests/Core/Middleware/MiddlewarePipelineTest.php` (mới — trước đây `MiddlewarePipeline` chỉ được test gián tiếp qua `RouterTest.php`) — 5 test: class-string qua Container (regression), instance trực tiếp, trộn class-string + instance trong cùng chain, short-circuit với instance, `\InvalidArgumentException` cho input không hợp lệ (`@dataProvider`: int/array/object không implement `MiddlewareInterface`)
  - [x] Không sửa `Container.php`. Không sửa test cũ (`RouterTest.php`, `CsrfMiddlewareTest.php`, `AuthMiddlewareTest.php`, `AuthorizationMiddlewareTest.php`, `RateLimitMiddlewareTest.php`) — dùng làm regression lock
  - [x] Self Code Review — trace tay xác nhận toàn bộ middleware hiện có (`CsrfMiddleware`/`AuthMiddleware`/`AuthorizationMiddleware`/`RateLimitMiddleware`, mọi fixture) đăng ký bằng class-string → đi đúng nhánh `is_string()` cũ, hành vi không đổi
  - [x] Self Architecture Review — SRP đạt (`resolve()` tách biệt "biết lấy instance"), OCP cải thiện (mở rộng không sửa code gọi), không tạo abstraction mới (không `MiddlewareResolver`/`Factory`/`Definition`), YAGNI đạt, regression risk thấp có kiểm soát
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật — 361 tests, 597 assertions, PASS → **CMS-027 COMPLETED — tag `v0.0.27`**
- [x] **CMS-029** — Logger Integration (`core/Application.php`, wiring `Logger` vào luồng chạy thật)
  - [x] Roadmap Review sau CMS-028 liệt kê Candidate A (Integration/Wiring, chia nhỏ A1 Logger/A2 TenantManager/A3 ThemeManager), B (Auth/Authorization Foundation), C (Content Schema tiếp tục), D (Event Dispatcher), E (Queue) — Owner chọn **A1 — Logger Integration**, lý do: rủi ro thấp nhất, không phụ thuộc migration, ít chạm Core ổn định
  - [x] Architecture Analysis xác nhận qua đọc trực tiếp: `Application::logException()` tự viết logic ghi file trùng lặp gần như hoàn toàn với `Logger::log()` (CMS-024); `Database::onQueryExecuted()` (điểm mở từ CMS-004) và `Hook::onError()` (điểm mở từ CMS-009) chưa từng có listener nào đăng ký (đã grep xác nhận); `Logger` không tự auto-wire được (constructor cần `string $logPath`, không default) — khác các Foundation Component trước, cần đăng ký tường minh vào Container
  - [x] Owner Decision phạm vi: CHỈ 3 điểm — (1) đăng ký `Logger` singleton trong `registerCoreServices()`, path giữ nguyên `storage/logs/app.log`; (2) `logException()` chuyển sang gọi `Logger::log('error', ...)`, context gồm `exception_class/file/line/trace`; (3) `boot()` đăng ký `Hook::onError()` listener ghi log qua `Logger`, context gồm `hook/exception_class/file/line`. KHÔNG triển khai Database query logging (`onQueryExecuted` fire cho MỌI query, chưa có requirement debug/performance — để dành CMS riêng), KHÔNG log `PluginManager::getFailures()` (khác domain — lifecycle error của Plugin, không phải runtime Hook callback error), KHÔNG log level convention mới (giữ nguyên `Logger::log(string $level, ...)`, chỉ thống nhất dùng `'error'` cho 2 điểm trên), KHÔNG log rotation, KHÔNG Exception/Adapter/abstraction logging mới
  - [x] Sửa `core/Application.php` — CHỈ 3 điểm đúng Owner Decision: thêm `Logger::class` singleton, viết lại thân `logException()` (bỏ `mkdir`/`sprintf`/`file_put_contents` tự viết), thêm `$hook->onError(...)` trong `boot()` (đăng ký trước `$pluginManager->boot()`)
  - [x] `tests/Core/ApplicationTest.php` (+2 test, không sửa test cũ): `testExceptionLogContainsExceptionClassFileLineAndTrace`, `testHookCallbackExceptionIsLoggedViaLogger` (đăng ký Hook action ném lỗi trực tiếp qua `Hook::action()/do()`, không cần fixture plugin mới)
  - [x] Không sửa `core/Logger.php`/`core/Database.php`/`core/Hook.php`/`core/PluginManager.php`/`core/ExceptionHandler.php` — đúng phạm vi đã duyệt. Đây là lần đầu sửa `core/Application.php` kể từ CMS-019 (9 CMS không đụng tới)
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `ApplicationTest`: 13 tests/31 assertions; toàn bộ suite: 374 tests, 652 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-029 COMPLETED — tag `v0.0.29`**
- [x] **CMS-030** — TenantManager Integration (`core/Middleware/TenantResolverMiddleware.php` + `core/Application.php`)
  - [x] Roadmap Review sau CMS-029 liệt kê Candidate A (TenantManager Integration), B (Auth/Authorization Foundation), C (ThemeManager Integration), D (Content Schema tiếp tục), E (Event Dispatcher), F (Queue), G (Deferred Observability) — Owner chọn **Candidate A — TenantManager Integration**
  - [x] Architecture Analysis phát hiện quan trọng (đọc trực tiếp `config/tenants.php`): kiến trúc gốc đã dự trù sẵn `core/Middleware/TenantResolverMiddleware.php` (comment "task CMS-009+") — xác nhận đây đúng là vị trí thiết kế gốc, không phải quyết định tuỳ ý. Xác nhận `Request::getHost()` đọc `$_SERVER['HTTP_HOST']` trực tiếp — client-controllable, bắt buộc chỉ dùng làm giá trị lookup qua prepared statement, không tin cậy cho mục đích khác. Xác nhận `View::__construct(themesPath, activeTheme, defaultTheme)` — `activeTheme` là tham số constructor, có thể đọc `TenantManager` tại thời điểm Closure factory chạy (lazy, sau khi Middleware đã set) mà không cần sửa `View.php`
  - [x] Owner Decision (5 Open Question): Q1 resolve tại Middleware (không phải `Application::boot()` — `boot()` idempotent, không nhận `Request`, đổi chữ ký sẽ phá tính idempotent); Q2 fail-closed — domain không khớp `site_domains` → 404 ngay, không fallback tenant mặc định; Q3 sửa Closure `View::class` đọc `TenantManager::current()['theme_active']`, fallback `config('app.theme')` khi NULL hoặc chưa có tenant; Q4 không xử lý `system_admin.domains` bypass (để CMS Super Admin riêng); Q5 `/health` và route hệ thống tương lai nằm ngoài phạm vi Middleware
  - [x] Final Design: `TenantResolverMiddleware` inject `Database`+`TenantManager` qua constructor (đúng pattern `AuthMiddleware`), dùng `Database::selectOne()` với 1 câu SQL `JOIN sites+site_domains` (không qua `QueryBuilder::join()` — né giới hạn đã ghi nhận ở Technical Debt #3), 404 dùng đúng envelope `{success:false,data:null,message:'Not Found',errors:[]}` khớp `ExceptionHandler`. Phát hiện kỹ thuật quan trọng: Global Middleware (`Router::middleware()`) áp dụng vô điều kiện cho MỌI route, không có cơ chế exclude per-route — giải pháp dùng `Router::group()` (đã có từ CMS-006/018) bọc riêng `ModuleManager::boot()`, giữ `/health` đăng ký ngoài group, không cần sửa `Router.php`/`MiddlewarePipeline.php`/`Route.php`
  - [x] `core/Middleware/TenantResolverMiddleware.php` (mới) — `process()` query `sites JOIN site_domains WHERE domain = ?`, không khớp → 404 (không gọi `$next`), khớp → `TenantManager::setCurrent()` → `$next($request)`. Không xử lý Auth/Permission/site status/Super Admin
  - [x] Sửa `core/Application.php` — `registerCoreServices()`: **phát hiện bug thật trước khi báo cáo** (trace lại `Container::get()`: không có binding thì không cache dù auto-wire được) — `TenantManager::class` phải đăng ký `singleton()` tường minh, nếu không `TenantResolverMiddleware` và `View` Closure sẽ thao tác trên 2 instance `TenantManager` khác nhau, tích hợp không hoạt động dù test có vẻ pass giả; sửa Closure `View::class` đọc `TenantManager::current()['theme_active'] ?? config('app.theme')`. `boot()`: bọc `$moduleManager->boot($router, ...)` trong `router->group(['middleware' => [TenantResolverMiddleware::class]], ...)`, `/health` giữ nguyên ngoài group, chữ ký `boot()` không đổi
  - [x] `tests/Core/Middleware/TenantResolverMiddlewareTest.php` (mới, 4 test): resolve thành công, `TenantManager::setCurrent()` đúng dữ liệu, domain không khớp → 404 đúng envelope + không gọi `$next`, không set tenant khi domain không khớp
  - [x] `tests/Core/ApplicationTest.php` — thêm helper `seedTenant()` (tạo bảng tối giản + insert, không chạy `MigrationManager` thật — giữ test độc lập/nhanh), áp dụng cho 5 test bị ảnh hưởng (`/ping`, 2 test `/boom` debug true/false, 2 test log liên quan `/boom`); thêm 3 test mới: theme đọc từ tenant, fallback `config('app.theme')` khi `theme_active` NULL, `/health` vẫn hoạt động không cần seed
  - [x] **Phát hiện fixture thiếu khi viết test**: `tests/Fixtures/AppProduction/` chưa từng có `config/database.php` (trước CMS-030 không route nào ở fixture này chạm Database) — thiếu file này khiến `/boom` ném `ConnectionException` che giấu ý nghĩa test debug=false. Tạo `tests/Fixtures/AppProduction/config/database.php` (SQLite in-memory, giống fixture `App`) — ngoài File Impact Prediction ban đầu, phát sinh khi implement, đã báo cáo minh bạch trước khi PHPUnit chạy
  - [x] Không sửa `TenantManager.php`/`View.php`/`Database.php`/`Router.php`/`Route.php`/`MiddlewarePipeline.php` — đúng phạm vi đã duyệt
  - [x] Technical Debt ghi nhận (không xử lý trong CMS-030): site `suspended`/`maintenance` chưa được chặn ở `TenantResolverMiddleware` (domain vẫn resolve thành công dù site không active); domain normalization (lowercase/bỏ `:port`) chưa triển khai, giữ nguyên `Request::getHost()` thô; `system_admin.domains` bypass (đã có sẵn trong `config/tenants.php`) chưa triển khai — để dành CMS Super Admin/Authorization site-level riêng
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `TenantResolverMiddlewareTest`: 4 tests/9 assertions; `ApplicationTest`: 16 tests/34 assertions; toàn bộ suite: 381 tests, 664 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-030 COMPLETED — tag `v0.0.30`**
- [x] **CMS-031** — Auth/Authorization Foundation (`core/AuthenticationService.php`)
  - [x] Roadmap Review sau CMS-030 — so sánh 6 candidate còn mở:

    | Candidate | Decision | Reason |
    |---|---|---|
    | B — Auth/Authorization Foundation | **Chọn** | Duy nhất thực sự "mở khoá" tiến độ Phase 3 (Module Admin/User/Page cần Auth thật); dependency sẵn sàng 100% từ CMS-028 (`users/roles/permissions/role_permissions/user_site_roles`); business value cao nhất, technical/security risk chấp nhận được với Architecture Analysis kỹ |
    | C — ThemeManager Integration | Hoãn | Rủi ro thấp nhưng chưa có bằng chứng lỗi thật (`theme_active` hiện luôn NULL, chưa seed data) |
    | D — Content Schema Migration Phase 2 | Hoãn | Rủi ro kỹ thuật thấp nhất nhưng 0 Module dùng tới — vi phạm YAGNI ở mức schema |
    | E — Event Dispatcher | Hoãn | 0 use case thật, rủi ro cao nếu tái cấu trúc `Hook.php` (đã có 2+ dependent: `PluginManager`, `Application::onError()`) |
    | F — Queue | Hoãn | 0 use case thật, đã hoãn liên tục từ CMS-025 |
    | G — Deferred Observability (`PluginManager::getFailures()` logging) | Hoãn | Rủi ro thấp nhất nhưng không block gì — làm sau cũng được |

    Owner chọn **Candidate B — Auth/Authorization Foundation**
  - [x] Architecture Analysis (đọc trực tiếp `Auth.php`/`Authorization.php`/`Session.php`/`Database.php`/`Container.php`/`Application.php`/`Router.php`/`Request.php`/7 migration CMS-028/`database-design.md`/`config/auth.php`): xác nhận `Auth::login()`/`Authorization::roles()/permissions()` có API public tái sử dụng nguyên vẹn, không cần sửa; xác nhận `config('auth.login_throttle')` đã sẵn sàng khớp `RateLimiter::hit()` (để dành, không dùng trong CMS-031); xác nhận `Session::set()` public đủ dùng để nạp roles/permissions mà không cần sửa `Authorization.php`
  - [x] Owner Decision 8 Open Question: Q1 Service gọi `Database` trực tiếp (không Repository, đúng tiền lệ CMS-030); Q2 KHÔNG route/Controller trong CMS-031 (chỉ Foundation Service); Q3 `site_id` lấy từ `TenantManager::id()`; Q4 KHÔNG rate limit (để CMS riêng); Q5 chặn login nếu `users.status !== 'active'`; Q6 dummy `password_verify()` khi email không tồn tại (chống user enumeration timing); Q7 `PASSWORD_DEFAULT`; Q8 1 session = 1 site (đổi site cần login lại)
  - [x] Final Design: `AuthenticationService` (constructor inject `Database`/`Auth`/`Session`/`TenantManager`), method public duy nhất `attempt(string $email, string $password): bool` — trả `bool` cho mọi lỗi xác thực (đúng convention `Csrf::verify()`/`RateLimiter::hit()`, không tạo Exception mới), chỉ throw `\LogicException` built-in khi `TenantManager::check() === false` (lỗi tiền điều kiện của caller, không phải lỗi user). Thứ tự `status` check **SAU** `password_verify()` (không phải trước) — lý do bảo mật: tránh timing side-channel phân biệt tài khoản `locked` qua tốc độ phản hồi. Xác nhận **không cần đăng ký Container tường minh** — `AuthenticationService` không giữ state, auto-wire đủ dùng, CMS-031 không chạm `Application.php` (khác 3 CMS liên tiếp trước)
  - [x] `core/AuthenticationService.php` — `DUMMY_HASH` hằng số bcrypt cố định (chống timing side-channel khi email không tồn tại), query `users` chỉ lấy `id/password/status` (không `SELECT *`), 2 query raw SQL riêng cho roles/permissions (JOIN qua `user_site_roles`→`roles`→`role_permissions`→`permissions`, né giới hạn `QueryBuilder::join()` Technical Debt #3), `$userId` ép kiểu `(int)` tường minh (đúng pattern CMS-030), `Auth::login()` chỉ nhận `id`/`email` (không `password`), `Session::set('auth.roles'/'auth.permissions', list<string>)`
  - [x] `tests/Core/AuthenticationServiceTest.php` (mới, 10 test) — `Database` SQLite in-memory thật (seed tay `sites/site_domains/users/roles/permissions/role_permissions/user_site_roles`), `Session`/`Auth`/`TenantManager` thật, không mock: login đúng, sai password, email không tồn tại, status không active, roles/permissions nạp đúng Session, đúng site theo `TenantManager` (2 site khác nhau), `Auth::login()` được gọi đúng, `LogicException` khi chưa có tenant, hành vi email-không-tồn-tại giống hệt sai-password (không throw, cùng kết quả)
  - [x] Không sửa `Auth.php`/`Authorization.php`/`Session.php`/`Database.php`/`TenantManager.php`/`Application.php`/`Container.php`/`Router.php` — đúng phạm vi Foundation đã khoá, `Auth.php` tiếp tục không chứa password logic, `Authorization.php` tiếp tục chỉ đọc Session
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật — `AuthenticationServiceTest`: 10 tests; toàn bộ suite: 391 tests, 676 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-031 COMPLETED — tag `v0.0.31`**
- [ ] Event / Queue — chưa đánh số CMS; Event có nghi vấn trùng lặp với `Hook` (xem `core-architecture.md` mục 6), Queue để sau khi Foundation hoàn tất (Owner Decision CMS-025)
- [ ] Module System (Module: User/Page/SEO...) — thuộc Phase 3, chưa đánh số CMS
- [ ] ThemeManager Integration (Candidate C) / Content Schema Migration tiếp tục (Candidate D) / Database query logging / `PluginManager::getFailures()` logging / Site status policy / Domain normalization / Super Admin domain bypass / Login route + Controller / Rate limiting login / Multi-site session — chưa đánh số CMS, để dành sau khi có nhu cầu thật

## Phase 2 — Database Migration thật (bảng `sites`, `users`, `roles`...)

- [x] **CMS-028** — Database Migration Phase 2 — Tenant/Auth/Role foundation (`database/migrations/2026_08_01_00000{1..7}_*.php`)
  - [x] Roadmap Review sau CMS-027 liệt kê 5 candidate (A. Database Migration, B. Integration/Wiring, C. Event Dispatcher, D. Queue, E. Module System) — Owner chọn Candidate A, lý do: rủi ro thấp nhất (dùng nguyên `MigrationManager` đã có từ CMS-013, chưa từng chạy migration thật), mở khoá nhiều candidate khác (B/E phụ thuộc trực tiếp)
  - [x] Owner Decision phạm vi: đúng 7 bảng nhóm Tenant/Auth/Role (`sites/site_domains/users/roles/permissions/role_permissions/user_site_roles`), KHÔNG gồm `login_logs/password_resets/personal_access_tokens` (thuộc Auth enhancement, chưa cần để mở khoá TenantManager/Auth cơ bản), KHÔNG tạo bảng `plans` (`sites.plan_id` giữ nullable, không FK — YAGNI, chưa có requirement Billing/SaaS thật), KHÔNG seed data, migration chỉ tạo schema (DDL thuần, không business logic/không Trigger — đúng `database-design.md` mục 6)
  - [x] Architecture Analysis phát hiện xung đột kỹ thuật thật: schema `database-design.md` giả định MySQL 8+ (`ENUM`/`BIGINT UNSIGNED`/`ON UPDATE CURRENT_TIMESTAMP`) nhưng toàn bộ test suite dự án luôn chạy SQLite in-memory (`tests/Fixtures/config/database.php`) — không thể PHPUnit-verify migration MySQL-only syntax trong môi trường hiện tại
  - [x] Owner Decision (Final Design): Phương án A — Portable SQL, loại `ENUM`→`VARCHAR` (validate ở Service layer sau), loại `UNSIGNED`, loại `ON UPDATE CURRENT_TIMESTAMP`→`TIMESTAMP NULL` (Service tự set). Riêng mệnh đề Primary Key auto-increment (`AUTO_INCREMENT` MySQL vs `AUTOINCREMENT` SQLite — không có cú pháp chung) được Owner approve cho phép **duy nhất 1 điểm rẽ nhánh driver** trong mỗi migration Closure qua `$db->connection()->getAttribute(\PDO::ATTR_DRIVER_NAME)` (dùng `Database::connection(): PDO` đã public sẵn từ CMS-004, không sửa `MigrationManager`/`Database`) — không cho phép rẽ nhánh thêm ở phần schema khác nếu chưa có Architecture Issue Report riêng
  - [x] 7 migration file: `001_create_sites_table`, `002_create_site_domains_table` (FK→sites CASCADE), `003_create_users_table`, `004_create_roles_table` (FK→sites CASCADE), `005_create_permissions_table`, `006_create_role_permissions_table` (composite PK, FK→roles/permissions CASCADE), `007_create_user_site_roles_table` (FK→users/sites CASCADE, FK→roles RESTRICT) — đúng thứ tự dependency, đúng Index/Unique theo `database-design.md` mục 2
  - [x] `tests/Core/RealMigrationsTest.php` (mới, 11 test) — chạy `MigrationManager` trỏ thẳng `database/migrations/` thật (khác `MigrationManagerTest.php` dùng fixture riêng), SQLite in-memory thật không mock: đủ 7 bảng đúng thứ tự, đúng cột (`PRAGMA table_info`), UNIQUE constraint từng bảng, composite PK `role_permissions`, rollback đúng thứ tự ngược, migrate lại sau rollback (idempotent), `status()` phản ánh đúng applied/pending
  - [x] **Failure Analysis thật** (phát hiện qua PHPUnit thật, không suy đoán): `testRolesTenantNameUniqueConstraintIsEnforced` FAIL lần chạy đầu — nguyên nhân xác định KHÔNG PHẢI bug migration: UNIQUE `(tenant_id, name)` đúng ANSI SQL semantics, `NULL ≠ NULL` trong composite UNIQUE nên 2 role hệ thống (`tenant_id = NULL`) cùng tên KHÔNG bị chặn ở cả SQLite lẫn MySQL — test ban đầu giả định sai, không phải khác biệt driver. Owner approve sửa DUY NHẤT test (đổi kịch bản sang `tenant_id = 1` — role theo tenant cụ thể, đúng phạm vi UNIQUE thật sự enforce), không sửa migration/schema
  - [x] Technical Debt ghi nhận (không sửa trong CMS-028): `roles.tenant_id` nullable + UNIQUE `(tenant_id, name)` không ngăn được trùng tên role hệ thống khi `tenant_id IS NULL` — để dành CMS Role/Auth Service sau này xử lý ở Service layer (đúng nguyên tắc "không Trigger cho business logic" đã áp dụng cho ràng buộc homepage ở `database-design.md` mục 6.1)
  - [x] Không sửa `MigrationManager.php`/`Database.php`/bất kỳ Core Component nào đã tag version — 0 file Core cũ bị đụng, đúng phạm vi Foundation đã duyệt
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `RealMigrationsTest`: 11 tests/51 assertions; toàn bộ suite: 372 tests, 648 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-028 COMPLETED — tag `v0.0.28`**

Phần còn lại của Phase 2 (Content/Media/Menu/Form/SEO/Theme/Plugin/Settings) chưa bắt đầu, chưa đánh số CMS. Xem `database-design.md` mục 3-5.

## Phase 3+ — Module Auth/User/Role/Page/SEO...

Tương ứng CMS-014 → CMS-019 ở trên và các module tiếp theo tham khảo `00-master-spec.md`.
