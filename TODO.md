# TODO — CMS Đa Website

> Theo dõi tiến độ theo Phase/Task. Task ID dạng `CMS-XXX`, đánh số tuần tự theo thứ tự triển khai thực tế (không nhảy số).

## Phase 1 — Core Framework Foundation

Mục tiêu: dựng khung core tự viết (không framework nền), đủ để boot 1 request qua Router → Middleware → View/JSON. Chi tiết xem `cms-architecture-proposal.md` mục 2 và phần "QUYẾT ĐỊNH CHÍNH THỨC".

- [x] **CMS-001** — Khởi tạo project skeleton
  - [x] Tạo cấu trúc thư mục đầy đủ (`app/`, `core/`, `modules/`, `plugins/`, `themes/`, `public/`, `storage/`, `resources/`, `database/`, `config/`, `docs/`)
  - [x] `.gitkeep` cho các thư mục rỗng
  - [x] `composer.json` với PSR-4 autoload (`Core\`, `App\`, `Modules\`)
  - [x] `.gitignore`
  - [x] `git init` + commit đầu tiên — đã chạy thủ công (xác nhận qua `.git/COMMIT_EDITMSG`, `.git/index`).
  - [x] `composer install` / `composer dump-autoload` — đã chạy thủ công (xác nhận qua `vendor/autoload.php`).
- [x] **CMS-002** — Config loader + file cấu hình
  - [x] `core/Config.php` — sửa từ static class (vi phạm "No Global Variable"/"Dependency Injection") sang instance-based, inject `configPath` qua constructor
  - [x] `config/app.php`, `config/database.php` — sửa để đọc qua `getenv()` với default an toàn thay vì hard-code `debug=true`/`env=development` trực tiếp trong file version-controlled
  - [x] `config/cache.php`, `config/auth.php`, `config/tenants.php` — tạo mới (còn thiếu so với danh sách đã chốt trong `cms-architecture-proposal.md`)
  - [x] Dọn thư mục thừa `config/core/` (rỗng, không thuộc kiến trúc)
- [x] **Architecture/Code Review (sau CMS-002)** — phát hiện `public/index.php` (tự tạo, không thuộc task nào) vẫn gọi API static cũ của `Config` (`Config::init()`/`Config::get()`) → lỗi runtime `Call to undefined method`. Đã sửa: chuyển sang instance-based (`new Config(...)`), bỏ echo trực tiếp `database.connections.mysql.host` ra output (rủi ro rò rỉ thông tin hạ tầng). Kết luận: Phase 1 (CMS-001, CMS-002) đạt tiêu chuẩn sau khi sửa.
- [ ] **Kỹ thuật nợ cần theo dõi**: chưa có cơ chế nạp `.env` (`config/*.php` đọc `getenv()` nhưng chưa quyết định dotenv library hay set qua server) — chưa gây lỗi vì luôn có default, cần xử lý trước khi deploy production, chưa thuộc phạm vi task nào hiện tại.
- [ ] **Quyết định kiến trúc cần chốt trước Phase 3** (phát hiện ở Architecture Review tổng thể sau CMS-005): `Database` và `View` đều `final class`, không có interface (`DatabaseInterface`/`ViewInterface`). Hiện chấp nhận được (YAGNI, chỉ 1 implementation), nhưng phải quyết định rõ trước khi module Auth/User đầu tiên viết Repository/Controller — thêm interface sau khi nhiều module đã phụ thuộc sẽ là breaking change tốn kém.
- [x] **CMS-003** — `core/Container.php` (DI Container, PSR-11)
  - [x] Design Review — 9 điểm bổ sung: PSR-11 thật (`psr/container`), chỉ Constructor Injection, Circular Dependency detection, 3 exception (`ContainerException`, `BindingNotFoundException`, `CircularDependencyException`), singleton theo vòng đời instance (không static/global), quy ước Service Provider (chưa tạo file — hoãn tới task Plugin/Module), Reflection cache, API giữ nguyên 6 method, chuẩn bị Unit Test
  - [x] `core/Container.php`, `core/ContainerException.php`, `core/BindingNotFoundException.php`, `core/CircularDependencyException.php`
  - [x] `composer.json` — thêm `psr/container ^2.0` (require), `phpunit/phpunit ^10.5` (require-dev), autoload-dev `Tests\` → `tests/`
  - [x] `tests/Core/ContainerTest.php` + `tests/Fixtures/*.php` — 12 test case bao phủ resolve class/interface, singleton, auto-wiring đệ quy, circular dependency (đúng kịch bản `UserService → RoleService → PermissionService → UserService`), binding not found, `instance()`, scalar có/không default, `make()`, `has()`
  - [x] `phpunit.xml`
  - [x] Code Review — trace tay toàn bộ luồng logic, không phát hiện lỗi
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHPUnit 10.5.64, PHP 8.3.30), 0 Errors/Failures/Warnings/Deprecations → **CMS-003 COMPLETED — tag `v0.0.3`**
- [x] **CMS-004** — `core/Database.php` + `core/QueryBuilder.php` (PDO wrapper, transaction API)
  - [x] Design Review — 14 mục: vai trò Database (connection+execute+transaction+exception+log) tách bạch QueryBuilder (chỉ dựng SQL, luôn giao Database thực thi), Transaction API nested-safe, không Connection Pool (ghi nhận lý do), 4 Exception chuẩn hoá, Prepared Statement 100%, Multi-tenant qua `forTenant()` sugar (không business logic), Query Logging chỉ hook/interface, không tối ưu sớm, sẵn sàng multiple connections + Database-per-Tenant
  - [x] `core/Database.php`, `core/QueryBuilder.php`, `core/Database/{DatabaseException,ConnectionException,QueryException,TransactionException,IdentifierValidator,SqlCompiler}.php`
  - [x] `tests/Fixtures/config/database.php` (SQLite in-memory), `tests/Core/DatabaseTest.php`, `tests/Core/QueryBuilderTest.php` — 31 test case (transaction commit/rollback/nested, exception, query log, CRUD, whereIn/forTenant, identifier injection, edge case mảng rỗng, count()/aggregate regression, SQL injection qua identifier)
  - [x] Self Code Review — phát hiện & sửa 4 bug thật: `whereIn([])`/`insert([])`/`update([])` sinh SQL sai cú pháp; `count()` đưa `"COUNT(*) as aggregate"` qua `IdentifierValidator` như 1 identifier (phát hiện qua PHPUnit thật) → tách `core/Database/SqlCompiler.php` (`@internal`, chỉ `QueryBuilder` dùng, không Business Logic) để `count()` tự dựng SQL riêng, đồng thời đưa `QueryBuilder.php` về dưới 300 dòng
  - [x] Self Architecture Review — đối chiếu đủ các nguyên tắc đã chốt, không phát hiện vấn đề
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHPUnit 10.5.64, PHP 8.3.30), 0 Errors/Failures/Warnings/Deprecations → **CMS-004 COMPLETED — tag `v0.0.4`**
- [x] **CMS-005** — `core/View.php` (Theme Engine PHP thuần)
  - [x] Design Review + 9 điều chỉnh: View Resolution 2 cấp cố định (active theme → default theme → `ViewNotFoundException`, bỏ `addPath()`/N-path đã đề xuất trước), Layout 1 cấp (không đa cấp), Data Binding chỉ qua `render()` (không global/static), Escape tường minh (không tự động), Partial/Component dùng chung `include()`, Exception rút gọn còn 2 lớp (`ViewException`, `ViewNotFoundException`)
  - [x] `core/View.php`, `core/View/ViewException.php`, `core/View/ViewNotFoundException.php`
  - [x] `tests/Fixtures/themes/{active,default}/views/*` (fixture 2 theme) + `tests/Core/ViewTest.php` (15 test: resolve/dot notation/theme fallback/view not found/path traversal/data binding/escape/layout+section+yield/partial/section không cân bằng/reset state) + `tests/Core/ViewContainerIntegrationTest.php` (regression: ráp qua Container CMS-003)
  - [x] Self Code Review — phát hiện & sửa rò rỉ output buffer khi `section()` gọi không cân bằng (cả trong test lẫn production `render()` — thêm cơ chế tự dọn buffer trong `finally`)
  - [x] Self Architecture Review — đối chiếu đủ 9 nguyên tắc, không phát hiện vấn đề
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHPUnit 10.5.64, PHP 8.3.30), 0 Errors/Failures/Warnings/Deprecations → **CMS-005 COMPLETED — tag `v0.0.5`**
- [x] **CMS-006** — `core/Router.php` (Router + HTTP Layer: Request/Response/Route/Middleware Pipeline/Controller Resolver)
  - [x] Design Review + 10 điểm bổ sung: Router chỉ Routing (không Database/View/Auth/Validation), Request/Response Immutable (`new self(...)` thay vì `clone` — PHP 8.1 không cho gán lại `readonly` trong `__clone()`), Middleware Pipeline Onion (Before/After + short-circuit), Route Parameter không ép kiểu/validate, Duplicate Route throw ngay lúc đăng ký (không đợi runtime), Group chỉ merge prefix/middleware/domain, phân biệt rõ 404/405, Controller Contract (chỉ nhận Request, phải trả Response), tách `ControllerResolver` riêng theo sơ đồ `Middleware Pipeline → Controller Resolver → Controller`, HTTP layer tự viết nhẹ (không PSR-7/15, giữ tinh thần "không framework nền")
  - [x] `core/Http/{Request,Response}.php`, `core/Route.php`, `core/Router.php`, `core/Router/{ControllerResolver,RouteNotFoundException,MethodNotAllowedException,DuplicateRouteException}.php`, `core/Middleware/{MiddlewareInterface,MiddlewarePipeline}.php`
  - [x] `tests/Fixtures/Http/*.php` (8 fixture) + `tests/Core/RouterTest.php` (14 test) + `tests/Core/Router/ControllerResolverTest.php` (3 test) + `tests/Core/Http/RequestTest.php` (3 test) + `tests/Core/RouterContainerDatabaseViewIntegrationTest.php` (1 test — Regression: Router ráp đúng Container+Database+View, không sửa 3 file đó)
  - [x] Self Code Review — trace tay toàn bộ 21 test case (đặc biệt thứ tự Onion và phân biệt 404/405), không phát hiện lỗi
  - [x] Self Architecture Review — đối chiếu đủ 10 nguyên tắc, không phát hiện vấn đề
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật, 0 Errors/Failures/Warnings/Deprecations → **CMS-006 COMPLETED — tag `v0.0.6`**
  - [x] **Architecture Review tổng thể HTTP Layer (sau khi Completed)** — phát hiện `Request::fromGlobals()` chỉ đọc `$_POST`, bỏ sót JSON body (PHP không tự điền `$_POST` khi `Content-Type: application/json`) → sẽ chặn cứng `POST /api/v1/auth/login` (module Auth, Phase 3) ngay khi bắt đầu. Đã vá: `resolveBody()` đọc `php://input` + `json_decode` khi Content-Type là JSON, fallback `$_POST` cho form thường; sửa `extractHeaders()` bắt thêm `CONTENT_TYPE`/`CONTENT_LENGTH` (2 header không có tiền tố `HTTP_` trong `$_SERVER`, khác mọi header khác). Thêm 3 test (`tests/Core/Http/RequestFromGlobalsTest.php`, dùng stream wrapper giả lập `php://input`). **Verified** — xác nhận không phá vỡ test cũ.
  - [ ] Ghi nhận (không chặn): `Response::json()` chưa tự bọc chuẩn `{success, data, message, errors}` — cân nhắc thêm `Response::apiSuccess()/apiError()` khi module API đầu tiên viết, không bắt buộc phải làm ở CMS-006/007. `dispatch()` ném exception (không trả `Response`) cho 404/405 — CMS-011/012 phải bắt và map thành `Response`, ghi rõ ở đây để không quên.
- [x] **CMS-007** — `core/Session.php` (wrapper duy nhất quanh PHP Session)
  - [x] Design Review + 8 điểm bổ sung: Session chỉ Storage (không login/logout/check quyền), Lazy Start (không tự start trong constructor), Flash đúng vòng đời 1 request (hết hạn theo tuổi request, không phải "xoá khi đọc"), Namespace dot-notation lồng nhau giống `Config::get()` (`auth.user_id/roles/permissions`, `csrf.token`, `locale.current`, `tenant.current`), Security (`regenerate()`, `destroy()` xoá cả cookie, cookie params đầy đủ từ Config), API tối giản (không `push()/increment()/decrement()`)
  - [x] `core/Session.php`, `core/SessionException.php` (guard gọi trước `start()`)
  - [x] `tests/Fixtures/config/auth.php` (fixture) + `tests/Core/SessionTest.php` (13 test — mô phỏng 3 "request" trong 1 test qua `session_write_close()`/`start()` lại để test đúng vòng đời Flash)
  - [x] Self Code Review — trace tay Flash lifecycle qua 3 "request" mô phỏng, không phát hiện lỗi
  - [x] Self Architecture Review — đối chiếu đủ 8 nguyên tắc, không phát hiện vấn đề
  - [x] **Sửa Risky Test** (phát hiện qua PHPUnit thật) — `RouterTest::testDoesNotConfuseNotFoundWithMethodNotAllowed` dựa vào type của `catch` để "xác minh ngầm", không có assertion thật → PHPUnit báo Risky. Viết lại: `catch (Throwable)` rộng + `assertInstanceOf()`/`assertNotInstanceOf()` tường minh cho cả 2 chiều (404 không được là 405, 405 không được là 404) trong cùng 1 test — không dùng `@doesNotPerformAssertions`. Rà soát toàn bộ `catch` khác trong test suite, xác nhận không còn risky nào khác.
  - [x] **Sửa Deprecation** (phát hiện qua PHPUnit thật, root cause do người dùng phân tích) — `Tests\Fixtures\Http\PhpInputStreamStub` không khai báo property `$context` tường minh; PHP Stream Wrapper API tự gán `$stream->context` khi đăng ký wrapper → PHP 8.2+ báo "Creation of dynamic property" Deprecated. Sửa: khai báo `public mixed $context = null;` tường minh (không dùng `#[AllowDynamicProperties]` vì chỉ che cảnh báo, không sửa gốc). Rà soát toàn bộ project — xác nhận đây là stream wrapper tự viết DUY NHẤT, không còn chỗ nào khác.
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (94 tests, 140 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations → **CMS-007 COMPLETED — tag `v0.0.7`**
- [x] **CMS-008** — `core/Cache.php` + `core/Cache/{CacheDriver,FileCacheDriver,RedisCacheDriver,CacheException}.php` (Cache Layer)
  - [x] Design Review: interface `CacheDriver` tối giản (không tag ở driver), Tag support đặt ở `Cache` facade (registry key portable qua mọi driver), Tenant key là quy ước đặt tên (không phải API riêng, nhất quán `Database`/`QueryBuilder`), `RedisCacheDriver` dùng `ext-redis` (không thêm composer dependency), Redis test có điều kiện (`markTestSkipped` nếu không có Redis thật)
  - [x] `core/Cache.php`, `core/Cache/CacheDriver.php`, `core/Cache/FileCacheDriver.php` (atomic write: file tạm + `rename()`, chống corrupt khi nhiều PHP-FPM worker ghi đồng thời), `core/Cache/RedisCacheDriver.php`, `core/Cache/CacheException.php`
  - [x] `tests/Core/Cache/FileCacheDriverTest.php` (8 test), `tests/Core/CacheTest.php` (8 test — facade, tag, prefix, `remember()`), `tests/Core/Cache/RedisCacheDriverTest.php` (4 test, tự skip nếu môi trường không có Redis), `tests/Core/CacheContainerIntegrationTest.php` (1 test — Regression: ráp qua Container)
  - [x] Self Code Review — trace tay toàn bộ 21 test case mới (đặc biệt cơ chế tag qua registry key và atomic write), không phát hiện lỗi. Không sửa file nào của 7 Core Component trước đó — không có rủi ro regression từ phía đó.
  - [x] Self Architecture Review — SOLID/KISS/DRY/YAGNI/Security/Performance/Testability đều đạt, không phát hiện vấn đề
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (115 tests, 170 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (`RedisCacheDriverTest` — môi trường không có `ext-redis`) → **CMS-008 COMPLETED — tag `v0.0.8`**
  - [x] **Architecture Review riêng Cache Layer (sau khi Completed)** — phát hiện & sửa: `RedisCacheDriver::connection()` chỉ bắt `RedisException`, nhưng `ext-redis` mặc định KHÔNG ném exception cho `auth()`/`select()` thất bại (chỉ trả `false`) → sai password/database index bị bỏ qua âm thầm. Sửa: kiểm tra tường minh giá trị trả về của `connect()/auth()/select()`, ném `CacheException` rõ ràng ngay tại điểm lỗi. Ghi nhận Technical Debt: File cache không có cơ chế dọn định kỳ entry hết hạn chưa đọc lại (lazy-only expiry), registry tag không có TTL riêng — cần cron dọn khi lên production thật.
- [x] **CMS-009** — `core/Hook.php` (Hook System kiểu WordPress — Action/Filter/priority/wildcard, qua Container)
  - [x] Design: unified registry cho Action+Filter (khác nhau ở cách gọi `do()`/`apply()`, không khác cách đăng ký — đúng cách WordPress làm bên trong), priority mặc định 10 (số nhỏ chạy trước), wildcard trộn đúng thứ tự priority với exact match (không tách riêng trước/sau), cách ly lỗi try/catch riêng từng callback + điểm mở `onError()` (đúng `13-module-plugin.md`), không static/global/ham global, instance duy nhất trong 1 request qua Container
  - [x] `core/Hook.php`
  - [x] `tests/Core/HookTest.php` (17 test: priority, insertion order, filter chain, remove, wildcard, cách ly lỗi, onError, truyền tham số) + `tests/Core/HookContainerIntegrationTest.php` (2 test — Regression: singleton qua Container, auto-wire không cần bind tường minh)
  - [x] Self Code Review — trace tay toàn bộ 19 test case (đặc biệt thứ tự trộn wildcard+exact theo priority, và filter giữ nguyên giá trị khi 1 callback lỗi giữa chuỗi), không phát hiện lỗi. Không sửa file nào của 8 Core Component trước đó.
  - [x] Self Architecture Review — SOLID/KISS/DRY/YAGNI/Security/Performance/Testability đều đạt
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (133 tests, 192 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-009 COMPLETED — tag `v0.0.9`**

## Roadmap Phase 1+ (phần còn lại) — CHÍNH THỨC, xác nhận ngày hôm nay

Thay thế hoàn toàn kế hoạch cũ (Middleware cụ thể / `public/index.php` riêng / Error Handler riêng / `/health` riêng). Đối chiếu đã chốt: **Error Handler + `/health`** gộp vào CMS-012 (Application/Bootstrap — nơi tự nhiên để wire global exception handling và route kiểm thử pipeline); **`TenantResolverMiddleware` + `AuthMiddleware`/`CsrfMiddleware`** gộp vào CMS-015 (Authentication — lúc đó mới thực sự cần Auth/Tenant context trong Middleware Pipeline).

- [x] **CMS-010** — Module Manager (`core/ModuleManager.php` + `core/Module/*`)
  - [x] Design: discover module qua `module.json` (glob), resolve thứ tự load bằng topological sort + phát hiện circular dependency (**cùng mô hình** `Container::resolve()` — stack `resolving`, chặn tại chỗ), `boot()` nạp `routes.php` của module đã bật vào `Router` qua closure cô lập scope. Không tự query DB để biết module nào "bật" — nhận `enabledKeys` từ bên ngoài (giữ core trung lập, nhất quán `Database`/`View`/`Cache`).
  - [x] `core/ModuleManager.php`, `core/Module/{ModuleDescriptor,ModuleException,ModuleNotFoundException,CircularModuleDependencyException}.php`
  - [x] `tests/Fixtures/Modules/{Alpha,Beta,Circular1,Circular2,NoRoutes}/*`, `tests/Fixtures/ModulesInvalid/BadModule/module.json` + `tests/Core/ModuleManagerTest.php` (9 test) + `tests/Core/ModuleManagerContainerIntegrationTest.php` (1 test Regression — qua Container+Router)
  - [x] Self Code Review — trace tay toàn bộ 10 test case (đặc biệt: thứ tự load đúng dependency dù `enabledKeys` liệt kê ngược thứ tự), không phát hiện lỗi. Không sửa file nào của 9 Core Component trước đó.
  - [x] Self Architecture Review — SOLID/KISS/DRY/YAGNI/Security/Performance/Testability đều đạt
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (144 tests, 212 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-010 COMPLETED — tag `v0.0.10`**
- [x] **CMS-011** — Application / Bootstrap (`core/Application.php` + `public/index.php`)
  - [x] Design Review + 4 quyết định xác nhận: `View` dùng `config('app.theme')` cho cả active/default theme (chưa có TenantManager), `ModuleManager::boot()` mặc định bật tất cả module đã `discover()`, log lỗi qua `file_put_contents` trực tiếp (chưa xây `Core\Logger` đầy đủ — ngoài phạm vi), response lỗi luôn JSON `{success,data,message,errors}` (chưa phân biệt SSR/API)
  - [x] `core/Application.php` — `handle(Request): Response` thuần (test được) tách khỏi `run(): void` (I/O boundary duy nhất). `boot()` idempotent (guard `$booted`). Debug gate cho message lỗi 500 (`config('app.debug')`). Bổ sung 1 method ngoài thiết kế đã duyệt: `container(): Container` (cần thiết để test xác nhận Core Service đăng ký đúng, đã ghi rõ trong docblock)
  - [x] Sửa `config/app.php` (thêm key `theme`), sửa `public/index.php` (thay smoke test bằng bootstrap thật, còn 3 dòng)
  - [x] `tests/Fixtures/App/*` (config đầy đủ + module + theme fixture), `tests/Fixtures/AppProduction/*` (fixture riêng cho test `debug=false`) + `tests/Core/ApplicationTest.php` (11 test: health route, module route, 404/405/500, debug gate, logging, Container wiring, singleton, boot idempotent)
  - [x] Self Code Review — trace tay toàn bộ 11 test case, không phát hiện lỗi. Không sửa file nào của 9 Core Component trước đó ngoài `config/app.php` (đã duyệt) và `public/index.php` (đúng phạm vi task)
  - [x] Self Architecture Review — SOLID/KISS/YAGNI/Security/Performance/Testability đều đạt
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (154 tests, 237 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-011 COMPLETED — tag `v0.0.11`**
- [x] **CMS-012** — Plugin Manager (`core/PluginManager.php` + `core/Plugin/*`)
  - [x] Design Review + quyết định chốt trước khi code: `PluginManager` độc lập hoàn toàn với `ModuleManager` (không refactor, không chia sẻ abstraction, chấp nhận trùng lặp logic topological sort để giữ ổn định), `discover()` memoize trong instance (khác chủ đích với `ModuleManager::discover()`), `boot()` reset `failures` mỗi lần gọi, cách ly lỗi tuyệt đối ở tầng `require Hooks.php` (không rethrow), giữ public API tối giản (không thêm interface/abstraction ngoài phạm vi)
  - [x] `core/PluginManager.php`, `core/Plugin/{PluginDescriptor,PluginException,PluginNotFoundException,CircularPluginDependencyException}.php`
  - [x] `tests/Fixtures/Plugins/{GoodPluginA,GoodPluginB,BrokenPlugin,NoHooksPlugin,CircularA,CircularB,ScopeCheckPlugin}/*`, `tests/Fixtures/PluginsInvalid/BadPlugin/plugin.json`, `tests/Fixtures/PluginsDuplicate/{PluginX,PluginY}/plugin.json` + `tests/Core/PluginManagerTest.php` (16 test) + `tests/Core/PluginManagerContainerIntegrationTest.php` (2 test Regression)
  - [x] Sửa `core/Application.php` — CHỈ 2 điểm: đăng ký `PluginManager` vào Container, gọi `pluginManager->boot()` trong `boot()` ngay sau `ModuleManager`, không sửa gì khác
  - [x] Self Code Review — trace tay toàn bộ 18 test case (đặc biệt: thứ tự nạp theo dependency qua filter chain `plugin.trace`, cô lập lỗi 1 plugin không ảnh hưởng plugin khác, reset `failures` mỗi lần `boot()`, scope `Hooks.php` chỉ thấy đúng `$hook`), không phát hiện lỗi. Không sửa file nào của `ModuleManager`/`Hook`/`Container`/9 Core Component trước đó ngoài `core/Application.php` (đúng phạm vi đã duyệt)
  - [x] Self Architecture Review — SRP/Dependency/KISS/YAGNI/không premature optimization/không trùng Public API/`ModuleManager` không bị ảnh hưởng/không phát sinh Technical Debt mới ngoài trùng lặp đã được chấp nhận trước — đều đạt
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (171 tests, 273 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-012 COMPLETED — tag `v0.0.12`**
- [x] **CMS-013** — Migration System (`core/MigrationManager.php` + `core/Migration/*` + `bin/migrate.php`)
  - [x] Adversarial Architecture Review trước khi code — phát hiện & xử lý: driver detection không được tự đọc PDO (`getAttribute()`), không thêm API mới cho `Database`; Container trong `bin/migrate.php` sẽ trùng lặp logic wiring `Database` với `Application.php` nên bị loại; định dạng migration file chọn Closure-array (không interface, không class, không DSL)
  - [x] Final Design 8 quyết định chốt: (1) Closure-array `['up'=>Closure,'down'=>Closure]`, (2) DDL raw SQL qua `Database::statement()` — không Schema Builder, (3) rollback theo batch, (4) CLI entry point riêng `bin/migrate.php` — không sửa `Application.php`/`public/index.php`, (5) không dùng Container trong CLI, (6) fail-fast tuyệt đối (khác `ModuleManager`/`PluginManager`), (7) giữ `status()` trong Public API, (8) `driver: string` truyền qua constructor từ `Config`, `MigrationManager` không biết PDO
  - [x] `core/MigrationManager.php`, `core/Migration/{MigrationException,MigrationNotFoundException}.php`, `bin/migrate.php`
  - [x] `tests/Fixtures/Migrations/{Valid,Failing,Malformed}/*` + `tests/Core/MigrationManagerTest.php` (16 test: discover, migrate thành công, batch tăng dần qua nhiều lần chạy, status applied/pending, rollback theo batch đảo ngược, rollback rỗng khi chưa có batch, malformed migration, fail-fast dừng ngay khi lỗi, `MigrationNotFoundException` khi file bị xoá, validate driver, regression không ảnh hưởng bảng khác)
  - [x] Không sửa file nào trong 12 Core Component đã hoàn thành (`Application`/`Database`/`Container`/`Config`/`Router`/`View`/`Session`/`Cache`/`Hook`/`ModuleManager`/`PluginManager`) — 0 file Core cũ bị đụng, đúng Decision #4/#5/#8
  - [x] Self Code Review — trace tay toàn bộ 16 test case, phát hiện 1 sai sót đặt tên (không phải bug logic): fixture ban đầu dùng `ALTER TABLE ... DROP COLUMN` (rủi ro không tương thích SQLite cũ) + tên file không khớp nội dung — đã sửa thành 2 migration `CREATE TABLE` độc lập
  - [x] Self Architecture Review — SRP (chấp nhận mức gộp trách nhiệm tương đương `ModuleManager`), Dependency (không PDO, không Container, không API mới cho `Database`), KISS/YAGNI đạt
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (185 tests, 299 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-013 COMPLETED — tag `v0.0.13`**
- [x] **CMS-014** — Validation Layer (`core/Validator.php` + `core/Validation/*`)
  - [x] Design Review + 6 quyết định chốt: registry rule dạng closure nội bộ (không class-per-rule), trả `ValidationResult` thay vì throw cho input sai (chỉ throw `ValidationException` khi rule name không tồn tại — lỗi cấu hình), không đăng ký Container/Application (giống `MigrationManager`, không có state cần singleton), chỉ 1 rule format string kiểu Laravel, giữ `extend()` custom rule, validate hết toàn bộ rule của 1 field (không bail)
  - [x] `core/Validator.php`, `core/Validation/{ValidationResult,ValidationException}.php`
  - [x] `tests/Core/ValidatorTest.php` (31 test: passes/fails/errors/firstError, 16 rule built-in, custom rule qua `extend()`, custom message, unknown rule throw, field optional bị bỏ qua, nhiều lỗi tích luỹ trên 1 field)
  - [x] Không sửa file nào trong 13 Core Component đã hoàn thành — 0 dependency vào Config/Container/Database/Router/Session/Hook/ModuleManager/PluginManager/MigrationManager
  - [x] Self Code Review — phát hiện & sửa 2 vấn đề: (1) test tự viết sai logic (`min`/`max` mâu thuẫn số học trên cùng 1 giá trị) — sửa lại rule độc lập không mâu thuẫn; (2) bug thật trong rule `in` — ép `(string) $value` trực tiếp gây PHP Warning nếu `$value` là mảng, sửa bằng guard `is_scalar()`
  - [x] Self Architecture Review — SRP/Coupling/Cohesion/KISS/YAGNI đạt, 0 dependency vào Core Component khác
  - [x] **Verified** — `vendor/bin/phpunit` PASS (bao gồm trong lần chạy toàn bộ suite cùng CMS-015, 229 tests/382 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-014 COMPLETED — tag `v0.0.14`**
- [x] **CMS-015** — HTTP Request Layer (mở rộng `core/Http/Request.php` đã có từ CMS-006)
  - [x] Architecture Analysis phát hiện xung đột quan trọng: yêu cầu ban đầu mô tả trùng trách nhiệm với `Core\Http\Request` đã hoàn thành (v0.0.6) — chốt Decision #0: CMS-015 là MỞ RỘNG file đã có (không tạo Request thứ 2), mọi API mới additive, không breaking method cũ
  - [x] Final Design 9 quyết định chốt: giữ nguyên 8 method cũ (chỉ thêm alias), tham số constructor mới (`files/cookies/server`) đặt cuối với default `[]`, không Container/singleton, copy dữ liệu 1 lần, giữ eager JSON parsing, không phụ thuộc Session/Validator/Database/Auth, không Method Spoofing (`_method`), không Trusted Proxy (`ip()` chỉ đọc `REMOTE_ADDR`)
  - [x] Sửa `core/Http/Request.php` — CHỈ additive: thêm `files/cookies/server` (cuối constructor), `fromGlobals()` đọc thêm `$_FILES/$_COOKIE/$_SERVER`, `withRouteParams()` giữ nguyên 3 property mới, thêm 13 method (`method/uri/path/all/has/filled/cookie/file/ip/userAgent/isMethod/ajax/json`)
  - [x] `tests/Core/Http/RequestTest.php` (+14 test), `tests/Core/Http/RequestFromGlobalsTest.php` (+1 test)
  - [x] Rà soát toàn bộ ~30 lệnh gọi `new Request(...)` hiện có trong codebase — xác nhận 100% backward compatible (đều dùng ≤7 tham số, không bị ảnh hưởng bởi 3 tham số mới)
  - [x] Self Code Review — phát hiện & sửa 2 failure PHPUnit thật: test `ajax()`/`json()` tự viết dùng header key sai convention (chưa uppercase, trong khi `header()` chỉ chuẩn hoá phía tra cứu, không chuẩn hoá key đã lưu — hành vi đã có từ CMS-006, không sửa production code) — sửa đúng phạm vi trong test data
  - [x] Self Architecture Review — 0 dependency mới, đúng cả 9 Decision, không breaking Public API
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (229 tests, 382 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-015 COMPLETED — tag `v0.0.15`**
- [x] **CMS-016** — HTTP Response Layer (mở rộng `core/Http/Response.php` đã có từ CMS-006)
  - [x] Architecture Analysis + Final Design 6 quyết định: mở rộng file đã có (Decision #0), KHÔNG `apiSuccess()/apiError()` (business convention, không phải HTTP contract — Decision #1 REJECTED), thêm `withHeader()/withHeaders()/withStatus()` (Decision #2), `withCookie()` không đọc Config, giữ 0 dependency (Decision #3), KHÔNG `download()/file()` (filesystem là chủ đề riêng — Decision #4 REJECTED), thêm `withCache()/noCache()` chỉ thao tác `Cache-Control` (Decision #5)
  - [x] Sửa `core/Http/Response.php` — thêm property `cookies` (cuối constructor, default `[]`, tách riêng khỏi `headers` vì HTTP cho phép nhiều `Set-Cookie`), 7 method mới: `withHeader/withHeaders/withStatus/withCookie/withCache/noCache/getCookies`, `send()` gửi thêm `Set-Cookie` qua `header(..., false)`
  - [x] `tests/Core/Http/ResponseTest.php` (17 test — file test mới, trước đây `Response` chỉ được kiểm chứng gián tiếp qua `RouterTest`/`ControllerResolverTest`)
  - [x] Self Code Review — không phát hiện bug, trace tay xác nhận cookie cùng tên ghi đè đúng, `httponly` mặc định `true` nhưng tôn trọng giá trị `false` tường minh, chuỗi `with*()` không mutate instance gốc
  - [x] Self Architecture Review — 0 dependency mới, immutable tuyệt đối, đúng cả 6 Decision
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (243 tests, 424 assertions), 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-016 COMPLETED — tag `v0.0.16`**
- [x] **CMS-017** — Redirect (Architecture Decision, KHÔNG Implementation)
  - [x] Architecture Analysis phát hiện: `Response::redirect()` (CMS-006) + `Session::flash()`/`getFlash()` (CMS-007) + `Request::all()`/`header('Referer')` (CMS-015) đã đủ để Controller tự viết pattern "redirect kèm flash message/old input" mà không cần thêm Core code nào — Decision #0 được đặt ra: có thực sự cần 1 class trung gian (`Redirector`) hay chỉ là convention?
  - [x] Phân tích rủi ro nếu tạo class trung gian: phương án gắn thẳng vào `Response`/`Session` (mở rộng file đã có) sẽ **phá vỡ nguyên tắc "Response/Session độc lập tuyệt đối"** đã xuyên suốt từ CMS-001 và vừa tái khẳng định ở Decision #3 của CMS-016 (giữ `Response` 0 dependency) — xác định đây là xung đột kiến trúc thật, không phải chi tiết implementation
  - [x] **Owner quyết định (Decision #0): KHÔNG cần Core code mới** — đúng YAGNI, chưa có Module Auth/Form thật nào cần pattern này để chứng minh nhu cầu lặp lại đủ nhiều. Ghi nhận **convention chính thức**: Controller tự gọi `$session->flash($key, $value)` (có thể gọi nhiều lần cho nhiều field) rồi `return Response::redirect($location)` — không qua lớp trung gian nào
  - [x] Technical Debt ghi nhận: nếu Module Auth/Form sau này (Phase 3+) thực sự lặp lại boilerplate này ở ≥2 nơi, xem xét lại việc tạo `core/Redirector.php` (phương án C đã phân tích — service riêng, nhận `Session` qua constructor, KHÔNG đụng `Response.php`/`Session.php` đã hoàn thành) — không tạo trước khi có nhu cầu thật, đúng nguyên tắc "không tạo abstraction chỉ để DRY"
  - [x] **CMS-017 đóng bằng Architecture Decision — không có file tạo/sửa, không có PHPUnit run, không có tag mới** (không phải Implementation task)
- [x] **CMS-018** — Middleware Pipeline Extension (mở rộng `core/Router.php`, Middleware Pipeline gốc từ CMS-006 giữ nguyên)
  - [x] Architecture Analysis phát hiện: `MiddlewareInterface`/`MiddlewarePipeline` đã tồn tại và hoạt động từ CMS-006 (onion model, short-circuit, resolve qua Container) — phạm vi thật của CMS-018 không phải "xây Pipeline" mà là hoàn thiện cách đăng ký middleware: (1) Global Middleware (chưa có cơ chế nào áp dụng cho MỌI route), (2) Route-level middleware đơn lẻ (trước đây chỉ gán được qua `group()`)
  - [x] Final Decision: Router là owner duy nhất của middleware lifecycle (không sửa `Application.php`), giữ nguyên tuyệt đối `MiddlewareInterface`/`MiddlewarePipeline`/`process(Request,Closure):Response`/short-circuit, thứ tự onion `Global → Group → Route-specific → Controller`, loại trừ rõ: không alias middleware, không terminate hook, không plugin/event middleware
  - [x] Sửa `core/Router.php` — thêm `globalMiddleware` (property) + `middleware(array): static` (append, fluent), `get/post/put/patch/delete` thêm tham số cuối `array $middleware = []`, `addRoute()` gộp `[...groupMiddleware, ...routeMiddleware]` lúc đăng ký, `dispatch()` gộp `[...globalMiddleware, ...route->getMiddleware()]` **runtime** (không "nướng cứng" vào Route lúc đăng ký, tránh phụ thuộc thứ tự gọi `middleware()`). `core/Route.php` xác nhận **không cần sửa** (constructor vẫn nhận middleware phẳng như cũ)
  - [x] `tests/Fixtures/Http/MiddlewareC.php` (mới, tag thứ 3 để phân biệt Global/Group/Route trong 1 test) + `tests/Core/RouterTest.php` (+6 test: global chạy trên mọi route, thứ tự onion Global→Group→Route đúng, route-level chỉ áp dụng đúng route đó, route-level kết hợp đúng với group, global short-circuit chặn cả group/route/Controller, global không rò rỉ qua Router instance khác)
  - [x] Self Code Review — trace tay toàn bộ 6 test mới + xác nhận 17 test cũ không bị ảnh hưởng (tham số mới additive, default `[]`)
  - [x] Self Architecture Review — đúng Final Decision, không đụng `Application.php`/`Route.php`/`MiddlewareInterface.php`/`MiddlewarePipeline.php`
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `RouterTest.php` riêng: 19 tests/29 assertions OK; toàn bộ suite: 249 tests, 433 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-018 COMPLETED — tag `v0.0.18`**
- [x] **CMS-019** — Exception Handler (`core/ExceptionHandler.php`, tách khỏi `core/Application.php`)
  - [x] Architecture Analysis xác nhận: exception handling đã tập trung tuyệt đối tại `Application::handle()` từ CMS-011 (không duplicate ở đâu khác — `Router`/`MiddlewarePipeline`/`ControllerResolver`/`View` đều không có try/catch swallow, `View::renderTemplate()` chỉ rethrow sau khi dọn output buffer). Phát hiện xung đột cấu trúc loại bỏ Option "Middleware-based Exception Handler": `Router::match()` ném `RouteNotFoundException`/`MethodNotAllowedException` TRƯỚC KHI `MiddlewarePipeline` được gọi — 1 Middleware (kể cả Global, dù mới có ở CMS-018) không bao giờ bắt được 2 exception này
  - [x] Final Design 6 quyết định: tách `Core\ExceptionHandler` riêng (Option B, cải thiện SRP cho `Application`), mapping tĩnh không registry/extend() (YAGNI — chỉ 2/23 exception cần map riêng), debug block (exception/file/line/trace) chỉ khi `debug=true && status=500`, Logger vẫn ngoài phạm vi (giữ nguyên `logException()` tại `Application`, không chuyển vào `ExceptionHandler`), không auto-map `ValidationException`→422 (ranh giới: Controller tự catch nếu muốn), không map 403 (chưa có Auth/Authz)
  - [x] `core/ExceptionHandler.php` — `final class`, **0 dependency** (không Config/Container/Logging), 1 method `handle(Throwable, bool): Response`, mapping tĩnh 404/405/500, debug block dùng `explode("\n", getTraceAsString())` (không dùng `getTrace()` để tránh lỗi serialize JSON với object/resource không encode được)
  - [x] Sửa `core/Application.php` — gộp 3 nhánh `catch` (`RouteNotFoundException`/`MethodNotAllowedException`/`Throwable`) thành 1 `catch (Throwable)` duy nhất, delegate `ExceptionHandler`, quyết định log dựa trên `$response->getStatusCode() >= 500` (thay vì theo loại exception) — giữ **chính xác** hành vi log cũ (404/405 không log, 500 vẫn log) mà không cần `Application` biết tên class exception cụ thể nào nữa. Xoá `errorResponse()` (đã grep xác nhận 0 nơi khác dùng trước khi xoá). `isDebug()`/`logException()` không đổi 1 dòng
  - [x] `tests/Core/ExceptionHandlerTest.php` (8 test, unit thuần không Router/Application/filesystem) + `tests/Core/ApplicationTest.php` (+1 test `testRouteNotFoundDoesNotWriteToLogFile`, khoá tường minh hành vi "chỉ log status ≥ 500", 11 test cũ không sửa)
  - [x] Self Code Review — trace tay toàn bộ 8+1 test mới, đối chiếu số liệu PHPUnit thật khớp chính xác dự đoán (8 tests/20 assertions ExceptionHandlerTest, 11 tests/27 assertions ApplicationTest — đúng 10 test cũ + 1 test mới)
  - [x] Self Architecture Review — SRP cải thiện rõ rệt (`Application` chỉ boot+log-trigger+delegate, `ExceptionHandler` chỉ map), 0 dependency mới ngoài `Application→ExceptionHandler`, không đụng `Router.php`/`Route.php`/`Middleware/*`/`Response.php`/`Request.php`
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `ExceptionHandlerTest`: 8 tests/20 assertions; `ApplicationTest`: 11 tests/27 assertions; `RouterTest`: 19 tests/29 assertions; toàn bộ suite: 258 tests, 455 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-019 COMPLETED — tag `v0.0.19`**
- [x] **CMS-020** — CSRF Protection (`core/Csrf.php` + `core/Middleware/CsrfMiddleware.php`)
  - [x] Architecture Analysis phát hiện: `Session` (CMS-007) đã dự trù sẵn namespace `csrf.token` trong docblock; `cms-architecture-proposal.md` đã định vị CSRF là 1 Middleware trong pipeline gốc (`TenantResolver → Auth → Locale → CSRF (POST) → Controller`), chỉ áp dụng cho route dùng Session (Admin/SSR), không áp dụng `/api/*` (JWT) — loại trừ tự nhiên qua việc chỉ gắn middleware vào đúng group cần, không cần logic phát hiện API bên trong
  - [x] Final Design 7 quyết định: tách `Csrf` (token lifecycle, thuần logic) + `CsrfMiddleware` (tích hợp HTTP) — Option B; hardcode field/header name (`_token`/`X-CSRF-TOKEN`/`X-XSRF-TOKEN`, không Config); fail trả 419 (không 403) với envelope cố định; không `CsrfException` mới (Middleware tự trả Response, không đụng `ExceptionHandler`); token sống theo Session, không regenerate mỗi request; đặt trong Core (thuần cơ chế, không business logic); không sửa `View` (Controller tự truyền `$csrf->token()`)
  - [x] Xác nhận qua đọc trực tiếp `Container.php`: `Csrf`/`CsrfMiddleware` tự auto-wire được qua Container (fallback `class_exists()` khi không có binding tường minh) — **0 file Core cũ nào cần sửa**, kể cả `Application.php`
  - [x] Code Review Simulation (trước Implementation) phát hiện 1 gap cần xử lý: `Request::input('_token')` có thể trả về `array` (không chỉ `string`) — bắt buộc guard `is_string()` trước khi gọi `Csrf::verify()`, không được ép kiểu `(string)` mù quáng (tránh PHP Warning "Array to string conversion")
  - [x] `core/Csrf.php` (`token()`/`verify()`, `bin2hex(random_bytes(32))`, `hash_equals()`), `core/Middleware/CsrfMiddleware.php` (implement `MiddlewareInterface` có sẵn từ CMS-006, không đổi contract)
  - [x] `tests/Core/CsrfTest.php` (6 test) + `tests/Core/Middleware/CsrfMiddlewareTest.php` (11 test — 9 test chuẩn + 2 edge case bắt buộc từ Code Review: `_token` rỗng không fallback qua header, `_token` dạng mảng bị từ chối sạch không Warning)
  - [x] Self Code Review — trace tay toàn bộ 17 test mới, đối chiếu đúng số liệu PHPUnit thật (6 tests/7 assertions `CsrfTest`, 11 tests/17 assertions `CsrfMiddlewareTest`)
  - [x] Self Architecture Review — SRP đạt (2 class tách bạch), 0 file cũ bị sửa, CSRF hoàn toàn opt-in (không tự gắn route nào — việc đó thuộc Module/App sau này)
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `CsrfTest`: 6 tests/7 assertions; `CsrfMiddlewareTest`: 11 tests/17 assertions; toàn bộ suite: 275 tests, 479 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-020 COMPLETED — tag `v0.0.20`**
- [x] **CMS-021** — Authentication (`core/Auth.php` + `core/Middleware/AuthMiddleware.php`)
  - [x] Architecture Analysis phát hiện: `Session` (CMS-007) đã dự trù sẵn namespace `auth.user_id/auth.roles/auth.permissions`; không có bảng `users` nào tồn tại (chưa có migration thật); `02-module-auth.md` mô tả "Module Auth" đầy đủ (JWT/password reset/login_logs) là phạm vi Phase 3+ riêng, tách biệt khỏi CMS-021 Foundation
  - [x] Final Design (Option A): `Auth` CHỈ quản lý session-state ("đã đăng nhập hay chưa"), không verify credential, không Database, không password — việc xác thực mật khẩu against DB là trách nhiệm Module Auth tương lai, gọi `Auth::login()` SAU KHI đã tự verify xong
  - [x] `core/Auth.php` (`login()/logout()/check()/id()/user()`) — `login()` theo đúng thứ tự `regenerate()` → `remove('csrf.token')` → `set('auth.user_id')` → `set('auth.user')` (chống session fixation + rotate CSRF token khi nâng mức tin cậy); `logout()` dùng `Session::destroy()` (huỷ toàn bộ, không chỉ xoá `auth.*`)
  - [x] `core/Middleware/AuthMiddleware.php` — implement `MiddlewareInterface` có sẵn, chặn request chưa đăng nhập trả 401 JSON, không redirect, không Config
  - [x] `tests/Core/AuthTest.php` (13 test) + `tests/Core/Middleware/AuthMiddlewareTest.php` (3 test)
  - [x] Self Code Review phát hiện & sửa 1 bug thật trong TEST (không phải trong `Auth`): `Session::destroy()` khiến `isStarted()` về `false`, test kiểm tra lại trạng thái ngay sau `logout()` mà không `start()` lại session sẽ nhận `SessionException` — sửa bằng cách mô phỏng đúng "request kế tiếp tự start() lại", không đổi `Auth`/`Session`
  - [x] Xác nhận qua đọc lại `Container.php` (nhất quán CMS-020): `Auth`/`AuthMiddleware` tự auto-wire, **0 file Core cũ nào bị sửa**
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `AuthTest`: 13 tests/17 assertions; `AuthMiddlewareTest`: 3 tests/4 assertions; toàn bộ suite: 291 tests, 500 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-021 COMPLETED — tag `v0.0.21`**
- [x] **CMS-022** — Authorization (`core/Authorization.php` + `core/Middleware/AuthorizationMiddleware.php`)
  - [x] Architecture Analysis phát hiện xung đột kiến trúc thật (đã đọc trực tiếp `Container.php`/`MiddlewarePipeline.php`): cơ chế Middleware hiện tại (`list<class-string>`, `Container::get(string $id)`) không hỗ trợ tham số hoá per-route — không thể `new AuthorizationMiddleware(['admin'])` gắn trực tiếp vào 1 route cụ thể như đề xuất ban đầu
  - [x] Owner Decision: **Phương án C** — không sửa `MiddlewarePipeline`/`Router`/`Route`/`Container`, `AuthorizationMiddleware` không tham số hoá, chỉ là gate chung (chặn khi user không có role/permission nào); kiểm tra quyền cụ thể per-route là trách nhiệm Controller tự gọi `Authorization::hasRole()/can()` trực tiếp
  - [x] `core/Authorization.php` (`roles()/permissions()/hasRole()/hasAnyRole()/hasAllRoles()/hasPermission()/hasAnyPermission()/hasAllPermissions()/can()` — `can()` alias thuần của `hasPermission()`), đọc `auth.roles`/`auth.permissions` từ Session (namespace đã dự trù từ CMS-007), không ghi, không DB
  - [x] `core/Middleware/AuthorizationMiddleware.php` — chặn khi `roles()===[] && permissions()===[]`, trả 403 JSON
  - [x] `tests/Core/AuthorizationTest.php` (17 test) + `tests/Core/Middleware/AuthorizationMiddlewareTest.php` (4 test)
  - [x] **Verified** — `vendor/bin/phpunit` PASS (trong lần chạy toàn bộ suite cùng CMS-023/024) → **CMS-022 COMPLETED — tag `v0.0.22`**
- [x] **CMS-023** — Rate Limiter (`core/RateLimiter.php` + `core/Middleware/RateLimitMiddleware.php`)
  - [x] Architecture Analysis phát hiện đúng loại xung đột như CMS-022 (Middleware cần tham số `key`/`maxAttempts`/`decaySeconds` per-route, cơ chế hiện tại không hỗ trợ)
  - [x] Owner Decision: `RateLimiter` là Foundation Component thuần cơ chế (đọc/ghi/reset quota qua Session, namespace `rate_limit.{key}`, chỉ integer timestamp — không object/DateTime/serialize). `RateLimitMiddleware` là **placeholder framework component** — chỉ implement `MiddlewareInterface` và `return $next($request);`, KHÔNG tự xác định key/limit, KHÔNG gọi `hit()`. Logic rate-limit thật do Module tương lai tự gọi `RateLimiter` trực tiếp (biết đủ business context để xác định bucket, VD `hit('login:'.$ip, 5, 60)`)
  - [x] `core/RateLimiter.php` (`hit()/tooManyAttempts()/attempts()/remaining()/clear()/availableIn()`)
  - [x] `tests/Core/RateLimiterTest.php` (14 test) + `tests/Core/Middleware/RateLimitMiddlewareTest.php` (4 test — chỉ xác nhận hành vi passthrough, không test 429 vì hành vi đó không tồn tại theo Owner Decision)
  - [x] **Verified** — `vendor/bin/phpunit` PASS (trong lần chạy toàn bộ suite cùng CMS-022/024) → **CMS-023 COMPLETED — tag `v0.0.23`**
- [x] **CMS-024** — Logger (`core/Logger.php`)
  - [x] Architecture Analysis: phát hiện "CMS-024 — Cache" (đề xuất ban đầu) trùng lặp hoàn toàn với `Cache`/`CacheDriver` đã hoàn thành từ CMS-008 (tag `v0.0.8`) — dừng lại, Owner xác nhận đổi CMS-024 thành Logger. Grep toàn bộ `core/` xác nhận: chỉ 1 nơi ghi log thật (`Application::logException()`), 2 điểm mở đã dự trù từ trước chưa được nối dây (`Database::onQueryExecuted()` từ CMS-004, `Hook::onError()` từ CMS-009)
  - [x] Owner Decision: không PSR-3 (không thêm Composer package), constructor nhận `string $logPath` trực tiếp (không Config, theo tiền lệ `MigrationManager`), không level filtering/channel/formatter/handler/rotation/async/buffering — API tối giản đúng 1 method `log(level, message, context)`
  - [x] `core/Logger.php` — ghi 1 dòng `[timestamp] level: message {context json nếu có}` vào file cố định, tự tạo thư mục cha nếu thiếu (cùng cơ chế `Application::logException()`), `@file_put_contents(..., FILE_APPEND|LOCK_EX)` không throw khi ghi thất bại
  - [x] `tests/Core/LoggerTest.php` (8 test, filesystem thật/temp dir)
  - [x] Không nối dây vào `Application`/`Database`/`Hook`/`ExceptionHandler` — đúng phạm vi Foundation, việc thay thế `Application::logException()` và nối 2 điểm mở còn để dành CMS sau
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `LoggerTest`: 8 tests/10 assertions; toàn bộ suite: 338 tests, 561 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-024 COMPLETED — tag `v0.0.24`**
- [x] **CMS-025** — TenantManager (`core/TenantManager.php`)
  - [x] Architecture Analysis phát hiện: `core/View.php` (docblock từ CMS-005) đã dự trù `$activeTheme` "tương lai: TenantManager"; `config/app.php` (comment từ CMS-011) chờ "TenantManager thật (Phase 2+)"; `Session.php` đã dự trù namespace `tenant.current` từ CMS-007; `Cache`/`QueryBuilder::forTenant()` chờ nguồn cung cấp `{tenantId}` — 6 điểm mở khác nhau xác nhận gap thật, không suy đoán. Đồng thời phát hiện `TODO.md` lỗi thời so với `cms-architecture-proposal.md` gốc (thiếu hẳn `TenantManager`/`ThemeManager` trong danh sách Core Component dòng 405)
  - [x] Phát hiện giới hạn giống `Auth` (CMS-021): bảng `sites`/`site_domains` (nơi thật sự resolve domain→tenant, theo `database-design.md`) chưa tồn tại (chưa có migration) — TenantManager KHÔNG tự resolve, chỉ quản lý state "tenant hiện tại"
  - [x] Owner Decision quan trọng: **KHÔNG dùng Session** (khác `Auth`/`Authorization`/`Csrf`/`RateLimiter`) — vì tenant phải xác định cho MỌI request (kể cả API/JWT, không riêng luồng Session-based), ép `Session::start()` chỉ để biết site nào sẽ vi phạm triết lý lazy-start đã có từ CMS-007. Dùng property in-memory thuần — state tự cô lập theo từng request vì `Container` đã là 1-per-request
  - [x] `core/TenantManager.php` — `setCurrent(int|string $tenantId, array $data=[])/check()/id()/current()`, **0 dependency** (không constructor, mức cô lập cao nhất từ trước tới nay, thấp hơn cả `Logger`)
  - [x] `tests/Core/TenantManagerTest.php` (9 test, unit thuần — không Session/filesystem/DB, đơn giản nhất từ trước tới nay)
  - [x] Không sửa `View`/`Application`/`Session`/`Config`/`Database`/`Request` — đúng phạm vi Foundation, việc nối dây (`View` factory trong `Application.php` đọc `TenantManager::current()`, resolve domain→tenant qua `site_domains`) để dành CMS sau khi có migration `sites`/`site_domains` thật
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `TenantManagerTest`: 9 tests; toàn bộ suite: 347 tests, 574 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-025 COMPLETED — tag `v0.0.25`**
- [x] **CMS-026** — ThemeManager (`core/ThemeManager.php` + `core/Theme/*`)
  - [x] Architecture Analysis xác nhận ranh giới rõ ràng với `View` (đọc lại toàn văn `View.php`): `View` chỉ render (resolve file theo active/default theme đã biết trước), không hề biết theme nào tồn tại/metadata — `ThemeManager` chỉ discovery/metadata, không render. So sánh với `ModuleManager`/`PluginManager` xác nhận đây là Component mới thật, mô hình discovery-qua-manifest giống `ModuleManager` (không memoize) nhưng đơn giản hơn (không dependency resolution giữa theme). Đọc `database-design.md` xác nhận filesystem LUÔN là source of truth cho theme (không chỉ tạm thời do thiếu migration như `Auth`/`TenantManager` — đây là nguyên tắc thiết kế vĩnh viễn) → **ThemeManager không dùng Database, kể cả trong tương lai**
  - [x] Owner Decision: API chỉ `discover()/find()` (không `exists()/all()/active()/setActive()`); `ThemeDescriptor` chỉ `key/name/version/screenshot/path` (không `dependencies/boot file/enabled state/tenant_id`); **không memoize** (đối lập `PluginManager`, giống `ModuleManager` — filesystem là source of truth, theme có thể đổi runtime); không `boot()`; không đăng ký `Application`/`Container` (Foundation trước, integration để CMS sau)
  - [x] `core/ThemeManager.php` (`discover()/find()`, glob `theme.json`, không memoize), `core/Theme/ThemeDescriptor.php` (value object readonly), `core/Theme/ThemeException.php` (`cannotRead()/invalidManifest()`, mirror `ModuleException`)
  - [x] `tests/Core/ThemeManagerTest.php` (7 test) + fixture `tests/Fixtures/Themes/{Alpha,Beta}` + `tests/Fixtures/ThemesInvalid/BadTheme`
  - [x] Không sửa `View`/`Application`/`Container`/`Config`/`TenantManager` — đúng phạm vi Foundation, việc nối `ThemeManager` vào `View`/`Application` (thay `config('app.theme')` tĩnh) để dành CMS sau
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `ThemeManagerTest`: 7 tests/15 assertions; toàn bộ suite: 354 tests, 589 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-026 COMPLETED — tag `v0.0.26`**
- [x] **CMS-027** — Middleware Parameterization (`core/Middleware/MiddlewarePipeline.php`)
  - [x] Architecture Analysis xác nhận lại (đọc trực tiếp `MiddlewarePipeline.php`/`Router.php`/`Route.php`/`Container.php`/`AuthorizationMiddleware.php`/`RateLimitMiddleware.php`): giới hạn gốc đúng như phát hiện ở CMS-022/023 — `MiddlewarePipeline::handle()` chỉ chấp nhận `list<class-string>`, resolve cứng qua `Container::get(string $id)`, không có đường nào truyền instance đã cấu hình sẵn vào pipeline
  - [x] Owner Decision: **CMS-027 chỉ là infrastructure** — `MiddlewarePipeline` hỗ trợ thêm `MiddlewareInterface` instance đã cấu hình sẵn (song song class-string cũ), KHÔNG redesign `AuthorizationMiddleware`/`RateLimitMiddleware`, KHÔNG thêm permission parameter/rate limit configuration, KHÔNG đổi security policy. Invalid middleware (không phải string, không phải `MiddlewareInterface`) → `throw new \InvalidArgumentException` (không tạo Exception class mới, tránh abstraction thừa). Bắt buộc: `Container.php` không sửa, `Router`/`Route` runtime không đổi (chỉ PHPDoc)
  - [x] Sửa `core/Middleware/MiddlewarePipeline.php` — thêm `private resolve(mixed $middlewareEntry): MiddlewareInterface` (string → `Container::get()` như cũ; `MiddlewareInterface` → dùng trực tiếp; còn lại → `\InvalidArgumentException` kèm `get_debug_type()`), `handle()` gọi qua `resolve()` thay vì `Container::get()` trực tiếp, tham số closure đổi `string` → `mixed` (để lỗi type rơi vào `resolve()` thay vì `TypeError` mù mờ của PHP)
  - [x] Cập nhật PHPDoc (không đổi runtime): `core/Route.php` (constructor + `getMiddleware()`), `core/Router.php` (`middleware()/get()/post()/put()/patch()/delete()/group()/addRoute()` + 2 property `$groupMiddleware`/`$globalMiddleware`) — kiểu mới `list<class-string<MiddlewareInterface>|MiddlewareInterface>`
  - [x] `tests/Core/Middleware/MiddlewarePipelineTest.php` (mới — trước đây `MiddlewarePipeline` chỉ được test gián tiếp qua `RouterTest.php`) — 5 test: class-string qua Container (regression), instance trực tiếp, trộn class-string + instance trong cùng chain, short-circuit với instance, `\InvalidArgumentException` cho input không hợp lệ (`@dataProvider`: int/array/object không implement `MiddlewareInterface`)
  - [x] Không sửa `Container.php`. Không sửa test cũ (`RouterTest.php`, `CsrfMiddlewareTest.php`, `AuthMiddlewareTest.php`, `AuthorizationMiddlewareTest.php`, `RateLimitMiddlewareTest.php`) — dùng làm regression lock
  - [x] Self Code Review — trace tay xác nhận toàn bộ middleware hiện có (`CsrfMiddleware`/`AuthMiddleware`/`AuthorizationMiddleware`/`RateLimitMiddleware`, mọi fixture) đăng ký bằng class-string → đi đúng nhánh `is_string()` cũ, hành vi không đổi
  - [x] Self Architecture Review — SRP đạt (`resolve()` tách biệt "biết lấy instance"), OCP cải thiện (mở rộng không sửa code gọi), không tạo abstraction mới (không `MiddlewareResolver`/`Factory`/`Definition`), YAGNI đạt, regression risk thấp có kiểm soát
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật — 361 tests, 597 assertions, PASS → **CMS-027 COMPLETED — tag `v0.0.27`**
- [x] **CMS-029** — Logger Integration (`core/Application.php`, wiring `Logger` vào luồng chạy thật)
  - [x] Roadmap Review sau CMS-028 liệt kê Candidate A (Integration/Wiring, chia nhỏ A1 Logger/A2 TenantManager/A3 ThemeManager), B (Auth/Authorization Foundation), C (Content Schema tiếp tục), D (Event Dispatcher), E (Queue) — Owner chọn **A1 — Logger Integration**, lý do: rủi ro thấp nhất, không phụ thuộc migration, ít chạm Core ổn định
  - [x] Architecture Analysis xác nhận qua đọc trực tiếp: `Application::logException()` tự viết logic ghi file trùng lặp gần như hoàn toàn với `Logger::log()` (CMS-024); `Database::onQueryExecuted()` (điểm mở từ CMS-004) và `Hook::onError()` (điểm mở từ CMS-009) chưa từng có listener nào đăng ký (đã grep xác nhận); `Logger` không tự auto-wire được (constructor cần `string $logPath`, không default) — khác các Foundation Component trước, cần đăng ký tường minh vào Container
  - [x] Owner Decision phạm vi: CHỈ 3 điểm — (1) đăng ký `Logger` singleton trong `registerCoreServices()`, path giữ nguyên `storage/logs/app.log`; (2) `logException()` chuyển sang gọi `Logger::log('error', ...)`, context gồm `exception_class/file/line/trace`; (3) `boot()` đăng ký `Hook::onError()` listener ghi log qua `Logger`, context gồm `hook/exception_class/file/line`. KHÔNG triển khai Database query logging (`onQueryExecuted` fire cho MỌI query, chưa có requirement debug/performance — để dành CMS riêng), KHÔNG log `PluginManager::getFailures()` (khác domain — lifecycle error của Plugin, không phải runtime Hook callback error), KHÔNG log level convention mới (giữ nguyên `Logger::log(string $level, ...)`, chỉ thống nhất dùng `'error'` cho 2 điểm trên), KHÔNG log rotation, KHÔNG Exception/Adapter/abstraction logging mới
  - [x] Sửa `core/Application.php` — CHỈ 3 điểm đúng Owner Decision: thêm `Logger::class` singleton, viết lại thân `logException()` (bỏ `mkdir`/`sprintf`/`file_put_contents` tự viết), thêm `$hook->onError(...)` trong `boot()` (đăng ký trước `$pluginManager->boot()`)
  - [x] `tests/Core/ApplicationTest.php` (+2 test, không sửa test cũ): `testExceptionLogContainsExceptionClassFileLineAndTrace`, `testHookCallbackExceptionIsLoggedViaLogger` (đăng ký Hook action ném lỗi trực tiếp qua `Hook::action()/do()`, không cần fixture plugin mới)
  - [x] Không sửa `core/Logger.php`/`core/Database.php`/`core/Hook.php`/`core/PluginManager.php`/`core/ExceptionHandler.php` — đúng phạm vi đã duyệt. Đây là lần đầu sửa `core/Application.php` kể từ CMS-019 (9 CMS không đụng tới)
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `ApplicationTest`: 13 tests/31 assertions; toàn bộ suite: 374 tests, 652 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-029 COMPLETED — tag `v0.0.29`**
- [x] **CMS-030** — TenantManager Integration (`core/Middleware/TenantResolverMiddleware.php` + `core/Application.php`)
  - [x] Roadmap Review sau CMS-029 liệt kê Candidate A (TenantManager Integration), B (Auth/Authorization Foundation), C (ThemeManager Integration), D (Content Schema tiếp tục), E (Event Dispatcher), F (Queue), G (Deferred Observability) — Owner chọn **Candidate A — TenantManager Integration**
  - [x] Architecture Analysis phát hiện quan trọng (đọc trực tiếp `config/tenants.php`): kiến trúc gốc đã dự trù sẵn `core/Middleware/TenantResolverMiddleware.php` (comment "task CMS-009+") — xác nhận đây đúng là vị trí thiết kế gốc, không phải quyết định tuỳ ý. Xác nhận `Request::getHost()` đọc `$_SERVER['HTTP_HOST']` trực tiếp — client-controllable, bắt buộc chỉ dùng làm giá trị lookup qua prepared statement, không tin cậy cho mục đích khác. Xác nhận `View::__construct(themesPath, activeTheme, defaultTheme)` — `activeTheme` là tham số constructor, có thể đọc `TenantManager` tại thời điểm Closure factory chạy (lazy, sau khi Middleware đã set) mà không cần sửa `View.php`
  - [x] Owner Decision (5 Open Question): Q1 resolve tại Middleware (không phải `Application::boot()` — `boot()` idempotent, không nhận `Request`, đổi chữ ký sẽ phá tính idempotent); Q2 fail-closed — domain không khớp `site_domains` → 404 ngay, không fallback tenant mặc định; Q3 sửa Closure `View::class` đọc `TenantManager::current()['theme_active']`, fallback `config('app.theme')` khi NULL hoặc chưa có tenant; Q4 không xử lý `system_admin.domains` bypass (để CMS Super Admin riêng); Q5 `/health` và route hệ thống tương lai nằm ngoài phạm vi Middleware
  - [x] Final Design: `TenantResolverMiddleware` inject `Database`+`TenantManager` qua constructor (đúng pattern `AuthMiddleware`), dùng `Database::selectOne()` với 1 câu SQL `JOIN sites+site_domains` (không qua `QueryBuilder::join()` — né giới hạn đã ghi nhận ở Technical Debt #3), 404 dùng đúng envelope `{success:false,data:null,message:'Not Found',errors:[]}` khớp `ExceptionHandler`. Phát hiện kỹ thuật quan trọng: Global Middleware (`Router::middleware()`) áp dụng vô điều kiện cho MỌI route, không có cơ chế exclude per-route — giải pháp dùng `Router::group()` (đã có từ CMS-006/018) bọc riêng `ModuleManager::boot()`, giữ `/health` đăng ký ngoài group, không cần sửa `Router.php`/`MiddlewarePipeline.php`/`Route.php`
  - [x] `core/Middleware/TenantResolverMiddleware.php` (mới) — `process()` query `sites JOIN site_domains WHERE domain = ?`, không khớp → 404 (không gọi `$next`), khớp → `TenantManager::setCurrent()` → `$next($request)`. Không xử lý Auth/Permission/site status/Super Admin
  - [x] Sửa `core/Application.php` — `registerCoreServices()`: **phát hiện bug thật trước khi báo cáo** (trace lại `Container::get()`: không có binding thì không cache dù auto-wire được) — `TenantManager::class` phải đăng ký `singleton()` tường minh, nếu không `TenantResolverMiddleware` và `View` Closure sẽ thao tác trên 2 instance `TenantManager` khác nhau, tích hợp không hoạt động dù test có vẻ pass giả; sửa Closure `View::class` đọc `TenantManager::current()['theme_active'] ?? config('app.theme')`. `boot()`: bọc `$moduleManager->boot($router, ...)` trong `router->group(['middleware' => [TenantResolverMiddleware::class]], ...)`, `/health` giữ nguyên ngoài group, chữ ký `boot()` không đổi
  - [x] `tests/Core/Middleware/TenantResolverMiddlewareTest.php` (mới, 4 test): resolve thành công, `TenantManager::setCurrent()` đúng dữ liệu, domain không khớp → 404 đúng envelope + không gọi `$next`, không set tenant khi domain không khớp
  - [x] `tests/Core/ApplicationTest.php` — thêm helper `seedTenant()` (tạo bảng tối giản + insert, không chạy `MigrationManager` thật — giữ test độc lập/nhanh), áp dụng cho 5 test bị ảnh hưởng (`/ping`, 2 test `/boom` debug true/false, 2 test log liên quan `/boom`); thêm 3 test mới: theme đọc từ tenant, fallback `config('app.theme')` khi `theme_active` NULL, `/health` vẫn hoạt động không cần seed
  - [x] **Phát hiện fixture thiếu khi viết test**: `tests/Fixtures/AppProduction/` chưa từng có `config/database.php` (trước CMS-030 không route nào ở fixture này chạm Database) — thiếu file này khiến `/boom` ném `ConnectionException` che giấu ý nghĩa test debug=false. Tạo `tests/Fixtures/AppProduction/config/database.php` (SQLite in-memory, giống fixture `App`) — ngoài File Impact Prediction ban đầu, phát sinh khi implement, đã báo cáo minh bạch trước khi PHPUnit chạy
  - [x] Không sửa `TenantManager.php`/`View.php`/`Database.php`/`Router.php`/`Route.php`/`MiddlewarePipeline.php` — đúng phạm vi đã duyệt
  - [x] Technical Debt ghi nhận (không xử lý trong CMS-030): site `suspended`/`maintenance` chưa được chặn ở `TenantResolverMiddleware` (domain vẫn resolve thành công dù site không active); domain normalization (lowercase/bỏ `:port`) chưa triển khai, giữ nguyên `Request::getHost()` thô; `system_admin.domains` bypass (đã có sẵn trong `config/tenants.php`) chưa triển khai — để dành CMS Super Admin/Authorization site-level riêng
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `TenantResolverMiddlewareTest`: 4 tests/9 assertions; `ApplicationTest`: 16 tests/34 assertions; toàn bộ suite: 381 tests, 664 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-030 COMPLETED — tag `v0.0.30`**
- [x] **CMS-031** — Auth/Authorization Foundation (`core/AuthenticationService.php`)
  - [x] Roadmap Review sau CMS-030 — so sánh 6 candidate còn mở:

    | Candidate | Decision | Reason |
    |---|---|---|
    | B — Auth/Authorization Foundation | **Chọn** | Duy nhất thực sự "mở khoá" tiến độ Phase 3 (Module Admin/User/Page cần Auth thật); dependency sẵn sàng 100% từ CMS-028 (`users/roles/permissions/role_permissions/user_site_roles`); business value cao nhất, technical/security risk chấp nhận được với Architecture Analysis kỹ |
    | C — ThemeManager Integration | Hoãn | Rủi ro thấp nhưng chưa có bằng chứng lỗi thật (`theme_active` hiện luôn NULL, chưa seed data) |
    | D — Content Schema Migration Phase 2 | Hoãn | Rủi ro kỹ thuật thấp nhất nhưng 0 Module dùng tới — vi phạm YAGNI ở mức schema |
    | E — Event Dispatcher | Hoãn | 0 use case thật, rủi ro cao nếu tái cấu trúc `Hook.php` (đã có 2+ dependent: `PluginManager`, `Application::onError()`) |
    | F — Queue | Hoãn | 0 use case thật, đã hoãn liên tục từ CMS-025 |
    | G — Deferred Observability (`PluginManager::getFailures()` logging) | Hoãn | Rủi ro thấp nhất nhưng không block gì — làm sau cũng được |

    Owner chọn **Candidate B — Auth/Authorization Foundation**
  - [x] Architecture Analysis (đọc trực tiếp `Auth.php`/`Authorization.php`/`Session.php`/`Database.php`/`Container.php`/`Application.php`/`Router.php`/`Request.php`/7 migration CMS-028/`database-design.md`/`config/auth.php`): xác nhận `Auth::login()`/`Authorization::roles()/permissions()` có API public tái sử dụng nguyên vẹn, không cần sửa; xác nhận `config('auth.login_throttle')` đã sẵn sàng khớp `RateLimiter::hit()` (để dành, không dùng trong CMS-031); xác nhận `Session::set()` public đủ dùng để nạp roles/permissions mà không cần sửa `Authorization.php`
  - [x] Owner Decision 8 Open Question: Q1 Service gọi `Database` trực tiếp (không Repository, đúng tiền lệ CMS-030); Q2 KHÔNG route/Controller trong CMS-031 (chỉ Foundation Service); Q3 `site_id` lấy từ `TenantManager::id()`; Q4 KHÔNG rate limit (để CMS riêng); Q5 chặn login nếu `users.status !== 'active'`; Q6 dummy `password_verify()` khi email không tồn tại (chống user enumeration timing); Q7 `PASSWORD_DEFAULT`; Q8 1 session = 1 site (đổi site cần login lại)
  - [x] Final Design: `AuthenticationService` (constructor inject `Database`/`Auth`/`Session`/`TenantManager`), method public duy nhất `attempt(string $email, string $password): bool` — trả `bool` cho mọi lỗi xác thực (đúng convention `Csrf::verify()`/`RateLimiter::hit()`, không tạo Exception mới), chỉ throw `\LogicException` built-in khi `TenantManager::check() === false` (lỗi tiền điều kiện của caller, không phải lỗi user). Thứ tự `status` check **SAU** `password_verify()` (không phải trước) — lý do bảo mật: tránh timing side-channel phân biệt tài khoản `locked` qua tốc độ phản hồi. Xác nhận **không cần đăng ký Container tường minh** — `AuthenticationService` không giữ state, auto-wire đủ dùng, CMS-031 không chạm `Application.php` (khác 3 CMS liên tiếp trước)
  - [x] `core/AuthenticationService.php` — `DUMMY_HASH` hằng số bcrypt cố định (chống timing side-channel khi email không tồn tại), query `users` chỉ lấy `id/password/status` (không `SELECT *`), 2 query raw SQL riêng cho roles/permissions (JOIN qua `user_site_roles`→`roles`→`role_permissions`→`permissions`, né giới hạn `QueryBuilder::join()` Technical Debt #3), `$userId` ép kiểu `(int)` tường minh (đúng pattern CMS-030), `Auth::login()` chỉ nhận `id`/`email` (không `password`), `Session::set('auth.roles'/'auth.permissions', list<string>)`
  - [x] `tests/Core/AuthenticationServiceTest.php` (mới, 10 test) — `Database` SQLite in-memory thật (seed tay `sites/site_domains/users/roles/permissions/role_permissions/user_site_roles`), `Session`/`Auth`/`TenantManager` thật, không mock: login đúng, sai password, email không tồn tại, status không active, roles/permissions nạp đúng Session, đúng site theo `TenantManager` (2 site khác nhau), `Auth::login()` được gọi đúng, `LogicException` khi chưa có tenant, hành vi email-không-tồn-tại giống hệt sai-password (không throw, cùng kết quả)
  - [x] Không sửa `Auth.php`/`Authorization.php`/`Session.php`/`Database.php`/`TenantManager.php`/`Application.php`/`Container.php`/`Router.php` — đúng phạm vi Foundation đã khoá, `Auth.php` tiếp tục không chứa password logic, `Authorization.php` tiếp tục chỉ đọc Session
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật — `AuthenticationServiceTest`: 10 tests; toàn bộ suite: 391 tests, 676 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-031 COMPLETED — tag `v0.0.31`**
- [x] **CMS-033** — Authentication Rate Limiting (`core/AuthenticationService.php`, mở rộng)
  - [x] Roadmap Review sau CMS-031 liệt kê Candidate B (Login Route + Rate Limiting), Authorization tham số hoá thật, Site status policy — Owner chọn **Login Route + Rate Limiting**, nhưng Architecture Review phát hiện xung đột: route/Controller là business logic, Module System (Phase 3) chưa bắt đầu, chưa có nơi hợp lệ đặt route theo đúng ranh giới Core/Business đã áp dụng xuyên suốt dự án
  - [x] Owner Decision (Phương án A): **CHỈ** mở rộng `AuthenticationService`, KHÔNG Route/Controller/Module (để dành khi Module System thật bắt đầu) — giải quyết đúng Technical Debt #24, không đụng #25
  - [x] Owner Decision chi tiết: rate-limit key `login:{lowercase_email}`; hành vi khi bị chặn trả `false` (không phân biệt được với sai password, không Exception mới, không đổi public API); thứ tự bắt buộc `tooManyAttempts()` → `password_verify()` → FAIL: `hit()` / SUCCESS: `clear()` → `status` check → `Auth::login()` — **`hit()`/`clear()` gắn với kết quả `password_verify()`, không gắn với kết quả cuối `attempt()`**
  - [x] Final Design: thêm `RateLimiter`+`Config` vào constructor `AuthenticationService` (6 dependency), không đăng ký Container mới (vẫn auto-wire được), không sửa `Auth.php`/`Authorization.php`/`RateLimiter.php`/`Application.php`/`Router.php`/`Container.php`/Middleware/Database layer/Migration
  - [x] Implementation: `attempt()` thêm guard `tooManyAttempts()` ngay sau tenant-check (chặn sớm, không tốn query DB khi đã bị rate-limit); `hit()` chỉ gọi khi verify thất bại; `clear()` chỉ gọi khi verify thành công (kể cả khi sau đó `status !== 'active'` khiến `attempt()` vẫn trả `false` — đúng thiết kế, không phải bug)
  - [x] `tests/Fixtures/config/auth.php` — bổ sung `login_throttle` (`max_attempts: 3, decay_seconds: 60`), thuần additive, không ảnh hưởng test khác dùng chung fixture
  - [x] Final Verification (Phase 4, không code) phát hiện 1 khoảng trống test thật: hành vi "`clear()` xảy ra dù tài khoản inactive" (đúng thiết kế đã duyệt nhưng phản trực giác) chưa có test khoá lại — kết luận **APPROVED WITH MINOR CHANGES**
  - [x] `tests/Core/AuthenticationServiceTest.php` (+5 test so với CMS-031, tổng 15 test): 4 test rate-limit cơ bản (chặn khi vượt `max_attempts`, vẫn thành công khi dưới ngưỡng, key scoped theo từng email, `clear()` khi verify đúng) + `testRateLimitClearsEvenWhenInactiveAccountUsesCorrectPassword` (khoá tường minh hành vi đã duyệt ở Phase 4, tránh refactor sau này vô tình đổi thứ tự `clear()`/status check)
  - [x] Không sửa `Auth.php`/`Authorization.php`/`RateLimiter.php`/`Application.php`/`Router.php`/`Container.php`/mọi Middleware/Database layer/Migration — đúng phạm vi đã khoá
  - [x] Technical Debt #24 (rate limiting login chưa có) — **RESOLVED**. Technical Debt #25 (chưa có route/Controller login) và #26 (1 session = 1 site) — giữ nguyên, chưa xử lý
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `AuthenticationServiceTest`: 15 tests/28 assertions; toàn bộ suite: 396 tests, 692 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-033 COMPLETED — tag `v0.0.33`**
- [x] **CMS-034** — Module System Bootstrap: Auth Module (`modules/Auth/`)
  - [x] Roadmap Review sau CMS-033 liệt kê Candidate A (Module System Bootstrap), B (Site Status Policy), C (ThemeManager Integration), D (Content Schema Migration), E (Domain Normalization + Super Admin Bypass), F (Deferred Observability), G (AuthorizationMiddleware tham số hoá thật), H/I (Event Dispatcher/Queue) — Owner chọn **Candidate A**, lý do: candidate duy nhất giải quyết đúng root cause vừa phát hiện ở CMS-033 (route `/login` bị chặn vì thiếu Module System), mở khoá Technical Debt #25 và tiền đề cho toàn bộ Phase 3
  - [x] Architecture Analysis (đọc trực tiếp `ModuleManager.php`/`Application.php`/`Router.php`/`ControllerResolver.php`/`composer.json`): xác nhận `modules/` hoàn toàn rỗng (chỉ `.gitkeep`) — đây là Module thật đầu tiên; PSR-4 `"Modules\\": "modules/"` đã cấu hình từ CMS-001, chưa từng dùng; `ModuleManager::boot()` require `routes.php` qua closure cô lập scope (chỉ `$router` khả kiến); `ControllerResolver` resolve Controller qua `Container::get()` (auto-wire, không cần đăng ký tường minh); `Application::boot()` tự động discover+enable mọi module trong `modules/`, không cần sửa `Application.php`
  - [x] Phát hiện xung đột kiến trúc, dừng lại nêu rõ trước khi Design: (1) CSRF cho `/login` — chưa có endpoint nào phát hành token trước khi submit; (2) GET `/login` — chưa có theme/View Admin Panel nào tồn tại
  - [x] Owner Decision: CSRF **Phương án A** (không CSRF cho `/login`, có chủ đích, không phải thiếu sót — "login CSRF" là lớp rủi ro khác Synchronizer Token không nhắm tới, không tạo `/csrf-token` endpoint); **không** GET `/login` (JSON API POST-only, để dành UI thật cho Admin Panel Module sau); response thành công trả `{id, email, roles, permissions}` (không password/hash/tenant internals); dùng `Validator` (CMS-014) có sẵn, không tự viết validation riêng; method Controller đặt tên `handle(Request): Response`
  - [x] `modules/Auth/module.json` (`key: "auth"`), `modules/Auth/routes.php` (1 dòng, `$router->post('/login', [LoginController::class, 'handle'])`), `modules/Auth/LoginController.php` (namespace `Modules\Auth`, inject `AuthenticationService`/`Auth`/`Authorization`/`Validator` — toàn bộ auto-wire, không đăng ký Container) — validate `email: required|email`/`password: required|string` → 422 kèm `errors` chi tiết; `attempt()` fail → 401 message thống nhất (không phân biệt lý do); success → 200 `data.{id,email,roles,permissions}` đọc qua `Auth::id()/user()`+`Authorization::roles()/permissions()`
  - [x] `tests/Core/ModuleAuthIntegrationTest.php` (mới, 5 test) — dùng `ModuleManager` trỏ thẳng `modules/` **thật** của dự án (không fixture), `Router::dispatch()` thật, không qua `Application::bootstrap()` (tránh phụ thuộc config production/ghi log thật vào `storage/`): login thành công đúng response shape, sai password → 401 message thống nhất, thiếu field → 422 với `errors`, email sai định dạng → 422, không leak password hash trong response
  - [x] Không sửa `Application.php`/`ModuleManager.php`/`Router.php`/`AuthenticationService.php`/`Auth.php`/`Authorization.php`/`Container.php`/Middleware/Migration/Database/Composer/PHPUnit config — đúng phạm vi đã khoá; `composer dump-autoload` xác nhận PASS (PSR-4 `Modules\` lần đầu dùng thật hoạt động đúng)
  - [x] Technical Debt #25 (chưa có route/Controller login) — **RESOLVED**. Technical Debt #26 (1 session = 1 site) — giữ nguyên, chưa xử lý. Ghi nhận Technical Debt mới: không CSRF cho `/login` (có chủ đích, Owner Decision)
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `ModuleAuthIntegrationTest`: 5 tests/16 assertions; toàn bộ suite: 401 tests, 708 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-034 COMPLETED — tag `v0.0.34`**
- [x] **CMS-035** — Auth Module: Logout Endpoint (`modules/Auth/LogoutController.php`)
  - [x] Roadmap Review sau CMS-034 liệt kê Candidate A (Logout Endpoint), B (Site Status Policy), C (ThemeManager Integration), D (Content Schema Migration), E (Domain Normalization + Super Admin Bypass), F (Deferred Observability), G (`AuthorizationMiddleware` tham số hoá thật), H (CSRF cho login), I (Event Dispatcher/Queue) — không có candidate nào "unlock" lớn như CMS-034, Owner chọn **Candidate A — Logout Endpoint**, lý do: rủi ro thấp nhất tuyệt đối, hoàn thiện cặp login/logout tối thiểu, dùng `Auth::logout()` đã ổn định từ CMS-021
  - [x] Architecture Analysis (đọc trực tiếp `Auth.php`/`Session.php`/`LoginController.php`/`AuthMiddleware.php`): xác nhận `Auth::logout()` chỉ gọi `Session::destroy()` (huỷ toàn bộ `$_SESSION`, không riêng `auth.*`), không throw ở bất kỳ trạng thái nào; xác nhận `Session::destroy()` xoá luôn mọi `rate_limit.*` như hệ quả tự nhiên (không cần code thêm để "dọn" rate limiter); xác nhận `AuthMiddleware` đã tồn tại sẵn (không cần middleware mới) nhưng việc dùng nó ràng buộc trực tiếp với quyết định idempotent-hay-không
  - [x] Owner Decision (3 Open Question): logout **idempotent** — luôn 200 dù đã/chưa đăng nhập, **không** dùng `AuthMiddleware` (khớp bản chất "không thể thất bại" của `Auth::logout()` từ CMS-021); response `errors: []` (không `null`, đúng convention `LoginController`), message tiếng Việt không dấu; **không CSRF** cho `/logout` (nhất quán quyết định login CMS-034, tránh tạo endpoint không dùng được do chưa có token-issuing flow); **không** thêm test đọc `rate_limit.*` internals của Session (tránh coupling `LogoutController`↔`Session` internals — chỉ verify qua `Auth::check()/user()` public API)
  - [x] `modules/Auth/LogoutController.php` (mới, Controller riêng — không thêm method vào `LoginController`, giữ đúng 1 action/1 class), `modules/Auth/routes.php` (+1 dòng `POST /logout`)
  - [x] `tests/Core/ModuleAuthIntegrationTest.php` (+2 test): `testLogoutClearsAuthenticatedUser`, `testLogoutSucceedsWhenNotLoggedIn`
  - [x] **Test Failure Analysis thật** (phát hiện qua PHPUnit thật, không suy đoán): lần chạy đầu — `SessionException: Phai goi Session::start() truoc khi doc/ghi du lieu session.` ở cả 2 test mới (2 Errors). **Root Cause: lỗi test assumption, không phải implementation** — `Session::destroy()` kết thúc session (`isStarted() === false`), test gọi `Auth::check()/user()` ngay sau dispatch `/logout` (trong cùng session đã bị huỷ) thay vì mô phỏng "request kế tiếp" tự `start()` lại — **chính xác cùng loại lỗi đã xảy ra và đã sửa ở CMS-021** (`AuthTest::testLogoutClearsAuthenticationState`)
  - [x] Test Fix: thêm `$this->session->start();` ngay sau dispatch `/logout`, trước khi gọi `Auth::check()/user()`, đúng pattern `AuthTest.php` (CMS-021) — **không sửa** `LogoutController.php`/`Auth.php`/`Session.php`/bất kỳ Core class nào, không đổi assertion nào khác
  - [x] Không sửa `Auth.php`/`AuthenticationService.php`/`Authorization.php`/`Session.php`/`Router.php`/`Application.php`/`ModuleManager.php`/`Container.php`/mọi Middleware/`LoginController.php`/Migration/Composer/PHPUnit config — đúng phạm vi đã khoá
  - [x] Không tạo Technical Debt mới, không đánh dấu resolved Technical Debt nào ngoài phạm vi CMS-035
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `ModuleAuthIntegrationTest`: 7 tests/26 assertions; toàn bộ suite: 403 tests, 718 assertions, 0 Errors/Failures/Warnings/Risky, 4 Skipped đúng thiết kế (Redis) → **CMS-035 COMPLETED — tag `v0.0.35`**
- [x] **CMS-036** — Site Status Policy (`core/Middleware/TenantResolverMiddleware.php`, mở rộng)
  - [x] Roadmap Review sau CMS-035 liệt kê 12 candidate (A. Site Status Policy, B. ThemeManager Integration, C. Content Schema Migration, D. Domain Normalization + Super Admin Bypass, E. Deferred Observability, F. `AuthorizationMiddleware` tham số hoá thật, G. CSRF Token Flow, H. `roles` UNIQUE fix, I. Module/Plugin theo Site, J. User Registration, K. Multi-site Session, L. Event/Queue) — không candidate nào "unlock" lớn như CMS-034; Owner chọn **Candidate A — Site Status Policy (Technical Debt #21)**, lý do: security impact cao nhất trong nhóm rủi ro thấp (site `suspended`/`maintenance` đang thật sự hoạt động bình thường), ưu tiên vấn đề kiến trúc đang tồn tại hơn tính năng mới (Candidate J — User Registration — dù giá trị cao hơn nhưng còn nhiều Open Question nghiệp vụ chưa trả lời)
  - [x] Architecture Analysis (đọc trực tiếp `TenantResolverMiddleware.php`/`TenantManager.php`/`Application.php`/`Router.php`/migration `sites`/test liên quan): xác nhận cột `status` đã có sẵn trong `$site` (từ `SELECT sites.*`), không cần query mới; xác nhận vị trí mở rộng chính xác (giữa nhánh 404 và `TenantManager::setCurrent()`); xác nhận docblock cũ ghi rõ "site status ... thuộc phạm vi CMS riêng sau này" — CMS-036 chính là CMS đó
  - [x] Owner Decision (4 Open Question): `maintenance` → HTTP 503 + `"Site is under maintenance."`; `suspended` → HTTP 403 + `"Site has been suspended."`; điều kiện chặn **fail-closed tuyệt đối** `status !== 'active'` (không `in_array()`, bao gồm NULL/rỗng/giá trị lạ/tương lai); phạm vi test **chỉ** `TenantResolverMiddlewareTest.php` (không mở rộng `ApplicationTest.php`/`TenantManagerTest.php`)
  - [x] Final Design: 1 khối `if ($site['status'] !== 'active')` + `private statusBlockedResponse()` (match `maintenance`/`suspended`/`default`) — phát hiện 1 điểm Owner Decision chưa chỉ định tường minh (mã/message cho giá trị status không khớp `maintenance`/`suspended`), dừng lại nêu rõ, Owner xác nhận: **403 + `"Site is not available."`** cho mọi giá trị còn lại
  - [x] Sửa `core/Middleware/TenantResolverMiddleware.php` — thêm đúng 1 khối kiểm tra (dùng `$site['status']` đã có sẵn, không thêm query SQL) + 1 `private method` nội bộ, không `TenantManager::setCurrent()`/không `$next()` khi bị chặn, giữ nguyên envelope `{success,data,message,errors}`. Cập nhật docblock đầu file
  - [x] `tests/Core/Middleware/TenantResolverMiddlewareTest.php` — `seedSite()` thêm tham số `string $status = 'active'` (default an toàn, 4 test cũ không đổi assertion), thêm 4 test: `testAllowsActiveSiteExplicitly`, `testBlocksMaintenanceSiteWith503`, `testBlocksSuspendedSiteWith403`, `testBlocksUnknownStatusValue`
  - [x] Không sửa `TenantManager.php`/`Database.php`/`Application.php`/`Router.php`/`Container.php`/`AuthenticationService.php`/`Auth.php`/`Authorization.php`/`modules/Auth/*`/migration/`composer.json`/`phpunit.xml`/`ApplicationTest.php`/`TenantManagerTest.php` — đúng phạm vi đã khoá. Không tạo middleware/exception/helper/config/class/trait/interface mới
  - [x] Technical Debt #21 (site status chưa check) — **RESOLVED**. Không thay đổi trạng thái Technical Debt nào khác
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `TenantResolverMiddlewareTest`: 8 tests/24 assertions; toàn bộ suite: 407 tests, 733 assertions, 0 Errors/Failures/Warnings/Risky, 4 Skipped đúng thiết kế (Redis) → **CMS-036 COMPLETED — tag `v0.0.36`**
- [x] **CMS-037** — User Management Module (`modules/User/`) — bắt đầu chuyển hướng sang Product Development (không còn ưu tiên mở rộng Framework nếu không có Technical Debt thật)
  - [x] Roadmap Review sau CMS-036 ban đầu đề xuất "User Registration" (self-registration) — Owner Decision đổi hướng: **User Management Module cho Admin** (CRUD, không self-registration), đồng thời chuyển toàn bộ roadmap từ CMS-037 sang Product Development (CMS-037→CMS-049: User/Role/Permission/Dashboard/Media/Page/Menu/Category/Post/SEO/Settings/Theme Runtime/Plugin Runtime)
  - [x] Architecture Analysis (đọc trực tiếp `Auth.php`/`AuthenticationService.php`/`Authorization.php`/`Session.php`/migration `users`/`roles`/`user_site_roles`): xác nhận `users` không có `tenant_id` (quan hệ user↔site chỉ qua `user_site_roles`), chưa có API nào từng ghi vào `users`/`user_site_roles` (chỉ SELECT qua `AuthenticationService`), `roles`/`permissions` chưa có management API — nêu đầy đủ Open Question nghiệp vụ (role mặc định, tenant scoping, permission model, cấu trúc Module/Controller, Delete, password policy)
  - [x] Owner Decision (7 điểm): giữ nguyên `users.status` (`active/locked/pending`, không thêm `suspended`); scoped tuyệt đối theo `TenantManager::id()`, không query nào thấy user tenant khác; Authorization **permission-based** qua `can()` (không `hasRole('admin')`); Module **`User` riêng biệt** `Auth` (Auth chỉ Authentication); giữ Controller Contract (1 Controller = 1 `handle()`); **Delete User OUT OF SCOPE** (chỉ List/Create/Edit/Lock/Unlock/Assign Role); password `required|string|min:8`
  - [x] Final Design qua 2 vòng Owner Review: **Vòng 1** — Create User phải là 1 transaction hoàn chỉnh (không orphan user, không tách Create/AssignRole), cần giải pháp đảm bảo permission tồn tại (không phụ thuộc Owner tự seed SQL). **Vòng 2** — loại bỏ hoàn toàn phương án migration seed permission (CMS-037 chỉ giả định permission đã tồn tại, bootstrap để CMS-038/cơ chế riêng); bỏ `user.unlock` (gộp chung `user.lock` với Lock); role validation phải nằm **trong** transaction (không SELECT trước) — dùng `\InvalidArgumentException` built-in (không `RuntimeException`, tránh trùng nhánh kế thừa với `Core\Database\QueryException`)
  - [x] `modules/User/{module.json,routes.php}` + 6 Controller (`ListUsersController`/`CreateUserController`/`EditUserController`/`LockUserController`/`UnlockUserController`/`AssignRoleController`) — mỗi Controller đúng 1 `handle()`, gọi `Database` trực tiếp (không Repository/Service), permission qua `Authorization::can()` (`user.view/create/update/lock/assign_role` — không `user.unlock`), scoped `TenantManager::id()`, cross-tenant → 404 (không 403)
  - [x] `CreateUserController`: `Database::transaction()` — SELECT role hợp lệ **bên trong** Closure → `throw \InvalidArgumentException('invalid-role')` nếu sai → INSERT `users` (`password_hash($password, PASSWORD_DEFAULT)`) → INSERT `user_site_roles` → catch `\InvalidArgumentException` (422 "Role khong hop le.") và `Core\Database\QueryException` (422 "Email da ton tai.") tách biệt hoàn toàn (đã xác nhận qua đọc `QueryException.php`/`DatabaseException.php`: 2 nhánh kế thừa khác nhau, không rủi ro bắt nhầm)
  - [x] `tests/Core/ModuleUserIntegrationTest.php` (mới, 7 test) — `ModuleManager` trỏ `modules/` thật (đúng pattern CMS-034), permission seed trực tiếp trong test (không phụ thuộc migration): list scoped đúng tenant, create thành công (user+user_site_roles cùng transaction), rollback khi role invalid, rollback khi email trùng, lock/unlock dùng chung `user.lock`, 403 thiếu quyền, 404 cross-tenant
  - [x] Không sửa `core/*`/`modules/Auth/*`/migration nào/`composer.json`/`phpunit.xml` — đúng phạm vi đã khoá qua 2 vòng Owner Review
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `ModuleUserIntegrationTest`: 7 tests/21 assertions; toàn bộ suite: 414 tests, 754 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **CMS-037 COMPLETED**
- [x] **CMS-038** — CMS Foundation Completion (`bin/bootstrap.php` + `modules/Role/` + `modules/Dashboard/`)
  - [x] Owner chủ động tạm dừng Product Roadmap CMS-040+ (Content/Media/Menu...), yêu cầu hoàn thiện Foundation trước: Permission Bootstrap + Admin Bootstrap (Phase 1) → Role Management (Phase 2) → Dashboard Foundation (Phase 3) → Settings Foundation (Phase 4, chỉ Design)
  - [x] Architecture Analysis xác nhận qua đọc lại migration `roles` (CMS-028): **không có cột `is_system`** (khác đề xuất gốc `database-design.md`) — ý nghĩa `roles.tenant_id NULL` xác nhận qua 2 nguồn khớp nhau (`database-design.md` gốc + cách `CreateUserController` CMS-037 đã dùng): `NULL` = System Role dùng chung mọi tenant, có giá trị = Tenant Role riêng 1 site
  - [x] Owner Decision (5 quyết định): Bootstrap = CLI Command trong `bin/` (không HTTP route, không đưa logic vào `Auth`/`User` Module); Bootstrap chạy **ONE TIME ONLY** (kiểm tra `users` rỗng, từ chối nếu đã init); System Role (`tenant_id NULL`) — View allowed, Update/Delete/Permission modification forbidden qua Module; 11 permission bootstrap cố định (`user.*` x5, `role.*` x5, `dashboard.view`), không tạo permission Content/Media chưa tồn tại; Dashboard chỉ `GET /dashboard` (`user_count`/`role_count`), chưa UI
  - [x] `bin/bootstrap.php` — CLI nhận 5 tham số (site_name/domain/admin_name/admin_email/admin_password), kiểm tra `SELECT COUNT(*) FROM users` trước khi làm bất kỳ điều gì, toàn bộ 7 bước (Site→SiteDomain→User→SystemRole→11 Permission→11 RolePermission→UserSiteRole) trong 1 `Database::transaction()` — không migration mới, không đưa logic vào `modules/`
  - [x] `modules/Role/` (6 Controller: `ListRoles`/`CreateRole`/`EditRole`/`DeleteRole`/`AssignPermission`/`ListPermissions`) — System Role → 403 khi Update/Delete/AssignPermission (không phải 404 — "View allowed" nên không ẩn sự tồn tại, khác nguyên tắc cross-tenant); Tenant Role cross-tenant → 404 (giữ nguyên nguyên tắc `User` Module); `CreateRole` luôn gán `tenant_id` hiện tại, 422 nếu trùng tên; `DeleteRole` chặn nếu role đang gán cho user
  - [x] `modules/Dashboard/` (`DashboardController`) — `GET /dashboard`, `can('dashboard.view')`, `user_count`/`role_count` scoped `TenantManager::id()`
  - [x] **Fix sau PHPUnit thật** (2 lỗi phát hiện qua test thật, không suy đoán): (1) `ListPermissionsController` SELECT `description` nhưng test tự báo lỗi "no such column" — **điều tra lại phát hiện chẩn đoán ban đầu sai**: migration thật (`2026_08_01_000005`) CÓ cột `description`, lỗi thật nằm ở `ModuleRoleIntegrationTest.php::migrate()` thiếu cột này trong `CREATE TABLE` tự viết — sửa TEST (thêm cột), giữ nguyên Controller, không mất khả năng trả `description` thật; (2) `testDeleteRoleInUseReturns409` FAIL (200 thay vì 409) vì SQLite trong `Database::connect()` không bật `PRAGMA foreign_keys = ON` (giới hạn đã ghi nhận từ CMS-030) → sửa `DeleteRoleController` dùng `SELECT COUNT(*) FROM user_site_roles WHERE role_id = ?` tường minh trước `DELETE`, không phụ thuộc FK exception
  - [x] `tests/Core/ModuleRoleIntegrationTest.php` (12 test), `tests/Core/ModuleDashboardIntegrationTest.php` (2 test) — `ModuleManager` trỏ `modules/` thật, không fixture
  - [x] Settings Foundation (Phase 4) — **chỉ Design, không Implementation**: cần 1 migration mới `settings(id, tenant_id, key, value JSON, group)` khi triển khai thật (duy nhất phần cần migration trong toàn bộ CMS Foundation Completion)
  - [x] Không sửa `core/*`/`modules/Auth/*`/`modules/User/*`/migration nào/`composer.json`/`phpunit.xml`
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật — `ModuleRoleIntegrationTest`: 12 tests/17 assertions; `ModuleDashboardIntegrationTest`: 2 tests/4 assertions; toàn bộ suite: 428 tests, 775 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **CMS Foundation Completion (CMS-038) COMPLETED**
- [x] **CMS-040** — Page Management (`database/migrations/2026_08_02_000001_create_pages_table.php` + `modules/Page/`)
  - [x] Architecture Analysis (đọc `database-design.md` mục 3.1-3.2/5.3): xác nhận `pages` là entity Content duy nhất KHÔNG phụ thuộc `media` (khác `posts.featured_image_id`/`seo_meta.og_image_id`) — chọn `pages` triển khai đầu tiên, tách biệt hoàn toàn khỏi cụm `posts/categories/tags/post_tags/comments` (phụ thuộc lẫn nhau, chưa triển khai)
  - [x] Owner Decision (7 điểm qua Final Design): entity đầu tiên = `pages`; `content` lưu `TEXT` (không cột `JSON`, Application layer tự `json_encode/json_decode`); permission `page.view/create/update/delete/publish` (không `page.set_homepage` — Set Homepage dùng chung `page.update`); `page.publish` tách riêng; **không** `PageService`/Repository (Controller gọi `Database` trực tiếp); soft-delete chỉ `UPDATE deleted_at`, không restore/trash trong CMS-040
  - [x] **PHASE 0 riêng trước Implementation — Permission Bootstrap Gap**: phát hiện `page.*` không có cách nào được tạo (`Role` Module CMS-038 chỉ gán permission đã tồn tại, không tạo mới; `bin/bootstrap.php` chỉ có 11 permission cố định) — Owner Decision: mở rộng `bin/bootstrap.php` từ 11 → 16 permission (thêm `page.*`), xác lập **tiền lệ chính thức**: Module tương lai cần permission mới đều mở rộng mảng này, không tạo cơ chế seed riêng
  - [x] Migration `pages` — driver-aware PK (đúng convention CMS-028), `status VARCHAR(20)` không `ENUM`, 3 index (`uq_pages_tenant_slug`, `idx_pages_tenant_status`, `idx_pages_parent_id`), FK `tenant_id→sites CASCADE`/`parent_id→pages SET NULL`/`created_by→users RESTRICT`
  - [x] `modules/Page/` (6 Controller, đúng Controller Contract): `ListPagesController` (scoped tenant, loại `deleted_at`, không trả `content`), `CreatePageController` (1 INSERT, validate `parent_id` thuộc tenant, 422 nếu trùng `slug`), `EditPageController`/`DeletePageController` (404 cho cross-tenant VÀ page đã xoá mềm), `PublishPageController` (`UPDATE ... published_at = COALESCE(published_at, CURRENT_TIMESTAMP)` — chỉ set lần đầu, áp dụng pattern `PostService::publish()` từ `database-design.md` 6.3 sang `pages`), `SetHomepageController` (**duy nhất dùng `Database::transaction()`** — 2 UPDATE đúng `database-design.md` 6.1, verify tồn tại TRƯỚC transaction vì không có rủi ro orphan-row như `CreateUserController`)
  - [x] Tự phát hiện & tự sửa 1 lỗi trong lúc viết `CreatePageController` (dùng nhầm biến cho `created_by`, phải là `Auth::id()` không phải `Authorization`) — sửa trước khi hoàn thiện file, không phát sinh từ PHPUnit
  - [x] `tests/Core/ModulePageIntegrationTest.php` (17 test) — `ModuleManager` trỏ `modules/` thật, permission+`auth.user_id` seed qua Session (cần cho `created_by NOT NULL`)
  - [x] **Fix sau PHPUnit thật**: `tests/Core/RealMigrationsTest.php::EXPECTED_ORDER` (hằng số dùng chung 3 test) thiếu `2026_08_02_000001_create_pages_table` — gây 3 failure toàn suite (không phải lỗi migration/Page Module, thuần test expectation chưa cập nhật theo migration mới) — sửa đúng 1 hằng số, khắc phục cả 3 test cùng lúc
  - [x] Không sửa `core/*`/`modules/Auth/*`/`modules/User/*`/`modules/Role/*`/`composer.json`/`phpunit.xml`/migration cũ
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật — `ModulePageIntegrationTest`: 17 tests/27 assertions; toàn bộ suite: 445 tests, 807 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **CMS-040 COMPLETED — tag `v0.0.40`**
- [x] **CMS-044** — Theme/Layout Runtime & Public Website Rendering (`modules/Public/` + `themes/default/`)
  - [x] Roadmap Review sau CMS-040 — Owner xác nhận thứ tự mới: **CMS-044 Theme/Layout Runtime → CMS-045 Admin UI Foundation → CMS-041 Media Library → CMS-042 Menu Management → CMS-043 SEO Management → CMS-046 Public Website Polish** (thay thế thứ tự cũ đã lỗi thời)
  - [x] Architecture Analysis (đọc trực tiếp `core/View.php`/`core/ThemeManager.php`/`core/Route.php`/`core/Router.php`, glob `themes/`): xác nhận `View` đã đủ khả năng render+layout inheritance từ CMS-005, không thiếu thành phần nào; phát hiện `themes/` thật đang **rỗng hoàn toàn** (chỉ `.gitkeep`) và quy ước đường dẫn thật của `View::resolvePath()` có thư mục `views/` bắt buộc ở giữa (`themes/{theme}/views/pages/default.php`, khác đề xuất gốc phẳng của Owner); phát hiện rủi ro `Router::match()` (duyệt tuần tự, trả về khớp đầu tiên) khiến route wildcard `GET /{slug}` có thể "nuốt" route Admin GET 1-segment nếu đăng ký trước
  - [x] Owner Decision (5 điểm qua Owner Approval): (1) chọn `GET /{slug}` (không `/pages/{slug}`), bắt buộc `modules/Public/module.json.dependencies` để load sau cùng, không sửa `Router`, không xử lý collision handling; (2) chấp nhận rủi ro reserved slug (`login/users/roles/pages/dashboard`), không blacklist, không sửa `CreatePageController`; (3) template không tồn tại → Controller tự fallback `pages.default`, không 500; (4) test dùng fixture theme riêng, không phụ thuộc `themes/default/` thật; (5) chỉ render `title` + `content` JSON decode dạng text/basic, không block builder/component renderer
  - [x] `modules/Public/{module.json,routes.php,HomeController.php,PublicPageController.php}` — Module thứ 5, `dependencies: [auth,user,role,dashboard,page]` (buộc load cuối cùng qua topological sort có sẵn của `ModuleManager`), mỗi Controller đúng 1 `handle()`, không `Authorization::can()`, `Response::html()` không JSON. `HomeController`/`PublicPageController` dùng logic render trùng lặp có chủ đích (chấp nhận, không tạo helper/abstraction mới ngoài phạm vi đã duyệt)
  - [x] Tự phát hiện & tự sửa 1 sai sót trong lúc code: ban đầu tạo thêm `modules/Public/PageRenderer.php` (static helper) — không nằm trong danh sách file Owner đã duyệt, đã xoá và inline lại logic vào từng Controller trước khi báo cáo hoàn thành
  - [x] `themes/default/{theme.json,views/layouts/main.php,views/pages/default.php}` — Default theme structure đầu tiên của dự án (đúng quy ước `views/` bắt buộc phát hiện ở Architecture Analysis)
  - [x] `tests/Core/PublicPageRenderingTest.php` (8 test) + `tests/Fixtures/themes/test-theme/` (fixture riêng, không dùng `themes/default/` thật) — published/draft/deleted/cross-tenant/custom-template/template-fallback/homepage-có/homepage-không
  - [x] Phát hiện khi viết test (chưa nêu ở Architecture Analysis): `ModuleManager::resolveLoadOrder()` coi `dependencies` là bắt buộc phải nằm trong `$enabledKeys` (không chỉ gợi ý thứ tự) — production không ảnh hưởng vì `Application::boot()` luôn enable toàn bộ module `discover()` được; test phải enable đủ cả 6 module (chỉ đăng ký route, không khởi tạo Controller nên chi phí thấp)
  - [x] Không sửa `core/*`, `modules/Auth|User|Role|Page/*`, `composer.json`, `phpunit.xml`, `database/migrations/*` — đúng phạm vi đã khoá
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `PublicPageRenderingTest`: 8 tests/14 assertions; toàn bộ suite: 453 tests, 821 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **CMS-044 COMPLETED — tag `v0.0.44`**
- [x] **CMS-045** — Admin UI Foundation (`modules/Admin/` + `themes/default/views/admin/`)
  - [x] Architecture Analysis (đọc trực tiếp `core/View.php`/`core/Router.php`/`core/Auth.php`/`core/Csrf.php`/`core/Middleware/{Csrf,Auth,Authorization}Middleware.php`, `modules/Auth|User|Role|Dashboard|Page/*`, `themes/*`): xác nhận `POST /login` JSON hiện có (CMS-034) không CSRF/không redirect, không tái dùng trực tiếp được cho HTML; `Csrf`/`CsrfMiddleware` đã tồn tại đầy đủ nhưng **chưa từng gắn vào route thật nào**; phát hiện `View::resolvePath()` fallback `activeTheme → defaultTheme` cho phép đặt template Admin dưới `themes/default/views/admin/*` mà không cần theme riêng/không sửa Core; route `/admin/*` (≥2 segment) miễn nhiễm hoàn toàn với wildcard `GET /{slug}` (CMS-044), không cần khai báo `dependencies` chéo với `modules/Public/`
  - [x] Owner Decision (13 điểm qua Owner Approval): phạm vi CHỈ `GET/POST /admin/login`, `POST /admin/logout`, `GET /admin/dashboard` (không User/Role/Page/Settings/Menu/Media UI); login fail render lại form tại chỗ (không PRG, không Session::flash, không Form Helper); không tạo `GET /admin` (tránh hoàn toàn rủi ro collision với `/{slug}`); logout bắt buộc POST + CSRF; theme dùng `themes/default/views/admin/` (không theme riêng); tái sử dụng `AuthenticationService`/`Auth`/`Session` nguyên trạng; không dùng `AuthMiddleware` (trả JSON 401 sai ngữ nghĩa HTML) — Controller tự `Auth::check()` + `Response::redirect()`; Dashboard copy logic SQL từ `Modules\Dashboard\DashboardController`, không gọi lại Controller đó
  - [x] `modules/Admin/{module.json,routes.php,ShowLoginController.php,LoginController.php,LogoutController.php,DashboardController.php}` — Module thứ 6, `dependencies: [auth]`, mỗi Controller đúng 1 `handle()`. `POST /admin/login`+`POST /admin/logout` bọc `Router::group(['middleware' => [CsrfMiddleware::class]], ...)` — **lần đầu CSRF được gắn vào route thật trong toàn bộ dự án**
  - [x] `themes/default/views/admin/{layouts/main.php,pages/login.php,pages/dashboard.php}` — dùng đúng cơ chế fallback `activeTheme → defaultTheme` có sẵn của `View` (CMS-005), không tạo `themes/admin/` riêng
  - [x] `tests/Core/AdminUiFoundationTest.php` (7 test, đúng danh sách Owner yêu cầu) — `ModuleManager` trỏ `modules/` thật, `View` dùng `themes/default/` thật (không fixture — khác CMS-044, vì Admin theme là nội dung sản phẩm thật, không phải theme tuỳ biến theo tenant)
  - [x] **Fix sau PHPUnit thật**: `testLogoutClearsSessionAndDashboardRedirectsAgain` FAIL (`Core\SessionException`) vì đọc `Session::get()` ngay sau `Auth::logout()` (→ `Session::destroy()` kết thúc phiên) trong cùng "request" mô phỏng — sửa: thêm `$this->session->start()` trước khi đọc lại trạng thái, đúng pattern đã dùng ở `ModuleAuthIntegrationTest::testLogoutClearsAuthenticatedUser` (CMS-034) — chỉ sửa test, không đụng `core/*`/`modules/*`
  - [x] Không sửa `core/*`, `modules/Auth|User|Role|Dashboard|Page|Public/*`, `composer.json`, `phpunit.xml`, `database/migrations/*` — đúng phạm vi đã khoá
  - [x] **Technical Debt ghi nhận** (Owner Decision, không xử lý trong CMS-045): (1) login chưa PRG (refresh sau lỗi sẽ resubmit form); (2) `ShowLoginController` không redirect nếu user đã đăng nhập truy cập lại `/admin/login`; (3) `DashboardController` (Admin) trùng lặp logic SQL với `Modules\Dashboard\DashboardController` (JSON) — chấp nhận, đúng tiền lệ `HomeController`/`PublicPageController` (CMS-044)
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `AdminUiFoundationTest`: 7 tests/19 assertions; toàn bộ suite: 460 tests, 840 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **CMS-045 COMPLETED — tag `v0.0.45`**
- [x] **CMS-046** — Admin User Management UI (`modules/Admin/User*Controller.php` + `themes/default/views/admin/pages/users/`)
  - [x] Architecture Analysis (đọc trực tiếp `modules/User/*`, `modules/Admin/*`, `themes/default/views/admin/*`, `core/View.php`/`core/Router.php`/`core/Authorization.php`/`core/Session.php`, `tests/Core/ModuleUserIntegrationTest.php`): xác nhận `core/Http/Request.php` không hỗ trợ Method Spoofing → Edit HTML bắt buộc dùng `POST` (không `PATCH`); xác nhận route `GET /admin/users/create` (3-segment) và việc **không** thêm `GET /admin/users/{id}` (cũng 3-segment) né hoàn toàn rủi ro collision kiểu CMS-044; nêu rõ Open Question quan trọng nhất: copy nguyên transaction logic phức tạp của `CreateUserController` hay chấp nhận lệch tiền lệ tạo abstraction dùng chung
  - [x] Owner Decision (10 điểm qua Owner Approval): đặt tên Controller tiền tố `User` trong `modules/Admin/` (tránh nhầm namespace `modules/User`); authorization failure trả `Response::html('403 Forbidden', 403)` (không Forbidden View riêng, không `AuthorizationMiddleware`); **Phương án A** cho CreateUser — cho phép copy nguyên transaction logic, không tạo Service Layer, ghi nhận Technical Debt; giữ nguyên pattern form CMS-045 (render-lại-tại-chỗ, không PRG/Flash/Form Helper); route table đúng 8 route (không `GET /admin/users/{id}`, không Delete, không PATCH); mở rộng `CsrfMiddleware` group hiện có; view thêm `pages/users/` subfolder, dùng chung `admin.layouts.main`
  - [x] `modules/Admin/{UserListController,UserShowCreateController,UserCreateController,UserShowEditController,UserUpdateController,UserLockController,UserUnlockController,UserAssignRoleController}.php` — 8 Controller mới, mỗi Controller đúng 1 `handle()`, `Database` trực tiếp (không Service/Repository). `UserCreateController` copy nguyên transaction (role validate trong transaction, `password_hash()`, bắt riêng `\InvalidArgumentException`/`QueryException`) từ `Modules\User\CreateUserController` — **không sửa** file gốc đó
  - [x] `modules/Admin/routes.php` (mở rộng) — 8 route mới (`GET/POST /admin/users`, `GET /admin/users/create`, `GET/POST /admin/users/{id}` dạng edit, `/lock`, `/unlock`, `/role`), route ghi bọc `CsrfMiddleware` group đã có từ CMS-045
  - [x] `themes/default/views/admin/pages/users/{list,create,edit}.php` — `list.php` có thêm dropdown gán role inline (không có route/form riêng cho Assign Role theo đúng route table đã duyệt)
  - [x] `tests/Core/AdminUserManagementUiTest.php` (12 test, đúng danh sách Owner yêu cầu)
  - [x] **Fix sau PHPUnit thật**: `testCreateUserDuplicateEmailRendersFormAgainWithoutCreating` FAIL (302 thay vì 200) — Root Cause Analysis xác nhận **không phải lỗi `UserCreateController`** (logic bắt `QueryException` đúng thiết kế) mà do `tests/Core/AdminUserManagementUiTest.php::migrate()` thiếu `CREATE UNIQUE INDEX uq_users_email ON users (email)` (có ở `ModuleUserIntegrationTest.php` nhưng bị bỏ sót khi viết test mới) — sửa đúng 1 dòng trong test, không đụng Controller
  - [x] Không sửa `core/*`, `modules/User|Auth|Role/*`, `database/*`, `composer.json`, `phpunit.xml` — đúng phạm vi đã khoá qua toàn bộ vòng lặp
  - [x] **Technical Debt ghi nhận** (Owner Decision, chưa xử lý): (1) `UserCreateController` (Admin) và `Modules\User\CreateUserController` (API) trùng lặp transaction logic; (2) chưa có User Service Layer; (3) chưa có Admin UI trang chi tiết 1 user (`GET /admin/users/{id}`)
  - [x] **Phát hiện & sửa discrepancy đánh số**: dòng roadmap cũ liệt kê "CMS-046 Public Website Polish" (nhãn tạm thời từ thời CMS-044, chưa triển khai) — trùng số với CMS-046 (Admin User Management UI) vừa hoàn thành. Bỏ nhãn số khỏi mục cũ (chưa triển khai, chưa có code), giữ nguyên mô tả, tránh trùng lặp CMS-XXX — cùng cách xử lý đã áp dụng cho discrepancy CMS-032 trước đây
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `AdminUserManagementUiTest`: 12 tests/28 assertions; toàn bộ suite: 472 tests, 868 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **CMS-046 COMPLETED — tag `v0.0.46`**
- [x] **CMS-047** — Admin Role Management UI (`modules/Admin/Role*Controller.php` + `themes/default/views/admin/pages/roles/`)
  - [x] Architecture Analysis (đọc trực tiếp `modules/Role/*`, `modules/Admin/*`, `modules/User/*`, `core/Authorization.php`/`core/Router.php`/`core/View.php`/`core/Session.php`, `tests/Core/ModuleRoleIntegrationTest.php`, `tests/Core/AdminUserManagementUiTest.php`, migration `roles`/`permissions`/`role_permissions`): xác nhận route table 8 route không collision (khác method/khác literal segment cuối); phát hiện `Modules\Role\AssignPermissionController` **chỉ ADD, không có endpoint REMOVE nào trong toàn bộ dự án**; phát hiện migration thật `roles` có cột `is_system` nhưng **không Controller nào trong `modules/Role/*` đọc/ghi cột này** (dùng `tenant_id IS NULL` làm nguồn sự thật)
  - [x] Owner Decision (3 điểm qua Owner Approval, đều chọn phương án A đề xuất): (1) Permission Assignment — chỉ gán, không gỡ, không sửa `modules/Role/*`; (2) Delete Role lỗi — trả HTML rõ lý do (`403`/`409`), không redirect im lặng, vì Delete là hành động phá huỷ; (3) System Role — dùng `tenant_id IS NULL`, không dùng `is_system`
  - [x] `modules/Admin/{RoleListController,RoleShowCreateController,RoleCreateController,RoleShowEditController,RoleUpdateController,RoleDeleteController,RoleShowPermissionsController,RoleAssignPermissionsController}.php` — 8 Controller mới, copy logic từ `Modules\Role\*Controller` tương ứng (không sửa file gốc). `RoleShowPermissionsController` cho phép xem permissions của System Role (200, "View allowed" — Owner Decision 3 gốc CMS-038), chỉ `POST` assign mới chặn 403
  - [x] `modules/Admin/routes.php` (mở rộng) — 8 route mới, route ghi bọc `CsrfMiddleware` group đã có từ CMS-045
  - [x] `themes/default/views/admin/pages/roles/{list,create,edit,permissions}.php` — `list.php` ẩn nút Sửa/Xoá cho System Role; `permissions.php` chỉ hiển thị nút "Gán" cho permission chưa gán (không nút "Gỡ")
  - [x] `tests/Core/AdminRoleManagementUiTest.php` (14 test, đúng danh sách Owner yêu cầu)
  - [x] **Root Cause Analysis 2 vòng sau PHPUnit thật** (5 rồi 2 test FAIL, đều `419` thay vì response mong đợi): xác nhận **không phải bug `CsrfMiddleware`/`core/Csrf.php`/Controller** — CSRF hoạt động đúng thiết kế, chặn đúng request thiếu token hợp lệ. Vòng 1: 4 test lấy token qua trang List nhưng `actingAs()` thiếu quyền `role.view` (List trả 403, không có form/token) — thêm `role.view`; 1 test gọi POST 2 lần kiểm tra idempotent nhưng lần 2 cố lấy token mới từ trang không còn form (permission đã chuyển sang "Đã gán") — tái dùng token lần đầu (không tự rotate trong session). Vòng 2: 2 test system-role chỉ seed đúng 1 System Role, khiến trang List không có form Delete nào (`list.php` chỉ render form Delete cho role KHÔNG PHẢI system) — seed thêm 1 Tenant Role "vô hại" để có form/token. Toàn bộ fix chỉ trong `tests/Core/AdminRoleManagementUiTest.php`, không đụng production code
  - [x] Không sửa `core/*`, `modules/Auth|User|Role/*`, `database/*`, `composer.json`, `phpunit.xml` — đúng phạm vi đã khoá qua toàn bộ vòng lặp
  - [x] **Technical Debt ghi nhận** (Owner Decision, chưa xử lý): (1) chưa có remove permission workflow (giới hạn hệ thống, không riêng Admin UI); (2) cột `roles.is_system` tồn tại nhưng không dùng (dead column); (3) `modules/Admin/Role*Controller.php` trùng lặp logic với `modules/Role/*` (JSON) — cùng loại Technical Debt đã ghi nhận ở CMS-046 cho User
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `AdminRoleManagementUiTest`: 14 tests/32 assertions; toàn bộ suite: 486 tests, 900 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **CMS-047 COMPLETED — tag `v0.0.47`**
- [x] **CMS-041** — Media Library (`database/migrations/2026_08_03_000001_create_media_table.php` + `modules/Media/`)
  - [x] Architecture Analysis (đọc `07-module-media.md`/`database-design.md` mục 4, `composer.json`, `core/Http/Request.php`, `core/Validator.php`, `bin/bootstrap.php`, migration `pages`): xác nhận `storage/` chưa có thư mục lưu file upload; `sites.storage_used_bytes` tồn tại từ CMS-028 nhưng chưa dùng; `composer.json` không có thư viện xử lý ảnh/S3 SDK/Queue — spec gốc (resize/WebP/S3/`ImageProcessor`/`StorageDriver`) không khả thi nếu không thêm dependency; `pages` không tham chiếu `media` (không `featured_image_id`) — chưa có consumer thật nào để `media_usages` theo dõi
  - [x] Owner Decision (9 điểm qua Owner Approval): MVP chỉ bảng `media` (không `media_folders`/`media_variants`/`media_usages`/S3/Queue); `media.update` (PATCH alt/title/caption) nằm trong CMS-041, không Admin UI; validate file thủ công trong Controller (bắt buộc file, size ≤ 5MB, mime whitelist `jpeg/png/gif/pdf`), không Validator rule mới; kích hoạt `sites.storage_used_bytes` (cộng khi upload, trừ khi xoá, cùng transaction); storage `storage/app/media/{tenant_id}/{tên_file_duy_nhất}`; route phẳng `GET/POST /media`, `PATCH/DELETE /media/{id}` (không `/api/v1`); permission `media.view/upload/update/delete`
  - [x] Final Design Confirmation (Phase 2 riêng trước Implementation): xác nhận danh sách file cuối cùng, mô tả Controller responsibility, transaction flow Upload (move file → transaction INSERT+UPDATE storage_used_bytes → lỗi thì unlink dọn) và Delete (transaction DELETE+UPDATE storage_used_bytes → commit xong mới unlink), xác nhận không Core change, xác nhận route collision (kế thừa đúng rủi ro đã chấp nhận từ `/users`/`/roles`/`/pages`, không cần sửa `modules/Public/*`)
  - [x] `modules/Media/{module.json,routes.php,ListMediaController,UploadMediaController,UpdateMediaController,DeleteMediaController}.php` — 4 Controller đúng Controller Contract, `Database` trực tiếp, không Service/Repository
  - [x] **Phát hiện & xử lý xung đột kỹ thuật trong lúc code** (dùng `AskUserQuestion` xin quyết định trước khi tiếp tục): `move_uploaded_file()` chỉ hoạt động với upload HTTP thật, luôn trả `false` trong môi trường CLI/PHPUnit — không thể test "Upload success" như Owner yêu cầu. Owner chọn Phương án A: thêm 1 nhánh điều kiện `is_uploaded_file() ? move_uploaded_file() : rename()` trong `UploadMediaController` — giữ `move_uploaded_file()` cho request thật (bảo mật), `rename()` chỉ kích hoạt ngoài HTTP thật — không abstraction/dependency mới
  - [x] `tests/Core/ModuleMediaIntegrationTest.php` (13 test) — `ModuleManager` trỏ `modules/` thật, `UploadMediaController`/`DeleteMediaController` override qua `Container::singleton()` với thư mục storage TEMP riêng (không ghi file thật vào `storage/app/media` của repo, đúng pattern `View` CMS-044/045)
  - [x] **Root Cause Analysis + Fix sau PHPUnit thật**: `ModuleMediaIntegrationTest` PASS ngay (13 tests/35 assertions); toàn bộ suite FAIL 3 test trong `tests/Core/RealMigrationsTest.php` — xác nhận **không phải bug migration/production** (migration mới `2026_08_03_000001_create_media_table` đã chạy/rollback đúng thứ tự thật theo output PHPUnit) mà do `EXPECTED_ORDER` chưa cập nhật — đúng root cause đã gặp ở CMS-040 khi thêm `pages` — sửa đúng 1 dòng, thêm migration mới vào cuối `EXPECTED_ORDER`
  - [x] Không sửa `core/*`, `modules/Auth|User|Role|Page|Public|Admin/*`, `database/*` (trừ migration mới), `composer.json`, `phpunit.xml` — đúng phạm vi đã khoá qua toàn bộ vòng lặp
  - [x] **Technical Debt ghi nhận** (Owner Decision, chưa xử lý): (1) `rename()` fallback trong `UploadMediaController` chỉ phục vụ CLI/testing, không phải hành vi production thật (production luôn dùng `move_uploaded_file()`); (2) chưa có image processing (resize/variant/WebP) — cần thêm dependency mới nếu triển khai; (3) chưa có `media_usages` (theo dõi nơi sử dụng) — chưa có consumer thật để theo dõi, cần làm khi Page/Post thật sự tham chiếu `media`
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `ModuleMediaIntegrationTest`: 13 tests/35 assertions; toàn bộ suite: 499 tests, 940 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **CMS-041 COMPLETED — tag `v0.0.48`**
- [x] **CMS-042** — Menu Management (`database/migrations/2026_08_04_00000{1,2}_*.php` + `modules/Menu/`)
  - [x] Architecture Analysis (đọc `08-module-menu.md`/`database-design.md` mục 5.1, `core/Router.php`/`core/ThemeManager.php`, `modules/Page|Public|Admin/*`, `themes/default/views/layouts/main.php`): xác nhận `menu_items.type` spec gốc (`post_category`/`product_category`) tham chiếu Module chưa tồn tại; `ThemeManager`/`theme.json` không có cơ chế khai báo `location_key` hợp lệ; layout Public hiện không có chỗ render menu (không `modules/Public/*` nào sẵn sàng tích hợp); phát hiện rủi ro collision MỚI nếu sau này thêm route public `GET /menus/{location}` cùng "shape" với `GET /menus/{id}` (admin) — khác hẳn rủi ro `/{slug}` đã biết trước đây
  - [x] Owner Decision (10 điểm qua Owner Approval): permission hạt mịn `menu.view/create/update/delete` (không `menu.manage`); không Admin UI, không Public rendering, không sửa `modules/Public/*`/theme layout trong CMS-042; không route public nào cho Menu; `type` chỉ `page`/`custom`; `location_key` chuỗi tự do (không cơ chế Theme khai báo); CRUD từng item (không bulk-replace/kéo-thả); không Hook/Cache; chỉ 2 bảng `menus`/`menu_items`
  - [x] `modules/Menu/{module.json,routes.php,ListMenusController,CreateMenuController,ShowMenuController,UpdateMenuController,DeleteMenuController,CreateMenuItemController,UpdateMenuItemController,DeleteMenuItemController}.php` — 8 Controller, `Database` trực tiếp, không Service/Repository/Interface/Helper/Trait. `ShowMenuController` dựng cây bằng PHP thuần (1 query, gom theo `parent_id`, không recursive SQL/N+1). Route `/menu-items` tách tiền tố riêng khỏi `/menus` để né rủi ro collision đã phát hiện ở Architecture Analysis
  - [x] `DeleteMenuController`: `Database::transaction()` (2 `DELETE` liên quan, không dựa FK CASCADE thật). `DeleteMenuItemController`: BFS gom id con cháu chỉ dùng `SELECT`, kết quả 1 câu `DELETE ... WHERE id IN (...)` duy nhất — không transaction (chỉ 1 câu ghi). `UpdateMenuItemController` chặn self-parent (`parent_id === id` → 422, yêu cầu bổ sung của Owner)
  - [x] `bin/bootstrap.php` mở rộng `$permissionKeys` 20 → 24 (thêm `menu.*`)
  - [x] `tests/Core/ModuleMenuIntegrationTest.php` (20 test) — `ModuleManager` trỏ `modules/` thật, cùng pattern `ModulePageIntegrationTest`/`ModuleMediaIntegrationTest`
  - [x] **Fix sau PHPUnit thật**: `tests/Core/RealMigrationsTest.php::EXPECTED_ORDER` thiếu 2 migration mới (gây 3 failure toàn suite) — đúng root cause đã gặp ở CMS-040/041 (test-expectation chưa cập nhật, không phải bug migration) — sửa đúng 2 dòng, không đụng migration/`modules/Menu/*`/`core/*`
  - [x] Không sửa `core/*`, `modules/Auth|User|Role|Page|Public|Admin|Media/*`, `composer.json`, `phpunit.xml`, `themes/*`, migration cũ — đúng phạm vi đã khoá qua toàn bộ vòng lặp
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `ModuleMenuIntegrationTest`: 20 tests/42 assertions; toàn bộ suite: 519 tests, 992 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **CMS-042 COMPLETED — tag `v0.0.49`**
- [x] **CMS-043** — SEO Management (`database/migrations/2026_08_05_000001_create_seo_meta_table.php` + `modules/Seo/`)
  - [x] Architecture Analysis (đọc `10-module-seo.md`, `database-design.md` mục 5.3, `modules/Page|Menu|Public|Admin|Media/*`, `core/Router.php`/`core/ModuleManager.php`/`core/Database.php`/`core/Authorization.php`/`core/TenantManager.php`): xác nhận `entity_type` spec gốc (`post`/`product`) tham chiếu Module chưa tồn tại; phát hiện quan trọng nhất — `/sitemap.xml`/`/robots.txt` (route tĩnh 1-segment) sẽ bị `GET /{slug}` (Public) nuốt mất nếu Module `Seo` load sau `Public` theo thứ tự alphabet mặc định, cách khắc phục duy nhất đòi hỏi chạm `modules/Public/module.json` (đã khoá); `redirects` đòi hỏi can thiệp toàn cục vào luồng dispatch (không phải CRUD đơn thuần)
  - [x] Owner Decision qua Architecture Analysis + Final Design (2 vòng Owner Approval): MVP chỉ bảng `seo_meta`, **không** `redirects`/`sitemap_cache`/`/sitemap.xml`/`/robots.txt`/Hook/Cache/Admin UI/Public rendering; `entity_type` chỉ `page`; permission chỉ `seo.view`/`seo.update` (không `seo.manage`/`seo.create`/`seo.delete` — upsert-theo-entity, không có hành động tạo/xoá độc lập); upsert bằng `SELECT` rồi rẽ nhánh `INSERT`/`UPDATE`, không transaction/retry/lock; `og_image_id` FK `ON DELETE SET NULL` (khả thi vì `media` đã tồn tại); `schema_data` lưu `TEXT` (JSON string, giống `pages.content`)
  - [x] `modules/Seo/{module.json,routes.php,ShowSeoMetaController,UpdateSeoMetaController}.php` — 2 Controller, `Database` trực tiếp, không Service/Repository/Interface/Trait/Helper. Route param `entity_type`/`entity_id` gộp vào `$data` để validate qua `Validator` sẵn có (không so sánh thủ công riêng lẻ). Chưa có `seo_meta` → `200`/`data: null` (không phải lỗi); `entity_type` không hỗ trợ hoặc entity không tồn tại/khác tenant → `404`
  - [x] `bin/bootstrap.php` mở rộng `$permissionKeys` 24 → 26 (thêm `seo.view`/`seo.update`)
  - [x] `tests/Core/ModuleSeoIntegrationTest.php` (17 test) — `ModuleManager` trỏ `modules/` thật, cùng pattern `ModuleMenuIntegrationTest`/`ModuleMediaIntegrationTest`
  - [x] **Fix sau PHPUnit thật**: `tests/Core/RealMigrationsTest.php::EXPECTED_ORDER` thiếu `2026_08_05_000001_create_seo_meta_table` (gây 3 failure toàn suite) — đúng root cause đã gặp ở CMS-040/041/042 (test-expectation chưa cập nhật, không phải bug migration) — sửa đúng 1 dòng, không đụng migration/`modules/Seo/*`/`core/*`
  - [x] Không sửa `core/*`, `modules/Auth|User|Role|Page|Menu|Media|Public|Admin/*`, `composer.json`, `phpunit.xml`, `themes/*`, migration cũ — đúng phạm vi đã khoá qua toàn bộ vòng lặp
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `ModuleSeoIntegrationTest`: 17 tests/28 assertions; toàn bộ suite: 536 tests, 1025 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **CMS-043 COMPLETED — tag `v0.0.50`**
- [x] **Public Website Polish** (`modules/Public/{HomeController,PublicPageController}.php` + `themes/default/views/{layouts/main.php,pages/404.php}`)
  - [x] Architecture Analysis (PHASE 1 — đọc toàn bộ `modules/Public|Page|Menu|Seo|Media/*`, `themes/default/*`, `core/Router.php`/`core/View.php`/`core/ModuleManager.php`): xác nhận Public trước đó chỉ render `pages` thuần (không Menu/SEO/404 theme/Header/Footer); phát hiện route collision tiềm ẩn giữa `GET /media` (1-segment) và `GET /{slug}` (Public) hiện chỉ an toàn tình cờ nhờ thứ tự alphabet `glob()`, không phải hợp đồng chính thức; xác nhận Media Module không có route công khai phục vụ file (chặn tính năng Media URL)
  - [x] Owner Decision (PHASE 2, phân loại Bắt buộc/Nên làm/Hoãn cho 16 hạng mục): **Bắt buộc** — 404 themed page; **Nên làm** — Header/Footer khung tĩnh, Navigation Menu + Active menu, SEO meta injection (title/description/canonical), Open Graph (không `og:image`), JSON-LD; **Hoãn** — Breadcrumb, Media URL, Search, `/sitemap.xml`, `/robots.txt`, Redirect support, Asset optimization. Qua `AskUserQuestion` xác nhận thêm 4 điểm mở: `location_key='header'` (không làm Footer menu); Fallback `seo_meta` Option A (bỏ qua hoàn toàn nếu chưa có); bỏ hẳn `og:image`; ẩn hẳn `<nav>` nếu chưa có Menu
  - [x] `modules/Public/HomeController.php`/`PublicPageController.php`: SELECT `pages` bổ sung cột `id`; `render404()` (method mới, fallback `View::exists('pages.404')` → theme hoặc text thuần); `fetchSeoMeta()` (query `seo_meta`, `json_decode` `schema_data`); `fetchNavigation()` (query `menus`+`menu_items`+ page slug lookup `IN (...)`, không N+1, dựng cây bằng PHP thuần — copy logic từ `Modules\Menu\ShowMenuController::buildTree()`, trùng lặp có chủ đích, không tái sử dụng chéo Module); đánh dấu `active` cho menu item trỏ đúng page đang xem. Toàn bộ trùng lặp y hệt giữa 2 Controller (không Service/Trait/Helper dùng chung)
  - [x] `themes/default/views/pages/404.php` (mới) — theme cho 404. `themes/default/views/layouts/main.php` — thêm `<meta description>`/`<link canonical>`/`<meta og:title|og:description>` (không `og:image`)/JSON-LD (`json_encode` với `JSON_HEX_TAG|JSON_HEX_APOS|JSON_HEX_QUOT|JSON_HEX_AMP` — không dùng `$this->e()`, chặn `</script>` breakout XSS)/`<header><nav>` 2 cấp (ẩn nếu rỗng)/`<footer>` khung tĩnh rỗng (chưa có nguồn dữ liệu site name để tự điền)
  - [x] `tests/Core/PublicPageRenderingTest.php` — `migrate()` bổ sung 3 bảng `menus`/`menu_items`/`seo_meta` (bắt buộc vì Controller giờ luôn query, thiếu bảng gây lỗi toàn bộ file, không riêng test menu/seo); không thêm/bớt test method
  - [x] Không sửa `core/*`, `modules/Auth|User|Role|Page|Menu|Media|Seo|Admin/*`, `composer.json`, `phpunit.xml`, `database/*` — đúng phạm vi đã khoá
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30) — `PublicPageRenderingTest`: 8 tests/14 assertions (không đổi số lượng); toàn bộ suite: 536 tests, 1025 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **Public Website Polish COMPLETED — tag `v0.0.51`**
- [x] **Nợ tài liệu ghi nhận từ v0.0.51 — ĐÓNG DỨT ĐIỂM tại Phase 6**: (1) Local Demo Foundation (`bin/load_env.php`, `bin/create_database.php`, `bin/seed_demo.php`, `SETUP_LOCAL.md`, `.env.example`) — code đã chạy thật, Owner đã xác nhận qua screenshot từ trước Phase 3; (2) 2 Critical Bug Fix môi trường thật — `MigrationManager::runInTransactionIfSupported()` (MySQL/MariaDB DDL implicit-commit) và `core/Middleware/StartSessionMiddleware.php` (Session chưa bao giờ được start trong request pipeline thật) — đã ghi nhận chi tiết trong `DEPLOYMENT.md` mục 6 (Phase 6); (3) UI Kit Tech Green/Dark (`public/assets/css/*`, `public/assets/js/app.js`, toàn bộ theme Public+Admin đã áp style) — không phát sinh quyết định kiến trúc mới cần ghi riêng ngoài các mục Admin UI đã có (3.32 trở đi). Không có code nào thay đổi khi đóng mục này — thuần tài liệu hồi tố cho việc đã chạy thật từ lâu.
- [x] **Pages Management Admin UI** (`modules/Admin/Page*Controller.php` + `themes/default/views/admin/pages/pages/`)
  - [x] `modules/Admin/{PageListController,PageShowCreateController,PageCreateController,PageShowEditController,PageUpdateController,PageDeleteController,PagePublishController,PageSetHomepageController}.php` — 8 Controller mới, copy logic từ `Modules\Page\*Controller` tương ứng (không sửa file gốc), không Service/Repository/Interface/Trait/Helper mới
  - [x] `modules/Admin/routes.php` (mở rộng) — 3 GET + 5 POST route mới, route ghi bọc `CsrfMiddleware` group đã có từ CMS-045; không collision với route 3-segment tĩnh (`/admin/pages/create`)
  - [x] **Quyết định Rich Text** (qua `AskUserQuestion`, 2 vòng): dùng thư viện Quill.js qua CDN (không thêm Composer/npm dependency, chỉ nạp ở 2 view Create/Edit qua 2 yield point mới `head_extra`/`scripts_extra` bổ sung vào `admin/layouts/main.php`); đổi quy ước `pages.content` từ `{"text":...}` sang `{"html":...}` — thuần quy ước tầng Application/View, không sửa `modules/Page/*` (Validator JSON API chỉ kiểm `'content' => 'nullable|array'`)
  - [x] `themes/default/views/admin/pages/pages/{list,create,edit}.php` — `list.php` có toggle Publish/Unpublish + "Đặt trang chủ" + Delete (`data-confirm`); `create.php`/`edit.php` tích hợp Quill.js
  - [x] `themes/default/views/admin/partials/sidebar.php` — mở khoá link "Pages" (bỏ badge "Soon")
  - [x] `themes/default/views/pages/default.php` (Public site) — 4 nhánh fallback render content: `content['html']` (raw HTML, tin cậy tác giả Admin — cùng mô hình WordPress, đã Owner chấp thuận) → `content['text']` (escaped, tương thích ngược `bin/seed_demo.php`) → mảng generic (JSON dump) → scalar/null
  - [x] `tests/Core/AdminPageManagementUiTest.php` (11 test)
  - [x] **Fix sau PHPUnit thật**: `testCreatePageMissingPermissionReturns403Html` FAIL (419 thay vì 403) — Root Cause: `CsrfMiddleware` chạy trước Controller trong pipeline (`modules/Admin/routes.php`), request thiếu `_token` hợp lệ bị chặn ở tầng CSRF trước khi chạm logic permission — không phải bug Controller/Middleware order. Sửa: lấy CSRF token hợp lệ trực tiếp qua `Core\Csrf::token()` trong test (không qua form, vì user test không có quyền xem form). Chỉ sửa 1 test method, không đụng Controller/Core
  - [x] Không sửa `core/*`, `modules/Auth|User|Role|Page|Menu|Media|Seo|Public/*`, `database/*`, `composer.json`, `phpunit.xml`, `bin/bootstrap.php` (permission `page.*` đã có sẵn từ CMS-040) — đúng phạm vi đã khoá
  - [x] **Technical Debt ghi nhận**: `modules/Admin/Page*Controller.php` trùng lặp logic với `modules/Page/*` (JSON) — cùng loại Technical Debt đã ghi nhận ở CMS-046/047 cho User/Role
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật — `AdminPageManagementUiTest`: 11 tests/24 assertions; toàn bộ suite: 547 tests, 1049 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **Pages Management Admin UI COMPLETED — tag `v0.0.52`**
- [x] **Media Manager Admin UI** (`modules/Admin/Media*Controller.php` + `themes/default/views/admin/pages/media/`)
  - [x] Plan Review & Owner Approval (100%): thứ tự Phase 3.2/3.3/3.4 chốt Media → Menu → SEO; qua `AskUserQuestion` xác nhận thêm route mới `GET /admin/media/{id}/file` (serve file cho preview Grid) vì Public site chưa có Media URL
  - [x] `modules/Admin/{MediaListController,MediaFileController,MediaUploadController,MediaUpdateController,MediaDeleteController}.php` — 5 Controller mới, copy logic từ `Modules\Media\*Controller` tương ứng (không sửa file gốc), không Service/Repository/Interface/Trait/Helper mới
  - [x] `modules/Admin/routes.php` (mở rộng) — 2 GET (`/admin/media`, `/admin/media/{id}/file`) + 3 POST route mới, route ghi bọc `CsrfMiddleware` group đã có từ CMS-045
  - [x] `themes/default/views/admin/pages/media/list.php` — Grid ảnh/file, inline edit form (alt/title/caption), Upload Modal drag-drop (vanilla JS, progressive enhancement)
  - [x] `themes/default/views/admin/partials/sidebar.php` — mở khóa link "Media" (bỏ badge "Soon")
  - [x] `public/assets/css/components.css`/`public/assets/js/app.js` — bổ sung style Grid/Modal + handler modal/drag-drop (tái dùng token/pattern Phase 2, không thêm dependency)
  - [x] `tests/Core/AdminMediaManagementUiTest.php` (11 test), `storagePath` override qua `Container::singleton()` thư mục TEMP riêng, cùng pattern `ModuleMediaIntegrationTest`
  - [x] Không sửa `core/*`, `modules/Auth|User|Role|Page|Menu|Seo|Media|Public/*`, `database/*`, `composer.json`, `phpunit.xml`, `bin/bootstrap.php` (permission `media.*` đã có sẵn từ CMS-041) — đúng phạm vi đã khóa
  - [x] **Technical Debt ghi nhận**: `modules/Admin/Media*Controller.php` trùng lặp logic với `modules/Media/*` (JSON) — cùng loại Technical Debt đã ghi nhận ở CMS-046/047/Pages Admin UI
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật — `AdminMediaManagementUiTest`: 11 tests/26 assertions; toàn bộ suite: 558 tests, 1075 assertions, 0 Errors/Failures, 4 Skipped đúng thiết kế (Redis) → **Media Manager Admin UI COMPLETED — tag `v0.0.53`**
- [x] **Menu Builder Admin UI** (`modules/Admin/Menu*Controller.php` + `modules/Admin/MenuItem*Controller.php` + `themes/default/views/admin/pages/menus/`)
  - [x] Plan Review & Owner Approval (100%); qua `AskUserQuestion` xác nhận co che luu ket qua sap xep: **keo-tha that + AJAX (fetch) luu ngay** (khong chon phuong an form truyen thong doi so thu tu) — lan dau du an dung AJAX trong Admin UI
  - [x] `modules/Admin/{MenuListController,MenuCreateController,MenuShowController,MenuUpdateController,MenuDeleteController}.php` — 5 Controller CRUD Menu, copy logic tu `Modules\Menu\*Controller`
  - [x] `modules/Admin/{MenuItemCreateController,MenuItemUpdateController,MenuItemDeleteController}.php` — 3 Controller CRUD Menu Item, copy logic tu `Modules\Menu\*ItemController`. `MenuItemUpdateController` phuc vu 2 use case (Edit form thuong -> redirect; Drag-drop AJAX -> JSON) qua `Request::ajax()` co san tu CMS-015, khong tao Controller/route thu 2 trung logic
  - [x] `modules/Admin/routes.php` (mo rong) — 2 GET + 6 POST route moi, tach tien to `/admin/menu-items/*` khoi `/admin/menus/*` dung tien le phong collision tu CMS-042
  - [x] `themes/default/views/admin/pages/menus/{list,show}.php` — List (form Create inline) + Show (cay `menu_items` long nhau, form Add Item, keo-tha HTML5 `draggable` thuan)
  - [x] `themes/default/views/admin/partials/sidebar.php` — mo khoa link "Menu"; `public/assets/css/components.css`/`public/assets/js/app.js` — bo sung style tree + handler `fetch()` drag-drop (khong thu vien drag-drop ngoai)
  - [x] `tests/Core/AdminMenuManagementUiTest.php` (18 test)
  - [x] **Tu phat hien va sua trong luc code (truoc khi giao Owner test)**: (1) ban nhap dau `show.php` khai bao `function flatten()`/`function renderNodes()` o top-level file view — vi `View::renderTemplate()` dung `include` (khong `include_once`), se Fatal "Cannot redeclare function" khi view render lan 2 trong cung tien trinh PHP; sua bang closure cuc bo. (2) `Validator`'s `nullable` chi bo qua khi gia tri `=== null` (khong bo qua `''`) — ca `<select>` "Cap goc" va drag-drop "tha ve goc" deu gui `parent_id=''`, se luon bi tu choi; sua bang chuan hoa `''` -> `null` truoc validate
  - [x] **Fix sau PHPUnit thuc te**: `testUpdateMenuCrossTenantReturns404`, `testUpdateMenuItemCrossTenantReturns404` FAIL (419 thay vi 404) — cung nguyen nhan da gap o Pages Admin UI (`CsrfMiddleware` chan truoc khi Controller kip kiem tra tenant ownership) — sua bang lay token hop le qua `Core\Csrf::token()` truc tiep trong 2 test, khong dung Controller/Core
  - [x] Khong sua `core/*`, `modules/Auth|User|Role|Page|Media|Seo|Menu|Public/*`, `database/*`, `composer.json`, `phpunit.xml`, `bin/bootstrap.php` (permission `menu.*` da co san tu CMS-042) — dung pham vi da khoa
  - [x] **Technical Debt ghi nhan**: (1) `modules/Admin/Menu*Controller.php`/`MenuItem*Controller.php` trung lap logic voi `modules/Menu/*` (JSON) — cung loai Technical Debt da ghi nhan o Page/Media Admin UI; (2) drag-drop chi doi `parent_id`, khong tinh lai `sort_order` chi tiet giua cac anh em cung cap — don gian hoa co chu dich, chua co yeu cau that ve thu tu hien thi tinh vi
  - [x] **Verified** — `vendor/bin/phpunit` PASS tren moi truong that — `AdminMenuManagementUiTest`: 18 tests/37 assertions; toan bo suite: 576 tests, 1112 assertions, 0 Errors/Failures, 4 Skipped dung thiet ke (Redis) → **Menu Builder Admin UI COMPLETED — tag `v0.0.54`**
- [x] **SEO Meta Settings Admin UI** (`modules/Admin/Seo*Controller.php` + `themes/default/views/admin/pages/seo/`) — **PHASE 3 HOAN TAT** (Pages/Media/Menu/SEO — badge "Soon" cuoi cung da go khoi sidebar)
  - [x] Plan Review & Owner Approval (100%); qua `AskUserQuestion` xac nhan pham vi: yeu cau ban dau nhac "Global SEO Settings" (Site Title format/Robots.txt/Sitemap toggle) khong ton tai trong `modules/Seo/*` hay schema DB — Owner chon giu dung pham vi hien co (chi SEO Meta tung Page), khong mo bang/Controller moi cho Global Settings
  - [x] `modules/Admin/{SeoListController,SeoShowEditController,SeoUpdateController}.php` — 3 Controller, copy logic upsert tu `Modules\Seo\UpdateSeoMetaController` y het (SELECT roi INSERT/UPDATE, khong transaction), `entity_type` co dinh `'page'`
  - [x] `modules/Admin/routes.php` (mo rong) — 2 GET + 1 POST route moi
  - [x] `themes/default/views/admin/pages/seo/{list,edit}.php` — List (badge Da/Chua cau hinh) + form (title/description/canonical/OG image/schema type/schema data qua textarea JSON tho)
  - [x] `themes/default/views/admin/partials/sidebar.php` — mo khoa "SEO" (badge "Soon" cuoi cung)
  - [x] `tests/Core/AdminSeoManagementUiTest.php` (16 test)
  - [x] **Tu phat hien va sua trong luc code**: cung bug pattern `Validator`'s `nullable` khong bo qua `''` da gap o Menu Builder — `<select>` OG Image "-- Khong co --" gui `og_image_id=''`, luon bi tu choi truoc khi cham logic; sua bang chuan hoa `''` -> `null` truoc validate
  - [x] Khong sua `core/*`, `modules/Auth|User|Role|Page|Media|Menu|Seo|Public/*`, `database/*`, `composer.json`, `phpunit.xml`, `bin/bootstrap.php` (permission `seo.*` da co san tu CMS-043) — dung pham vi da khoa
  - [x] **Technical Debt ghi nhan**: (1) `modules/Admin/Seo*Controller.php` trung lap logic voi `modules/Seo/*` (JSON) — cung loai Technical Debt da ghi nhan o Page/Media/Menu Admin UI; (2) "Global SEO Settings" (Site Title format/Meta Description mac dinh/OG Image mac dinh/Robots.txt/Sitemap toggle) van la khoang trong — chua co bang/Module nao, can Architecture Analysis rieng neu trien khai trong tuong lai
  - [x] **Verified** — `vendor/bin/phpunit` PASS tren moi truong that — `AdminSeoManagementUiTest`: 16 tests/32 assertions; toan bo suite: 592 tests, 1144 assertions, 0 Errors/Failures, 4 Skipped dung thiet ke (Redis) → **SEO Meta Settings Admin UI COMPLETED — tag `v0.0.55`**
- [x] **Phase 4: Global Settings & SEO Infrastructure (Milestone v0.0.56)** — [DONE]
  - [x] Schema extension cho SEO Meta (`og_title`, `og_description`, `is_index`, `is_follow`) — migration `2026_08_08_000001_alter_seo_meta_add_og_robots_fields.php`, cap nhat `modules/Seo/UpdateSeoMetaController.php` (JSON API) + `modules/Admin/SeoUpdateController.php`/view (Admin UI)
  - [x] Migration `site_settings` table (1 ban ghi/tenant) — `2026_08_09_000001_create_site_settings_table.php`
  - [x] `SiteSettingsManager` Service Layer (`modules/Settings/SiteSettingsManager.php`) — Service Layer dau tien cua du an, runtime array cache theo tenant
  - [x] Dynamic `/sitemap.xml` (`SitemapController`) va `/robots.txt` (`RobotsController`)
  - [x] Giai quyet Route Collision Public qua `dependencies: ["settings"]` trong `modules/Public/module.json` — `Settings` boot truoc `Public`, dam bao `/sitemap.xml`/`/robots.txt` khop truoc `GET /{slug}`
  - [x] Admin UI Global Settings (`modules/Admin/{SettingShowEditController,SettingUpdateController}.php` + view) + muc "Cai dat chung" trong Sidebar
  - [x] `site_name` inject vao logo header Public layout (khong tu sinh fallback description/OG/favicon — giu nguyen Option A tu Public Website Polish)
  - [x] `tests/Core/ModuleSettingsIntegrationTest.php` (9 test, gom test route-collision truc tiep), `tests/Core/AdminSettingsManagementUiTest.php` (9 test)
  - [x] **Fix sau PHPUnit that**: `ModuleSettingsIntegrationTest` FAIL 3 test (`NOT NULL constraint failed: pages.created_by`) — loi o helper `seedPage()` cua chinh test moi (thieu `created_by`), khong phai loi Controller/Migration. Sua: them `seedUser()` + bo sung `created_by`
  - [x] **Verified** — `vendor/bin/phpunit` PASS tren moi truong that — toan bo suite: 613 tests, 1208 assertions, 0 Errors/Failures, 4 Skipped dung thiet ke (Redis) → **Phase 4 COMPLETED — tag `v0.0.56`**
- [x] **Phase 5: Public Engine Polish & Public Media Delivery (Milestone v0.0.57)** — [DONE]
  - [x] Architecture Analysis + Final Design (Owner Approval) — phat hien va sua 1 rui ro bao mat nghiem trong trong de xuat goc: `GET /media/{tenant_id}/{filename}` doi thanh `GET /media/{filename}` (bo `tenant_id` khoi URL, luon lay tu `TenantManager::id()` qua domain) de chan IDOR
  - [x] `modules/Media/MediaServeController.php` (moi) + `modules/Media/routes.php` (mo rong) — `GET /media/{filename}`, khop theo `media.path` (ten file vat ly duy nhat), khong phai `media.file_name` (ten goc, khong duy nhat trong 1 tenant); Cache-Control/ETag/Last-Modified, khong lam 304 That (Owner Decision MVP)
  - [x] `modules/Public/BreadcrumbBuilder.php` (moi) — walk nguoc `pages.parent_id`, MAX_DEPTH=20 chong vong lap vo han; tach class dung chung giua HomeController/PublicPageController (ngoai le tien le trung lap method, do Owner Decision)
  - [x] `modules/Public/SearchController.php` (moi) + `themes/default/views/pages/search.php` — `GET /search?q=`, LIKE tren title/content, status=published, deleted_at IS NULL, LIMIT 50, escape XSS
  - [x] `modules/Public/module.json` (mo rong `dependencies` them `"media"`) + `HomeController.php`/`PublicPageController.php` (inject BreadcrumbBuilder, resolve OG image/favicon URL qua Media Serve Route) + `themes/default/views/layouts/main.php` (`<meta robots>`, favicon, og:image fallback) + `themes/default/views/pages/default.php` (render breadcrumb)
  - [x] `tests/Core/PublicMediaServeTest.php` (8 test), `tests/Core/PublicSearchTest.php` (10 test), `tests/Core/PublicPageRenderingTest.php` (+6 test) — bao gom fixture theme (`tests/Fixtures/themes/test-theme/`) cap nhat song song vi test dung fixture rieng, khong phai `themes/default/` (CMS-044 Decision 4)
  - [x] **Root Cause Analysis dung vs RCA de xuat sai**: Owner de xuat sua SQL trong `SearchController.php` (thieu `deleted_at IS NULL`/`LIMIT 50`) nhung xac minh truc tiep source cho thay CA HAI DA CO SAN dung tu dau — loi that o 2 assertion trong test (view echo lai query nguoi dung gay nhieu chuoi assert). Sua dung 2 dong test, khong dung Controller
  - [x] Khong sua `core/*`, `modules/Media/{List,Upload,Update,Delete}MediaController.php`, `bin/bootstrap.php` — dung pham vi da khoa
  - [x] **Verified** — `vendor/bin/phpunit` PASS tren moi truong that — toan bo suite: 636 tests, 1247 assertions, 0 Errors/Failures, 4 Skipped dung thiet ke (Redis) → **Phase 5 COMPLETED — tag `v0.0.57`**
- [x] **Phase 6: Release Preparation & Technical Debt Clearance (Milestone `v0.0.58`) — [DONE]**
  - [x] Architecture Analysis + Final Design (Owner Approval 100%) — 3 fork chot: (1) Pilot Action Class tren `Page` truoc khi nhan rong; (2) CI matrix chi PHP 8.2+8.3 (da verify that, khong them 8.1 chua tung test); (3) `roles.is_system` chi danh dau deprecated, khong `DROP COLUMN`
  - [x] **Pilot Action Class Pattern** (`modules/Page/Actions/{PageActionException,PageNotFoundException,PageValidationException,CreatePageAction,UpdatePageAction,DeletePageAction,PublishPageAction,SetHomepageAction}.php`) — refactor 5 cap Controller (`modules/Page/*` JSON + `modules/Admin/Page*Controller.php` Admin) inject Action qua Constructor Injection, giu nguyen tang Response. Hop nhat 2 discrepancy hanh vi nho giua JSON/Admin goc (da xac nhan qua doc test suite - khong test nao assert vao 2 nhanh nay, an toan hop nhat): xu ly `template`/`parent_id` rong khi Create, cau truc `errors` cho loi slug trung/parent khong hop le
  - [x] **CI/CD**: `.github/workflows/phpunit.yml` — matrix PHP 8.2 + 8.3, `pdo_sqlite`+`mbstring`, chay tren push/PR vao `main`/`develop`
  - [x] **`DEPLOYMENT.md`** (moi) — 8 muc: Yeu cau he thong, Web Server Single Domain Multi-tenancy, Multi-tenancy DNS, Environment Variables (bang doi chieu bat buoc doi khi len Production), File Storage Permissions, Database Migration an toan, Cronjobs (xac nhan chua co tac vu dinh ky nao), Checklist Go-Live
  - [x] **Sua `.gitignore`**: bo sung `/storage/app/media/*` (giu `.gitkeep`) — phat hien trong luc viet `DEPLOYMENT.md` rang thu muc upload that chua tung duoc gitignore dung cach (khac `storage/cache|logs|framework` da co rule tu truoc)
  - [x] **Dinh chinh tai lieu cu phat hien trong luc code**: `core-architecture.md` (muc Role Model, gan CMS-037) tung ghi sai "migration khong co cot `is_system`" — doc lai truc tiep `database/migrations/2026_08_01_000004_create_roles_table.php:19` xac nhan cot **co ton tai that**, chi la khong Controller nao dung. Da sua lai dung + danh dau chinh thuc deprecated
  - [x] `core-architecture.md` — muc 3.44 (Pilot Action Class Pattern), danh dau Standby chinh thuc cho `core/Cache.php`/`core/Hook.php` (muc 3.7/3.8)
  - [x] **Verified** — `vendor/bin/phpunit` PASS tren moi truong that — `ModulePageIntegrationTest`: 17 tests/27 assertions; `AdminPageManagementUiTest`: 11 tests/24 assertions; toan bo suite: 636 tests, 1247 assertions, 0 Errors/Failures, 4 Skipped dung thiet ke (Redis), khong regression sau khi refactor 10 Controller da chay production → **Phase 6 COMPLETED — tag `v0.0.58`**
  - [ ] Quyet dinh nhan rong Action Class Pattern ra 24 cap Controller con lai — hoan toi khi Owner danh gia ket qua pilot that
- [x] **Phase 7: UI/UX Demo Polish & Enterprise Showcase Pack (Milestone `v0.0.59`) — [DONE]**
  - [x] Architecture Analysis + Final Design (Owner Approval 100%) — 3 fork chot: (1) A1 Landing Page dung `content['html']` co san qua Seeder, khong sua schema/Admin UI; (2) B1 mo rong `bin/seed_demo.php` procedural + `bin/add_site.php` moi, khong tao `database/seeders/` class-based; (3) C1 Activity Stream chi Page+Media+User (da co timestamp that), khong gom Menu (can migration)
  - [x] **Public Landing Page & Theme Polish**: `public/assets/css/public.css` bo sung `.hero-cta`/`.hero-mockup`/`.feature-grid`/`.feature-card`/`.showcase-block`/`.cta-footer` + breakpoint Tablet (992px) — CSS3 thuan, dung token Design System san co, khong dependency moi
  - [x] `themes/default/views/pages/default.php`: khong render `<h1>{title}</h1>` chung khi noi dung dung `content['html']` (tranh trung lap voi heading rieng trong Hero Section) — trang `content['text']`/scalar giu nguyen hanh vi cu
  - [x] **Enterprise Seeder**: `bin/seed_demo.php` viet lai hoan toan — 2 content pack that (`tech`: SaaS CMS Technology Co., `restaurant`: Green Gourmet Restaurant & Cafe), nhan tham so `[domain] [pack]`, tuong thich nguoc voi cach goi cu. Xoa toan bo text ky thuat ("Test Page")
  - [x] `bin/add_site.php` (moi) — tao Tenant thu 2 tro di, tai su dung Admin User + System Admin Role da co (`roles.tenant_id IS NULL`), khong sua `bin/bootstrap.php` (chi chay duoc 1 lan)
  - [x] `SETUP_LOCAL.md` +muc 12 — huong dan demo Multi-tenant 2 domain (`cms.test` + `restaurant.test`)
  - [x] **Admin Dashboard nang cap**: `modules/Admin/DashboardController.php` bo sung `page_count`/`media_count`/Activity Stream (UNION Page+Media+User, `LIMIT 8`), giu nguyen `user_count`/`role_count`. `themes/default/views/admin/pages/dashboard.php`: 4 Metric Card, Quick Action Bar, bang Activity Stream
  - [x] `tests/Core/PublicLandingPageTest.php` (moi, 4 test) — dung `themes/default/` THAT (khong phai fixture, vi fixture khong ho tro render `content['html']` qua `$this->raw()`)
  - [x] `tests/Core/AdminUiFoundationTest.php` — mo rong assertion Dashboard (+`page_count`/`media_count`) + 1 test moi xac nhan so lieu/Activity Stream phan anh dung du lieu seed
  - [x] **Fix sau PHPUnit that (2 vong)**: (1) tu phat hien truoc khi giao Owner test — `AdminUiFoundationTest.php` thieu bang `pages`/`media` (DashboardController gio luon query 2 bang nay); (2) **bac bo RCA sai tu Owner lan thu 2 trong du an** — Owner de xuat doi `DashboardController.php` sang `users.registered_at AS created_at`, xac minh truc tiep migration that (`2026_08_01_000003_create_users_table.php:21`) xac nhan cot `created_at` ton tai that, KHONG co cot `registered_at` nao trong toan bo codebase. Nguyen nhan that: bang `users` tu tao rieng trong `AdminUiFoundationTest.php::migrate()` thieu cot `created_at` — sua dung 1 dong trong test fixture, KHONG dung `DashboardController.php`
  - [x] Khong migration moi (dung khoa "KHONG MIGRATION" cua Owner cho Phase 7) — khong sua `core/*`, khong them dependency ben thu ba, giu nguyen CSS3 thuan
  - [x] **Verified** — `vendor/bin/phpunit` PASS tren moi truong that — `AdminUiFoundationTest`: 8 tests/24 assertions; toan bo suite: 641 tests, 1264 assertions, 0 Errors/Failures, 4 Skipped dung thiet ke (Redis) → **Phase 7 COMPLETED — tag `v0.0.59`**
- [x] **Phase 8: Beta Release Readiness & Staging Walkthrough (Milestone `v0.1.0-beta`) — [DONE]**
  - [x] Architecture Analysis + Final Design (Owner Approval 100%) — 2 phat hien ky thuat lam ro truoc khi viet tai lieu: (1) `public/uploads/` khong phai noi luu Media that (chi con lai trong `.gitignore` cu, that su la `storage/app/media/`); (2) Wildcard Certificate khong ap dung duoc cho mo hinh domain doc lap cua du an (khong phai subdomain cung 1 goc) — chot dung SAN da-domain qua certbot
  - [x] `DEMO_WALKTHROUGH.md` (moi) — kich ban Demo 4 buoc cho Sales/Founder: Landing Page Showcase, Multi-Tenant Real-time Switch, Admin Dashboard & Content, Menu Builder & SEO Automation — moi buoc co Muc tieu/Thao tac/Key Selling Points/Dieu kien chuan bi
  - [x] `STAGING_CHECKLIST.md` (moi) — checklist thao tac Web Server Setup (+tao `public/.htaccess` cho Apache), SSL/HTTPS Multi-Domain (certbot SAN, khong Wildcard), Data Initialization (chuoi lenh dung thu tu phu thuoc), Permissions & Environment — tham chieu nguoc `DEPLOYMENT.md`, khong lap lai noi dung
  - [x] Khong sua bat ky file PHP nao — chi 2 tai lieu Markdown moi, khong migration
  - [x] **Verified** — `vendor/bin/phpunit` PASS tren moi truong that — toan bo suite: 641 tests, 1264 assertions, 0 Errors/Failures, 4 Skipped dung thiet ke (Redis), khong regression (dung nhu du kien vi khong dung code PHP) → **Phase 8 COMPLETED — tag `v0.1.0-beta`**
- [x] **Phase 9: Production Go-Live & Monitoring (Milestone `v0.1.0`) — [DONE]**
  - [x] Architecture Analysis + Final Design (Owner Approval 100%) — phat hien quan trong: `GET /health` **da ton tai that** tu Core Foundation (`core/Application.php`, ngoai `TenantResolverMiddleware`), khong phai xay moi; 3 fork chot deu theo huong "thuan tai lieu": (1) A1 giu nguyen `/health` liveness, khong nang cap Readiness Check; (2) B1 chi khuyen nghi Nginx `limit_req` theo IP, khong wiring `RateLimiter` (Session-based, khong chan duoc script khong gui cookie) vao `LoginController`; (3) chi tai lieu huong dan `mysqldump` thu cong, khong viet `bin/backup.php`
  - [x] `PRODUCTION_MONITORING.md` (moi) — 4 muc: Health Check & Uptime Monitoring (UptimeRobot/Pingdom/systemd/Docker), Log Rotation (`/etc/logrotate.d/cms` day du directive), Backup & Disaster Recovery (`mysqldump`/`tar` + CronJob `0 2 * * *`), Security Hardening (Nginx `limit_req_zone` chong brute-force `/admin/login`, HTTP Security Headers kem luu y CSP phai whitelist `cdn.quilljs.com`, phan tich minh bach gioi han that cua `RateLimiter`)
  - [x] Khong sua bat ky file PHP nao — chi 1 tai lieu Markdown moi, khong migration
  - [x] **Verified** — `vendor/bin/phpunit` PASS tren moi truong that — 641 tests, 1264 assertions, 0 Errors/Failures, 4 Skipped dung thiet ke (Redis), khong regression (dung nhu du kien) → **Phase 9 COMPLETED — tag `v0.1.0`**
- [x] **Phase 10: Final Release Verification, Merge to Main & Launch Handoff — [DONE]**
  - [x] `HANDOFF_NOTES.md` (moi) — System Executive Summary, Key Assets & Entry Points, Quick Start 3 lenh (kem luu y minh bach: can hoan tat chuan bi ha tang truoc, khong phai toan bo quy trinh rut gon con 3 lenh), Post-Launch Maintenance Roadmap (Redis Cache Driver that, Async Queue, i18n, nhan rong Action Class Pattern, Plugin/Hook kich hoat, Media Thumbnail, Rate Limiting IP-based, Analytics)
  - [x] **Phat hien va tao moi `README.md`** — chua tung ton tai trong toan bo du an tu truoc toi gio, du Owner yeu cau "cap nhat neu can" (khong phai chi cap nhat, phai tao moi hoan toan) — lien ket toi toan bo tai lieu du an
  - [x] Huong dan Git Merge/Push Tag/Don nhanh feature — chi huong dan, KHONG tu thuc thi (dung nguyen tac "Git guidance only, never auto-commit/push/tag" da giu xuyen suot du an)
  - [x] Khong sua bat ky file PHP nao — chi tai lieu Markdown, khong migration
  - [x] **Verified** — `vendor/bin/phpunit` can Owner xac nhan lai lan cuoi truoc khi merge vao `main`
- [ ] Event / Queue — chưa đánh số CMS; Event có nghi vấn trùng lặp với `Hook` (xem `core-architecture.md` mục 6), Queue để sau khi Foundation hoàn tất (Owner Decision CMS-025)
- [ ] ThemeManager Integration / Content Schema Migration tiếp tục / Database query logging / `PluginManager::getFailures()` logging / Domain normalization / Super Admin domain bypass / `AuthorizationMiddleware` tham số hoá thật / CSRF cho `/login`+`/logout` / `roles` UNIQUE fix (#20) / Module/Plugin theo Site (#9) / Multi-site session (#26) — chưa đánh số CMS, để dành sau khi có nhu cầu thật

## Phase 2 — Database Migration thật (bảng `sites`, `users`, `roles`...)

- [x] **CMS-028** — Database Migration Phase 2 — Tenant/Auth/Role foundation (`database/migrations/2026_08_01_00000{1..7}_*.php`)
  - [x] Roadmap Review sau CMS-027 liệt kê 5 candidate (A. Database Migration, B. Integration/Wiring, C. Event Dispatcher, D. Queue, E. Module System) — Owner chọn Candidate A, lý do: rủi ro thấp nhất (dùng nguyên `MigrationManager` đã có từ CMS-013, chưa từng chạy migration thật), mở khoá nhiều candidate khác (B/E phụ thuộc trực tiếp)
  - [x] Owner Decision phạm vi: đúng 7 bảng nhóm Tenant/Auth/Role (`sites/site_domains/users/roles/permissions/role_permissions/user_site_roles`), KHÔNG gồm `login_logs/password_resets/personal_access_tokens` (thuộc Auth enhancement, chưa cần để mở khoá TenantManager/Auth cơ bản), KHÔNG tạo bảng `plans` (`sites.plan_id` giữ nullable, không FK — YAGNI, chưa có requirement Billing/SaaS thật), KHÔNG seed data, migration chỉ tạo schema (DDL thuần, không business logic/không Trigger — đúng `database-design.md` mục 6)
  - [x] Architecture Analysis phát hiện xung đột kỹ thuật thật: schema `database-design.md` giả định MySQL 8+ (`ENUM`/`BIGINT UNSIGNED`/`ON UPDATE CURRENT_TIMESTAMP`) nhưng toàn bộ test suite dự án luôn chạy SQLite in-memory (`tests/Fixtures/config/database.php`) — không thể PHPUnit-verify migration MySQL-only syntax trong môi trường hiện tại
  - [x] Owner Decision (Final Design): Phương án A — Portable SQL, loại `ENUM`→`VARCHAR` (validate ở Service layer sau), loại `UNSIGNED`, loại `ON UPDATE CURRENT_TIMESTAMP`→`TIMESTAMP NULL` (Service tự set). Riêng mệnh đề Primary Key auto-increment (`AUTO_INCREMENT` MySQL vs `AUTOINCREMENT` SQLite — không có cú pháp chung) được Owner approve cho phép **duy nhất 1 điểm rẽ nhánh driver** trong mỗi migration Closure qua `$db->connection()->getAttribute(\PDO::ATTR_DRIVER_NAME)` (dùng `Database::connection(): PDO` đã public sẵn từ CMS-004, không sửa `MigrationManager`/`Database`) — không cho phép rẽ nhánh thêm ở phần schema khác nếu chưa có Architecture Issue Report riêng
  - [x] 7 migration file: `001_create_sites_table`, `002_create_site_domains_table` (FK→sites CASCADE), `003_create_users_table`, `004_create_roles_table` (FK→sites CASCADE), `005_create_permissions_table`, `006_create_role_permissions_table` (composite PK, FK→roles/permissions CASCADE), `007_create_user_site_roles_table` (FK→users/sites CASCADE, FK→roles RESTRICT) — đúng thứ tự dependency, đúng Index/Unique theo `database-design.md` mục 2
  - [x] `tests/Core/RealMigrationsTest.php` (mới, 11 test) — chạy `MigrationManager` trỏ thẳng `database/migrations/` thật (khác `MigrationManagerTest.php` dùng fixture riêng), SQLite in-memory thật không mock: đủ 7 bảng đúng thứ tự, đúng cột (`PRAGMA table_info`), UNIQUE constraint từng bảng, composite PK `role_permissions`, rollback đúng thứ tự ngược, migrate lại sau rollback (idempotent), `status()` phản ánh đúng applied/pending
  - [x] **Failure Analysis thật** (phát hiện qua PHPUnit thật, không suy đoán): `testRolesTenantNameUniqueConstraintIsEnforced` FAIL lần chạy đầu — nguyên nhân xác định KHÔNG PHẢI bug migration: UNIQUE `(tenant_id, name)` đúng ANSI SQL semantics, `NULL ≠ NULL` trong composite UNIQUE nên 2 role hệ thống (`tenant_id = NULL`) cùng tên KHÔNG bị chặn ở cả SQLite lẫn MySQL — test ban đầu giả định sai, không phải khác biệt driver. Owner approve sửa DUY NHẤT test (đổi kịch bản sang `tenant_id = 1` — role theo tenant cụ thể, đúng phạm vi UNIQUE thật sự enforce), không sửa migration/schema
  - [x] Technical Debt ghi nhận (không sửa trong CMS-028): `roles.tenant_id` nullable + UNIQUE `(tenant_id, name)` không ngăn được trùng tên role hệ thống khi `tenant_id IS NULL` — để dành CMS Role/Auth Service sau này xử lý ở Service layer (đúng nguyên tắc "không Trigger cho business logic" đã áp dụng cho ràng buộc homepage ở `database-design.md` mục 6.1)
  - [x] Không sửa `MigrationManager.php`/`Database.php`/bất kỳ Core Component nào đã tag version — 0 file Core cũ bị đụng, đúng phạm vi Foundation đã duyệt
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — `RealMigrationsTest`: 11 tests/51 assertions; toàn bộ suite: 372 tests, 648 assertions, 0 Errors/Failures/Warnings/Risky/Deprecations, 4 Skipped đúng thiết kế (Redis) → **CMS-028 COMPLETED — tag `v0.0.28`**

Phần còn lại của Phase 2 (Content/Media/Menu/Form/SEO/Theme/Plugin/Settings) chưa bắt đầu, chưa đánh số CMS. Xem `database-design.md` mục 3-5.

## Phase 3+ — Module Auth/User/Role/Page/SEO...

Tương ứng CMS-014 → CMS-019 ở trên và các module tiếp theo tham khảo `00-master-spec.md`.

Từ PHASE 4 trở đi (Global Settings, Public Engine Polish, Release Prep, UI/UX Demo Polish, Beta Readiness, Production Monitoring, Final Handoff, Visual Page Builder), tiến độ chi tiết theo dõi ở `CHANGELOG.md` (theo version) và `core-architecture.md` mục 3 (theo Component) — không lặp lại checklist ở đây.

- [x] **CMS-048** — Visual Page Builder (PHASE 11) — Block Builder kéo-thả cho Admin Page (6 loại block: `heading`/`paragraph`/`image`/`hero`/`feature_grid`/`cta`), song song Quill.js Rich Text, không migration/không sửa JSON API (`pages.content` là cột JSON tự do, CMS-040). Chi tiết: `CHANGELOG.md` mục `[0.1.1]`, `core-architecture.md` mục 3.47.
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — 656 tests, 1305 assertions, 0 Errors, 0 Failures, 4 Skipped (Redis, đúng thiết kế) → **CMS-048 COMPLETED — tag `v0.1.1`**
  - [x] Lưu ý ID: nhánh Git checkout tên `feature/CMS-034-visual-page-builder`, nhưng `CMS-034` đã dùng cho Auth Module trước đó (mục CMS-034 ở trên) — đánh số lại đúng là CMS-048 trong tài liệu, không đổi tên nhánh đã checkout.

- [x] **CMS-049** — Advanced Analytics Dashboard (PHASE 12) — bảng `analytics_views` mới, `modules/Analytics/AnalyticsService.php` (Service Layer thứ 2 sau `SiteSettingsManager`), `AnalyticsTrackingMiddleware` ghi log Public site (silent-fail, chỉ status 200), Admin Dashboard hiển thị Total Views/Unique Visitors/Top Pages/biểu đồ SVG 7 ngày (Zero External JS Chart Library). Chi tiết: `CHANGELOG.md` mục `[0.1.2]`, `core-architecture.md` mục 3.48.
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — 666 tests, 1341 assertions, 0 Errors, 0 Failures, 4 Skipped (Redis, đúng thiết kế) → **CMS-049 COMPLETED — tag `v0.1.2`**
  - [x] Lưu ý quy trình: khác mọi Phase trước, phần lớn Phase này được triển khai trực tiếp trên nhánh `main` (không qua `feature/CMS-049-*` riêng) — đã báo lại cho Owner xác nhận, chưa rõ đây là chủ đích hay sai lệch quy trình.

- [x] **CMS-050** — Multi-language Support (i18n) & Localization MVP (PHASE 13) — Translation Table Pattern (chọn qua Architecture Review riêng, thắng JSON Column Pattern ở 5/5 tiêu chí), bảng `page_translations` mới, `core/I18n/Translator.php` (fallback locale + interpolation, static holder cô lập phục vụ helper `__()`), `core/Middleware/LocaleDetectionMiddleware.php` (route param → query → session → cookie → default), route công khai `/{locale}/...`, Tab ngôn ngữ VI/EN trong Admin Page Editor. Chi tiết: `CHANGELOG.md` mục `[0.1.3]`, `core-architecture.md` mục 3.49.
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — 689 tests, 1389 assertions, 0 Errors, 0 Failures, 4 Skipped (Redis, đúng thiết kế) → **CMS-050 COMPLETED — tag `v0.1.3`**
  - [x] Ghi nhận rủi ro kiến trúc (không chặn, xem `core-architecture.md` mục 3.49): pattern route `/{locale}/{slug}` về cấu trúc có thể khớp nhầm `/admin/...` (2 segment) — an toàn ở thứ tự nạp module hiện tại nhưng phụ thuộc thứ tự thư mục `modules/`, không phải bất biến hệ thống đảm bảo.

- [x] **CMS-051** — Comment/Review System (PHASE 14) — bảng `comments` mới (polymorphic `entity_type`/`entity_id`, MVP chỉ `'page'`), `modules/Public/CommentSubmitController.php` (route POST đầu tiên của Public module, CSRF + RateLimiter 5 lần/10 phút/session, `status='pending'` mặc định — moderation-first), 4 Controller Admin Moderation (List/Approve/Reject/Delete), Public render comment `approved` + Form gửi (chỉ `PublicPageController`, không `HomeController` — Owner Decision phạm vi). 3 permission mới `comment.view/moderate/delete`. Chi tiết: `CHANGELOG.md` mục `[0.1.4]`, `core-architecture.md` mục 3.50.
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — 715 tests, 1446 assertions, 0 Errors, 0 Failures, 4 Skipped (Redis, đúng thiết kế) → **CMS-051 COMPLETED — tag `v0.1.4`**
  - [x] 2 vòng Root Cause Analysis từ PHPUnit thật, cả 2 đều ở test (không phải code production): (1) `Csrf::token()` gọi vô điều kiện trong `PublicPageController` sẽ vỡ 2 test suite cũ chưa `Session::start()` — tự phát hiện trước khi giao Owner; (2) 3 lỗi test (`RouteNotFoundException` do nhét query string vào URI, `guest_name` trùng label UI, CSRF token không parse được từ list rỗng) — sửa bằng lấy token trực tiếp qua `Core\Csrf::token()` (Container).

- [x] **CMS-052** — Notification & Email System (PHASE 15) — `Core\Mail\MailerDriver` (interface) + `LogMailerDriver`/`SmtpMailerDriver` (tự viết qua fsockopen, không thư viện ngoài)/`ArrayMailerDriver` (test double), `Core\Mail\Mailer` (facade, silent-fail tuyệt đối), bảng `notifications` mới (in-app, polymorphic), `modules/Admin/NotificationService.php` (`notifyAdmins()`/`markAsRead()`/`unreadCount()`), 3 email template, tích hợp vào 3 Controller Comment Phase 14. Chi tiết: `CHANGELOG.md` mục `[0.1.5]`, `core-architecture.md` mục 3.51.
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — 736 tests, 1499 assertions, 0 Errors, 0 Failures, 4 Skipped (Redis, đúng thiết kế) → **CMS-052 COMPLETED — tag `v0.1.5`**
  - [x] Root Cause Analysis từ PHPUnit thật: `Core\View::resolvePath()` không chấp nhận dấu `-` trong tên template (`NAME_PATTERN`) — 3 file email đặt tên gạch ngang khiến `ViewNotFoundException` bị `Mailer::send()` bắt và silent-fail âm thầm, chỉ lộ ra qua `assertCount` sai số lượng. Đổi tên file sang underscore, không sửa `Core\View`.

- [x] **CMS-053** — Security & Audit Log System (PHASE 16) — bảng `audit_logs` mới (tenant_id/user_id nullable, old/new values JSON), `core/Security/AuditLogger.php` (Service inject qua constructor, KHÔNG static facade — giữ đúng nguyên tắc "không static/global mutable state"), `modules/Admin/AuditLogController.php` (lọc event/date range, phân trang thủ công đầu tiên của dự án), tích hợp ghi log vào 8 Controller (Login/Logout/Page CUD/Comment Approve-Reject/Settings). Chi tiết: `CHANGELOG.md` mục `[0.1.6]`, `core-architecture.md` mục 3.52.
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — 752 tests, 1538 assertions, 0 Errors, 0 Failures, 4 Skipped (Redis, đúng thiết kế) → **CMS-053 COMPLETED — tag `v0.1.6`**
  - [x] 3 điều chỉnh so với đặc tả gốc (trình bày ở Architecture Analysis trước khi code, Owner đã duyệt): instance method thay vì static; bỏ event "đổi mật khẩu" (tính năng chưa tồn tại); không có ngoại lệ SuperAdmin xem xuyên-tenant (cơ chế chưa được xây dựng ở bất kỳ đâu). Zero-regression tự nhiên — `AuditLogger` là class cụ thể (không interface), không cần sửa test cũ nào.
  - [x] Root Cause Analysis từ PHPUnit thật (chỉ ở test, không phải code production): 2 assertion `assertStringNotContainsString()` kiểm tra toàn bộ body đụng nhầm `<option>` trong dropdown lọc (dropdown liệt kê mọi event của tenant, đúng UX) — sửa bằng assertion nhắm `<span class="badge...">` (chỉ xuất hiện ở dòng dữ liệu thật).

- [x] **CMS-054** — System Settings & General Configurations (PHASE 17) — bảng `settings` mới (key-value tổng quát, đổi tên tránh trùng `site_settings` đã có từ Phase 4), `Modules\Settings\SettingManager.php` (Cache-aware qua `Core\Cache`, mã hoá AES-256-CBC qua `ext-openssl` cho giá trị nhạy cảm), Admin UI tại `/admin/system-settings` (đổi route tránh trùng `/admin/settings` đã đăng ký — nếu giữ nguyên sẽ `DuplicateRouteException` sập cả app), permission `settings.manage` mới tách biệt `settings.view`/`settings.update` cũ. Chi tiết: `CHANGELOG.md` mục `[0.1.7]`, `core-architecture.md` mục 3.53.
  - [x] **Verified** — `vendor/bin/phpunit` PASS trên môi trường thật (PHP 8.3.30, PHPUnit 10.5.64) — 772 tests, 1586 assertions, 0 Errors, 0 Failures, 4 Skipped (Redis, đúng thiết kế) → **CMS-054 COMPLETED — tag `v0.1.7`**
  - [x] 2 xung đột kỹ thuật tự phát hiện trước khi code (đổi tên bảng + route, xem chi tiết `core-architecture.md` mục 3.53) và 1 lỗi Container tự phát hiện trước khi giao Owner (không phải sau khi FAIL): `SettingManager` phụ thuộc `Core\Cache\CacheDriver` (interface) — đúng bài học `MailerDriver` Phase 15, đã đăng ký tường minh `FileCacheDriver` trong `AdminSettingTest.php` ngay từ đầu.
